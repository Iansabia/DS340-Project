---
phase: 07-experiments-and-interpretability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/run_experiment1_comparison.py
  - experiments/results/experiment1/cross_tier_summary.json
  - experiments/figures/experiment1_rmse_bar.png
  - experiments/figures/experiment1_pnl_curves.png
  - experiments/figures/experiment1_comparison_table.tex
autonomous: true
requirements: [EXP-01]

must_haves:
  truths:
    - "Cross-tier comparison table is produced with all 8 models ranked by RMSE"
    - "Bar chart visually compares RMSE across tiers with color-coded bars"
    - "Equity curves show cumulative P&L trajectories for all 8 models on the same axes"
    - "LaTeX table is ready for copy-paste into Phase 8 paper"
  artifacts:
    - path: "experiments/run_experiment1_comparison.py"
      provides: "Experiment 1 script: loads existing JSONs, produces figures + formatted outputs"
      min_lines: 80
    - path: "experiments/results/experiment1/cross_tier_summary.json"
      provides: "Consolidated summary JSON with all 8 models' metrics in one file"
    - path: "experiments/figures/experiment1_rmse_bar.png"
      provides: "Bar chart of RMSE by model, color-coded by tier"
    - path: "experiments/figures/experiment1_pnl_curves.png"
      provides: "Overlaid equity curves for all 8 models"
    - path: "experiments/figures/experiment1_comparison_table.tex"
      provides: "LaTeX-formatted comparison table"
  key_links:
    - from: "experiments/run_experiment1_comparison.py"
      to: "experiments/results/tier{1,2,3}/*.json"
      via: "load_all_results from results_store"
      pattern: "load_all_results"
---

<objective>
Formalize the existing 8-model cross-tier comparison into the centerpiece Experiment 1 deliverable. This is NOT retraining -- it loads the existing tier1/tier2/tier3 JSON results and produces publication-quality visualizations and a LaTeX table.

Purpose: The complexity-vs-performance comparison is the project's central research contribution. This plan synthesizes all model results into figures and tables ready for the Phase 8 paper.
Output: Summary JSON, RMSE bar chart, P&L equity curves, LaTeX table, formatted text table.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/evaluation/results_store.py:
```python
def load_all_results(results_dir: Path) -> list[dict]:
    """Load every .json file in results_dir, sorted by model_name."""

def load_results(path: Path) -> dict:
    """Load a single results JSON file."""
```

Result JSON schema (from existing tier1/xgboost.json):
```python
{
  "model_name": str,          # e.g. "XGBoost"
  "metrics": {
    "rmse": float,
    "mae": float,
    "directional_accuracy": float,
    "total_pnl": float,
    "num_trades": int,
    "win_rate": float,
    "sharpe_ratio": float,
    "sharpe_per_trade": float,
  },
  "timestamp": str,
  "threshold": float,          # 0.02
  "n_train_rows": int,         # 6802
  "n_test_rows": int,          # 1673
  "n_features": int,           # 31
  "pnl_series": list[float],   # cumulative P&L per bar
  # Tier 2/3 also have: seeds, seed_rmses, mean_rmse, std_rmse
}
```

Existing result directories:
- experiments/results/tier1/  (naive_spread_closes.json, volume_higher_volume_correct.json, linear_regression.json, xgboost.json)
- experiments/results/tier2/  (gru.json, lstm.json)
- experiments/results/tier3/  (ppo_raw.json, ppo_filtered.json)

Model display order (from run_baselines.py _MODEL_ORDER):
["Naive (Spread Closes)", "Volume (Higher Volume Correct)", "Linear Regression", "XGBoost", "GRU", "LSTM", "PPO-Raw", "PPO-Filtered"]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Experiment 1 cross-tier comparison script</name>
  <files>experiments/run_experiment1_comparison.py, experiments/results/experiment1/cross_tier_summary.json, experiments/figures/experiment1_rmse_bar.png, experiments/figures/experiment1_pnl_curves.png, experiments/figures/experiment1_comparison_table.tex</files>
  <read_first>
    - experiments/run_baselines.py (for _MODEL_ORDER and format_comparison_table)
    - src/evaluation/results_store.py (for load_all_results)
    - experiments/results/tier1/xgboost.json (schema reference)
    - experiments/results/tier2/gru.json (Tier 2 schema with seed_rmses)
    - experiments/results/tier3/ppo_raw.json (Tier 3 schema)
  </read_first>
  <action>
Create `experiments/run_experiment1_comparison.py` that:

1. **Loads all existing results** using `load_all_results` from `src.evaluation.results_store` for each of the 3 tier directories (`experiments/results/tier1/`, `tier2/`, `tier3/`). Combines into a single list.

2. **Produces a consolidated summary JSON** at `experiments/results/experiment1/cross_tier_summary.json` containing:
   - All 8 models' metrics in a single file
   - Tier label for each model (1, 2, or 3)
   - Ranking by RMSE (1=best)
   - For Tier 2/3 models, include `mean_rmse` and `std_rmse` from the `extra` field
   - Dataset metadata: n_train=6802, n_test=1673, n_features=31, threshold=0.02

3. **Creates RMSE bar chart** (`experiments/figures/experiment1_rmse_bar.png`):
   - Horizontal bar chart, models on y-axis, RMSE on x-axis
   - Color-coded by tier: blue=Tier 1, orange=Tier 2, red=Tier 3
   - Error bars for Tier 2/3 models using `std_rmse`
   - Title: "Experiment 1: RMSE by Model (Cross-Tier Comparison)"
   - Use matplotlib with `plt.tight_layout()`, dpi=150, figsize=(10, 6)

4. **Creates P&L equity curves** (`experiments/figures/experiment1_pnl_curves.png`):
   - Line plot with one line per model, all 8 on the same axes
   - X-axis: bar index (0 to len(pnl_series)), Y-axis: cumulative P&L
   - Color-coded by tier (same palette as bar chart)
   - Legend showing model name + final P&L value
   - Title: "Experiment 1: Cumulative P&L Equity Curves"
   - figsize=(12, 7), dpi=150

5. **Produces LaTeX table** (`experiments/figures/experiment1_comparison_table.tex`):
   - Columns: Model, Tier, RMSE, MAE, Dir Acc, P&L, Trades, Win Rate, Sharpe
   - Horizontal lines between tiers
   - Bold the best value in each column
   - Standard `\begin{table}...\end{tabular}...\end{table}` with caption

6. **Prints a formatted text table** to stdout (reusing the format from `format_comparison_table` in run_baselines.py).

The script should be runnable as: `python -m experiments.run_experiment1_comparison`

Use `matplotlib.use('Agg')` at the top to avoid GUI backend issues. Import `matplotlib.pyplot as plt` after setting backend.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && .venv/bin/python -m experiments.run_experiment1_comparison && test -f experiments/results/experiment1/cross_tier_summary.json && test -f experiments/figures/experiment1_rmse_bar.png && test -f experiments/figures/experiment1_pnl_curves.png && test -f experiments/figures/experiment1_comparison_table.tex</automated>
  </verify>
  <acceptance_criteria>
    - experiments/results/experiment1/cross_tier_summary.json exists and contains 8 model entries
    - experiments/figures/experiment1_rmse_bar.png exists and is >10KB (non-empty image)
    - experiments/figures/experiment1_pnl_curves.png exists and is >10KB
    - experiments/figures/experiment1_comparison_table.tex contains \begin{table} and 8 data rows
    - Script prints comparison table to stdout showing all 8 models
  </acceptance_criteria>
  <done>Experiment 1 cross-tier comparison is formalized with summary JSON, RMSE bar chart, equity curves, and LaTeX table all saved to disk</done>
</task>

</tasks>

<verification>
- `python -m experiments.run_experiment1_comparison` runs without error
- `experiments/results/experiment1/cross_tier_summary.json` has 8 model entries with tier labels and RMSE ranking
- Both PNG files are non-trivial (>10KB each)
- LaTeX table has proper formatting with 8 model rows
</verification>

<success_criteria>
- All 8 models appear in the comparison (4 Tier 1 + 2 Tier 2 + 2 Tier 3)
- RMSE ranking shows XGBoost near the top, PPO models near the bottom (consistent with Phase 5/6 findings)
- Figures are publication-quality with clear tier color coding
- LaTeX table is ready for copy-paste into the Phase 8 paper
</success_criteria>

<output>
After completion, create `.planning/phases/07-experiments-and-interpretability/07-01-SUMMARY.md`
</output>
