---
phase: 07.3-adaptive-trading-system
plan: 04
subsystem: infra
tags: [oracle-cloud, cron, deployment, bash, git-autocommit]

# Dependency graph
requires:
  - phase: 07.3-03
    provides: "trading_cycle.py, TradingStrategy, PositionManager, ContractClassifier"
provides:
  - "setup_oracle.sh: one-command Oracle VM deployment"
  - "oracle_keepalive.sh: free-tier reclamation prevention"
  - "oracle_autocommit.sh: hourly result push to GitHub"
  - "preflight_check.py: deployment artifact verification"
  - "GitHub Actions fallback with Oracle VM health check"
affects: [live-trading, data-collection, paper-trading]

# Tech tracking
tech-stack:
  added: [bash, cron, oracle-linux-9]
  patterns: [idempotent-setup, fallback-collection, cpu-keepalive]

key-files:
  created:
    - scripts/setup_oracle.sh
    - scripts/oracle_keepalive.sh
    - scripts/oracle_autocommit.sh
    - scripts/preflight_check.py
  modified:
    - .github/workflows/collect-and-trade.yml

key-decisions:
  - "HTTPS clone URL for Oracle VM (no SSH keys needed on VM)"
  - "Separate .venv-oracle to avoid torch contamination from dev .venv"
  - "Push to both master and main branches in autocommit (handles branch naming)"
  - "GitHub Actions skips collection if Oracle VM auto-committed within 30 minutes"

patterns-established:
  - "Idempotent setup: re-running setup_oracle.sh is safe (git pull if exists, reuse venv)"
  - "Cron architecture: three independent cron jobs (trading, keepalive, autocommit)"
  - "Oracle health check: grep git log for 'auto: trading' commits to detect VM activity"

requirements-completed: [ORACLE-DEPLOY, ORACLE-CRON, ORACLE-KEEPALIVE, ORACLE-AUTOCOMMIT]

# Metrics
duration: 3min
completed: 2026-04-08
---

# Phase 7.3 Plan 04: Oracle VM Deployment Summary

**Idempotent Oracle Cloud deployment with 15-min trading cron, CPU keepalive, hourly git auto-commit, and GitHub Actions fallback**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-08T22:48:05Z
- **Completed:** 2026-04-08T22:51:53Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created setup_oracle.sh: one-command VM deployment (Python 3.12, minimal deps, 3 cron jobs, model verification, no PyTorch)
- Created oracle_keepalive.sh (CPU burst every 10min) and oracle_autocommit.sh (hourly git push with position count)
- Updated GitHub Actions workflow as fallback: skips collection when Oracle VM is actively committing
- Created preflight_check.py to verify all 17 deployment artifacts before deploying

## Task Commits

Each task was committed atomically:

1. **Task 1: Oracle VM setup script, keep-alive, and auto-commit scripts** - `a02af9b0` (feat)
2. **Task 2: Update GitHub Actions workflow as fallback and verify deployment readiness** - `f2249353` (feat)

## Files Created/Modified
- `scripts/setup_oracle.sh` - One-command idempotent Oracle VM setup (Python 3.12, pip deps, cron, model verification)
- `scripts/oracle_keepalive.sh` - CPU burst to prevent free-tier VM reclamation
- `scripts/oracle_autocommit.sh` - Hourly git commit+push of trading data (positions.db, bars.parquet, history.jsonl)
- `scripts/preflight_check.py` - Pre-deployment artifact verification (17 required files)
- `.github/workflows/collect-and-trade.yml` - Added Oracle VM health check, conditional collection steps

## Decisions Made
- Used HTTPS clone URL on VM (SSH keys not available on Oracle VM)
- Separate .venv-oracle virtual environment to avoid any torch contamination from dev venv
- Auto-commit pushes to both master and main (handles branch naming flexibility)
- GitHub Actions checks for Oracle auto-commits within 30 minutes to decide whether to run fallback

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

**Oracle VM deployment requires manual SSH execution.**
Deploy with:
```bash
ssh -i /path/to/ssh-key.key opc@129.80.4.112 'bash -s' < scripts/setup_oracle.sh
```

## Next Phase Readiness
- All deployment scripts are ready and syntax-checked
- Models and live data artifacts verified present
- VM deployment can proceed via single SSH command
- GitHub Actions continues as hourly fallback if VM goes down

---
*Phase: 07.3-adaptive-trading-system*
*Completed: 2026-04-08*
