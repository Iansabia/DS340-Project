---
phase: 04-regression-baselines
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/models/linear_regression.py
  - src/models/xgboost_model.py
  - src/evaluation/results_store.py
  - tests/models/test_linear_regression.py
  - tests/models/test_xgboost_model.py
  - tests/evaluation/test_results_store.py
  - experiments/run_baselines.py
autonomous: true
requirements: [MOD-01, MOD-02]

must_haves:
  truths:
    - "Linear Regression is trained on the flat feature matrix and produces spread predictions on the test set"
    - "XGBoost is trained on the flat feature matrix and produces spread predictions on the test set"
    - "Both models are evaluated with RMSE, MAE, directional accuracy, and profit simulation"
    - "A comparison table shows all four Tier 1 models side by side with all metrics"
    - "Results are persisted to JSON for reproducibility and downstream consumption"
  artifacts:
    - path: "src/models/linear_regression.py"
      provides: "Linear Regression predictor wrapping sklearn"
      exports: ["LinearRegressionPredictor"]
    - path: "src/models/xgboost_model.py"
      provides: "XGBoost predictor wrapping xgboost"
      exports: ["XGBoostPredictor"]
    - path: "src/evaluation/results_store.py"
      provides: "Save/load evaluation results as JSON"
      exports: ["save_results", "load_results", "load_all_results"]
    - path: "experiments/run_baselines.py"
      provides: "End-to-end script training and evaluating all Tier 1 models"
  key_links:
    - from: "src/models/linear_regression.py"
      to: "src/models/base.py"
      via: "inherits BasePredictor"
      pattern: "class LinearRegressionPredictor.*BasePredictor"
    - from: "src/models/xgboost_model.py"
      to: "src/models/base.py"
      via: "inherits BasePredictor"
      pattern: "class XGBoostPredictor.*BasePredictor"
    - from: "experiments/run_baselines.py"
      to: "src/evaluation"
      via: "imports metrics, profit_sim, results_store"
      pattern: "from src.evaluation"
    - from: "experiments/run_baselines.py"
      to: "src/models"
      via: "imports all four Tier 1 models"
      pattern: "from src.models"
    - from: "src/evaluation/results_store.py"
      to: "experiments/"
      via: "saves JSON result files"
      pattern: "json.dump"
---

<objective>
Train Linear Regression and XGBoost on spread prediction, build a results storage layer, and create the baseline comparison script that produces the TA check-in results table.

Purpose: These are the first-class regression models per CLAUDE.md ("regression models are first-class, not afterthoughts"). The XGBoost model is the strongest tabular baseline and likely the best performer across all tiers. The comparison script generates a table showing all four Tier 1 models (Naive, Volume, Linear Regression, XGBoost) with regression metrics and profit simulation results. This is the deliverable for the April 4 TA check-in.

Output: LinearRegressionPredictor, XGBoostPredictor, results storage utilities, run_baselines.py experiment script.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-baselines/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that the executor needs. -->

From src/models/base.py:
```python
from abc import ABC, abstractmethod
import pandas as pd
import numpy as np

class BasePredictor(ABC):
    @abstractmethod
    def predict(self, X: pd.DataFrame) -> np.ndarray:
        """Return predicted spread changes, one per row."""
        ...

    @property
    @abstractmethod
    def name(self) -> str:
        """Human-readable model name for results tables."""
        ...

    def fit(self, X_train: pd.DataFrame, y_train: np.ndarray) -> "BasePredictor":
        """Train the model. Default no-op for baselines."""
        return self

    def evaluate(self, X_test: pd.DataFrame, y_test: np.ndarray) -> dict:
        """Run predict, then compute regression metrics and profit sim."""
        ...
```

From src/evaluation/metrics.py:
```python
def compute_regression_metrics(y_true: np.ndarray, y_pred: np.ndarray) -> dict:
    """Returns {"rmse": float, "mae": float, "directional_accuracy": float}"""
```

From src/evaluation/profit_sim.py:
```python
def simulate_profit(predictions: np.ndarray, actuals: np.ndarray, threshold: float = 0.02) -> dict:
    """Returns {"total_pnl": float, "num_trades": int, "win_rate": float, "sharpe_ratio": float, "pnl_series": list[float]}"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Linear Regression and XGBoost predictors with results storage</name>
  <files>
    src/models/linear_regression.py,
    src/models/xgboost_model.py,
    src/evaluation/results_store.py,
    tests/models/test_linear_regression.py,
    tests/models/test_xgboost_model.py,
    tests/evaluation/test_results_store.py
  </files>
  <read_first>
    src/models/base.py,
    src/models/naive.py,
    src/models/volume.py,
    src/evaluation/metrics.py,
    src/evaluation/profit_sim.py,
    tests/models/conftest.py,
    AGENTS.md
  </read_first>
  <behavior>
    LinearRegressionPredictor:
    - Test: name property returns "Linear Regression"
    - Test: fit() trains sklearn LinearRegression on X_train, y_train
    - Test: predict() returns np.ndarray with same length as X rows
    - Test: fit then predict on synthetic data produces reasonable values (not NaN, finite)
    - Test: works with evaluate() end-to-end (produces full metrics dict)
    - Test: raises RuntimeError if predict called before fit

    XGBoostPredictor:
    - Test: name property returns "XGBoost"
    - Test: fit() trains xgboost.XGBRegressor on X_train, y_train
    - Test: predict() returns np.ndarray with same length as X rows
    - Test: fit then predict on synthetic data produces reasonable values (not NaN, finite)
    - Test: works with evaluate() end-to-end (produces full metrics dict)
    - Test: raises RuntimeError if predict called before fit
    - Test: accepts hyperparameters via constructor (n_estimators, max_depth, learning_rate)

    results_store:
    - Test: save_results writes JSON with model_name, metrics, and timestamp
    - Test: load_results reads back identical dict
    - Test: load_all_results loads all JSON files from a directory into a list
    - Test: save_results creates output directory if it doesn't exist
  </behavior>
  <action>
    1. Create src/models/linear_regression.py with LinearRegressionPredictor(BasePredictor):
       - Constructor: stores sklearn LinearRegression instance (not fitted yet). self._fitted = False.
       - fit(X_train, y_train): calls self._model.fit(X_train.values, y_train). Sets self._fitted = True. Returns self.
       - predict(X): if not self._fitted, raise RuntimeError("Model must be fit before predict"). Returns self._model.predict(X.values).
       - name: "Linear Regression"

    2. Create src/models/xgboost_model.py with XGBoostPredictor(BasePredictor):
       - Constructor: accepts n_estimators=100, max_depth=6, learning_rate=0.1, random_state=42. Creates xgboost.XGBRegressor with these params. self._fitted = False.
       - fit(X_train, y_train): calls self._model.fit(X_train.values, y_train). Sets self._fitted = True. Returns self.
       - predict(X): if not self._fitted, raise RuntimeError("Model must be fit before predict"). Returns self._model.predict(X.values).
       - name: "XGBoost"

    3. Create src/evaluation/results_store.py:
       - save_results(model_name: str, metrics: dict, output_dir: Path, extra: dict | None = None) -> Path:
         Saves to output_dir/{model_name_slugified}.json with keys: model_name, metrics (the full dict from evaluate), timestamp (ISO8601), and any extra fields.
         Creates output_dir if needed. Returns the path to the saved file.
       - load_results(path: Path) -> dict: reads JSON from path.
       - load_all_results(results_dir: Path) -> list[dict]: reads all .json files in results_dir, returns list of dicts sorted by model_name.

    4. Follow TDD: write test_linear_regression.py, test_xgboost_model.py, test_results_store.py FIRST. Use the sample_features and sample_targets fixtures from tests/models/conftest.py (created in Plan 01). Run RED, then implement to GREEN.
  </action>
  <verify>
    <automated>cd /Users/iansabia/Desktop/DS340\ Project && .venv/bin/python -m pytest tests/models/test_linear_regression.py tests/models/test_xgboost_model.py tests/evaluation/test_results_store.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/models/linear_regression.py exists and contains "class LinearRegressionPredictor(BasePredictor)"
    - src/models/xgboost_model.py exists and contains "class XGBoostPredictor(BasePredictor)"
    - src/evaluation/results_store.py exists and contains "def save_results" and "def load_all_results"
    - LinearRegressionPredictor wraps sklearn.linear_model.LinearRegression
    - XGBoostPredictor wraps xgboost.XGBRegressor with configurable hyperparameters
    - Both raise RuntimeError if predict called before fit
    - Both work end-to-end with evaluate() method from BasePredictor
    - results_store save/load round-trips correctly
    - All new tests pass
  </acceptance_criteria>
  <done>
    Linear Regression and XGBoost predictors implement BasePredictor, train on feature matrices, and produce spread predictions. Results storage saves/loads evaluation results as JSON. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Baseline comparison experiment script</name>
  <files>
    experiments/__init__.py,
    experiments/run_baselines.py
  </files>
  <read_first>
    src/models/base.py,
    src/models/naive.py,
    src/models/volume.py,
    src/models/linear_regression.py,
    src/models/xgboost_model.py,
    src/evaluation/metrics.py,
    src/evaluation/profit_sim.py,
    src/evaluation/results_store.py
  </read_first>
  <action>
    1. Create experiments/__init__.py (empty package init).

    2. Create experiments/run_baselines.py as a CLI script that:
       a. Loads the processed flat feature matrix from data/processed/features_flat.parquet (the output of Phase 3).
       b. Splits into train/test based on temporal split. The data should already have a "split" column or a "time_idx" column. Use the split column if present; otherwise split at 80% of sorted time_idx. Raises clear error with helpful message if file not found or columns missing.
       c. Defines feature columns (all columns except "spread_target", "split", "time_idx", "pair_id", "timestamp") and target column ("spread_target").
       d. Instantiates all four models:
          - NaivePredictor()
          - VolumePredictor()
          - LinearRegressionPredictor()
          - XGBoostPredictor(n_estimators=200, max_depth=4, learning_rate=0.05)
       e. For each model:
          - Calls model.fit(X_train, y_train) (no-op for baselines)
          - Calls model.evaluate(X_test, y_test) to get full metrics dict
          - Calls save_results(model.name, results, Path("experiments/results/tier1"))
       f. After all models are evaluated, calls load_all_results to load all results.
       g. Prints a formatted comparison table to stdout using simple string formatting (no external table library):

          ```
          ====== Tier 1 Baseline Results ======

          Model                  | RMSE   | MAE    | Dir Acc | P&L    | Trades | Win Rate | Sharpe
          -----------------------|--------|--------|---------|--------|--------|----------|-------
          Naive (Spread Closes)  | 0.0432 | 0.0312 | 0.621   | 1.234  | 45     | 0.578    | 0.89
          Volume (Higher Vol)    | 0.0401 | 0.0298 | 0.645   | 1.567  | 52     | 0.596    | 1.12
          Linear Regression      | 0.0389 | 0.0275 | 0.667   | 2.345  | 38     | 0.632    | 1.45
          XGBoost                | 0.0356 | 0.0251 | 0.689   | 3.012  | 41     | 0.659    | 1.78
          ```

       h. The script should be runnable via: `python -m experiments.run_baselines` or `python experiments/run_baselines.py`
       i. Accept optional CLI args: --data-path (override default), --results-dir (override default), --threshold (profit sim threshold, default 0.02).
       j. Use argparse for CLI argument parsing.

    3. The script must handle the case where data/processed/features_flat.parquet does not yet exist (Phase 3 hasn't run). Print a clear error: "Feature matrix not found at {path}. Run Phase 3 (Feature Engineering) first." and exit with code 1.

    4. This script is the primary TA check-in deliverable. It should run cleanly, produce interpretable output, and save reproducible results.
  </action>
  <verify>
    <automated>cd /Users/iansabia/Desktop/DS340\ Project && .venv/bin/python -c "from experiments.run_baselines import main; print('import ok')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - experiments/run_baselines.py exists and is importable
    - Script has a main() function and if __name__ == "__main__" block
    - Script imports all four models: NaivePredictor, VolumePredictor, LinearRegressionPredictor, XGBoostPredictor
    - Script imports save_results, load_all_results from src.evaluation.results_store
    - Script accepts --data-path, --results-dir, --threshold CLI arguments via argparse
    - Script prints helpful error when feature matrix is missing (not a raw traceback)
    - Script prints formatted comparison table with all metrics for all models
    - Script saves individual model results as JSON to experiments/results/tier1/
  </acceptance_criteria>
  <done>
    run_baselines.py loads feature data, trains/evaluates all four Tier 1 models, saves results to JSON, and prints a comparison table. The script is the TA check-in deliverable and handles missing data gracefully.
  </done>
</task>

</tasks>

<verification>
1. All model classes are importable: LinearRegressionPredictor, XGBoostPredictor
2. Results store round-trips save/load correctly
3. run_baselines.py is importable and has main() function
4. Full test suite passes: `pytest tests/models/ tests/evaluation/ -v`
5. Script handles missing data gracefully with clear error message
</verification>

<success_criteria>
- Linear Regression trains via sklearn and produces spread predictions through BasePredictor interface
- XGBoost trains via xgboost and produces spread predictions through BasePredictor interface
- Both models are evaluated with RMSE, MAE, directional accuracy, and profit simulation
- Results are saved as JSON and loadable for comparison
- run_baselines.py produces a formatted comparison table of all four Tier 1 models
- Script is ready to run as the TA check-in deliverable once Phase 3 data is available
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-regression-baselines/04-02-SUMMARY.md`
</output>
