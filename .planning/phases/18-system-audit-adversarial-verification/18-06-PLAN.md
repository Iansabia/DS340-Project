---
phase: 18-system-audit-adversarial-verification
plan: 06
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - experiments/audit/build_paper_numbers_csv.py
  - experiments/results/audit/paper_numbers.csv
  - scripts/check_paper.sh
autonomous: true
requirements:
  - AUDIT-05
gap_closure: false

must_haves:
  truths:
    - "experiments/audit/build_paper_numbers_csv.py exists and produces experiments/results/audit/paper_numbers.csv with ≥ 20 rows"
    - "Each row has columns: claim_text, claim_value, kind, paper_section, line_number, source_file, source_command, match_status"
    - "scripts/check_paper.sh has been extended with ≥ 5 new audit_-prefixed regression checks"
    - "After extension, bash scripts/check_paper.sh exits 0 with ALL existing checks AND new audit checks passing"
    - "paper_numbers.csv only includes claims from headline sections (Abstract, §5.1, §5.8, §6.3, §8 Conclusions)"
  artifacts:
    - path: experiments/audit/build_paper_numbers_csv.py
      provides: "Generator script that walks PAPER_DRAFT.md headline sections and emits one CSV row per numeric claim"
      min_lines: 80
    - path: experiments/results/audit/paper_numbers.csv
      provides: "Tier 5 deliverable: traceable paper-claim-to-source mapping (consumed by Plan 07 AUDIT_REPORT.md)"
      min_lines: 21
    - path: scripts/check_paper.sh
      provides: "Existing 19-check validator extended with ≥ 5 new audit_-prefixed regression checks"
  key_links:
    - from: experiments/audit/build_paper_numbers_csv.py
      to: PAPER_DRAFT.md
      via: "regex extraction in headline sections only"
      pattern: "HEADLINE_SECTIONS"
    - from: experiments/audit/build_paper_numbers_csv.py
      to: experiments/results/canonical/headline.json
      via: "single-source-of-truth for source_file column"
      pattern: "canonical/headline.json"
    - from: scripts/check_paper.sh
      to: experiments/results/canonical/headline.json
      via: "Python helper canon() reads JSON values for grep regression"
      pattern: "canon "
---

<objective>
Tier 5 audit: every numeric claim in PAPER_DRAFT.md headline sections traces to a generation script + canonical file, AND scripts/check_paper.sh is extended with ≥ 5 new regression checks that recompute headline numbers from canonical/headline.json and grep-match them in PAPER_DRAFT.md.

Per RESEARCH.md, this is the smallest Tier in scope: scripts/audit_paper_numbers.py (already 80% built per Phase 17) does the regex extraction; this plan extends it to emit a CSV instead of a Markdown log, and adds bash regression checks.

Headline-section restriction is critical (Phase 17 lesson: all-sections produced 53+ false positives; restrict to Abstract / §5.1 / §5.8 / §6.3 / §8 Conclusions).

Implements requirement: AUDIT-05.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-system-audit-adversarial-verification/18-CONTEXT.md
@.planning/phases/18-system-audit-adversarial-verification/18-RESEARCH.md
@.planning/phases/18-system-audit-adversarial-verification/18-VALIDATION.md
@PAPER_DRAFT.md
@experiments/results/canonical/headline.json
@scripts/check_paper.sh
@scripts/audit_paper_numbers.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement experiments/audit/build_paper_numbers_csv.py and generate paper_numbers.csv (≥ 20 rows)</name>
  <files>experiments/audit/build_paper_numbers_csv.py, experiments/results/audit/paper_numbers.csv</files>
  <read_first>
    - 18-RESEARCH.md §Tier 5 — Number-by-Number Paper Trace (lines 1229-1382) — full Python skeleton (lines 1305-1376) is the source of truth; copy verbatim
    - 18-RESEARCH.md §Watch out for (lines 1378-1382) — confirms scripts/audit_paper_numbers.py already exists and headline-section restriction is critical
    - scripts/audit_paper_numbers.py — read carefully; its DOLLAR_RE / SHARPE_RE / BPS_RE / PCT_RE regexes are the source for the CSV builder's regex bank
    - PAPER_DRAFT.md — confirm the headline section heading patterns: "## Abstract", "### 5.1 ", "### 5.8 ", "### 6.3 ", "## 8."
    - experiments/results/canonical/headline.json — confirm the schema for cross-reference
    - 18-CONTEXT.md §Tier 5 — Number-by-number trace
  </read_first>
  <action>
    Create experiments/audit/build_paper_numbers_csv.py by copying the Python skeleton from 18-RESEARCH.md lines 1305-1376 verbatim. The skeleton includes:

    1. Module docstring (Tier 5: build experiments/results/audit/paper_numbers.csv)
    2. Imports (csv, json, Path, re)
    3. Constants: OUT, PAPER, HEADLINE
    4. Regex bank: DOLLAR_RE, SHARPE_RE, BPS_RE, PCT_RE — copy verbatim from RESEARCH.md (these match scripts/audit_paper_numbers.py for consistency)
    5. HEADLINE_SECTIONS tuple = ("## Abstract", "### 5.1 ", "### 5.8 ", "### 6.3 ", "## 8.")
    6. in_headline_section(line_idx, all_lines) walks backward to find most-recent heading
    7. main() -> int: for each line in PAPER_DRAFT.md, if inside a headline section, run all 4 regexes and emit one CSV row per match

    After copying, robustness improvement: set fieldnames explicitly rather than using rows[0].keys():

    ```python
    fieldnames = ["claim_text", "claim_value", "kind", "paper_section", "line_number", "source_file", "source_command", "match_status"]
    with OUT.open("w", newline="") as fh:
        w = csv.DictWriter(fh, fieldnames=fieldnames)
        w.writeheader()
        w.writerows(rows)
    ```

    Run after writing:
    ```bash
    PYTHONPATH=. python3 experiments/audit/build_paper_numbers_csv.py
    ```
    Expected: writes experiments/results/audit/paper_numbers.csv, prints "Wrote ... (N claims)", exits 0. N MUST be ≥ 20 per AUDIT-05 acceptance.
  </action>
  <verify>
    <automated>PYTHONPATH=. python3 experiments/audit/build_paper_numbers_csv.py && [ "$(wc -l < experiments/results/audit/paper_numbers.csv)" -ge 21 ]</automated>
  </verify>
  <acceptance_criteria>
    - experiments/audit/build_paper_numbers_csv.py exists and is ≥ 80 lines
    - PYTHONPATH=. python3 experiments/audit/build_paper_numbers_csv.py exits 0
    - experiments/results/audit/paper_numbers.csv exists with ≥ 21 lines (20 data + header)
    - Header line contains all 8 column names: claim_text, claim_value, kind, paper_section, line_number, source_file, source_command, match_status
    - Every data row has paper_section starting with "## Abstract" or "### 5.1" or "### 5.8" or "### 6.3" or "## 8" (headline sections only)
    - kind values are restricted to {sharpe, bps, dollar, pct}
  </acceptance_criteria>
  <done>Tier 5 produces a traceable paper_numbers.csv mapping every headline-section numeric claim to its canonical source — Plan 07 will use this to populate AUDIT_REPORT.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend scripts/check_paper.sh with ≥ 5 new audit_-prefixed regression checks</name>
  <files>scripts/check_paper.sh</files>
  <read_first>
    - scripts/check_paper.sh — read fully to understand existing 19-check structure, especially the check_ge helper, $PAPER variable, and where the "TOTAL: X/X OK" output happens
    - 18-RESEARCH.md §Tier 5 — Concrete bash skeleton (lines 1259-1303) — provides 7 example checks; copy ≥ 5 verbatim
    - experiments/results/canonical/headline.json — confirm the model keys: linear_regression, xgboost, ppo_filtered, etc., and field names sharpe_per_trade, alpha_bps_per_trade
    - PAPER_DRAFT.md — sanity-check that the rounded values from headline.json actually appear (e.g., grep "0.501" PAPER_DRAFT.md should return ≥ 1)
  </read_first>
  <action>
    Append a new "AUDIT-05: Phase 18 number-by-number regression checks" block to scripts/check_paper.sh. Place it BEFORE the final "TOTAL:" summary so the new checks are counted.

    Copy the bash skeleton from 18-RESEARCH.md lines 1259-1303 verbatim. The block defines:

    1. A `canon()` helper function that reads experiments/results/canonical/headline.json and prints a model field rounded to 3 decimal places (or 2 for values >= 100)
    2. Check 1: audit_lr_per_trade_sharpe_in_paper — extracts LR sharpe_per_trade, greps PAPER_DRAFT.md
    3. Check 2: audit_lr_alpha_bps_in_paper — extracts LR alpha_bps_per_trade, rounds to 1 decimal, greps "{value} bps"
    4. Check 3: audit_xgb_per_trade_sharpe_in_paper — XGBoost sharpe_per_trade
    5. Check 4: audit_ppo_filtered_alpha_bps_in_paper — PPO+autoencoder alpha_bps_per_trade
    6. Check 5: audit_per_pair_sharpe_3_2_in_abstract — greps abstract section for "≈ 3.2" or "approximately 3.2" or "3.2 ("
    7. Check 6: audit_walk_forward_11_windows_in_paper — greps for "11-window" or "11 walk-forward" or "across 11"
    8. Check 7: audit_test_rows_1673_in_paper — greps for "1,673"

    Verbatim copy. After copying:
    - If existing check_paper.sh uses a different helper name than `check_ge` (e.g., `check_eq`, `assert_count`), adapt to match the existing convention.
    - If existing check_paper.sh names its checks without an `audit_` prefix (e.g., REPL-06 checks may use `abstract_` prefix), KEEP the audit_ prefix per AUDIT-05's requirement that new checks are clearly labeled.
    - Insert BEFORE the final "TOTAL: X/X OK" or `exit $FAILS` line — the existing check counter must include the new checks.

    After modification, run the full validator:
    ```bash
    bash scripts/check_paper.sh
    ```
    Expected: exits 0; output shows ≥ 24 checks total (19 existing + ≥ 5 new); all marked OK.

    If any new check fails, the underlying issue is either:
    (a) The PAPER_DRAFT.md does NOT contain that number — investigate via `grep -n "<value>" PAPER_DRAFT.md`. If absent, add a brief note to Plan 07 indicating which paper section needs updating.
    (b) The grep regex is too strict — relax to allow leading spaces, parentheses, etc.
    (c) The `canon()` helper is rounding wrong — verify by running canon manually: `canon linear_regression sharpe_per_trade`.

    DO NOT make new checks pass by modifying PAPER_DRAFT.md in this plan — that is Plan 07's job. If a check fails because the paper genuinely lacks the number, mark the check `audit_<name>_pending` and add a `# TODO Plan 07: surface this number in <section>` comment. Plan 07 will resolve.
  </action>
  <verify>
    <automated>bash scripts/check_paper.sh && [ "$(grep -cE '^audit_|"audit_' scripts/check_paper.sh)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check_paper.sh exists (modified)
    - bash scripts/check_paper.sh exits 0
    - ≥ 5 new check names start with "audit_" prefix in the script source
    - Total checks reported (e.g., "TOTAL: X/X OK") increased by ≥ 5 vs pre-Plan-06
    - canon() helper function is defined and uses experiments/results/canonical/headline.json as the source
    - Each new audit_ check has a `check_ge "audit_<name>" 1 "$FOUND"` (or equivalent assertion) line
  </acceptance_criteria>
  <done>scripts/check_paper.sh now enforces canonical-numbers-in-paper consistency for ≥ 5 headline metrics; any future drift between headline.json and PAPER_DRAFT.md fails the validator immediately.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# CSV exists with ≥ 20 data rows
test -f experiments/results/audit/paper_numbers.csv
[ "$(wc -l < experiments/results/audit/paper_numbers.csv)" -ge 21 ]
head -3 experiments/results/audit/paper_numbers.csv

# CSV columns are correct
head -1 experiments/results/audit/paper_numbers.csv | tr ',' '\n'

# CSV only contains headline sections
awk -F',' 'NR>1 {print $4}' experiments/results/audit/paper_numbers.csv | sort -u

# check_paper.sh extended and passes
bash scripts/check_paper.sh
grep -c "audit_" scripts/check_paper.sh  # ≥ 5

# No regression in existing tests
PYTHONPATH=. python3 -m pytest tests/ -q --ignore=tests/audit -x 2>&1 | tail -5
```
</verification>

<success_criteria>
- experiments/audit/build_paper_numbers_csv.py exists, executable, exits 0
- experiments/results/audit/paper_numbers.csv has ≥ 21 lines (20 data + header)
- All paper_section values in CSV match headline-section restriction
- scripts/check_paper.sh has ≥ 5 new audit_-prefixed checks
- bash scripts/check_paper.sh exits 0 with all checks (existing + new) green
</success_criteria>

<output>
After completion, create `.planning/phases/18-system-audit-adversarial-verification/18-06-SUMMARY.md` documenting:
- Number of CSV rows produced and their kind distribution (sharpe / bps / dollar / pct counts)
- Names of the ≥ 5 new audit_ checks added to check_paper.sh
- Total check count in check_paper.sh before/after (e.g., "19 → 24+")
- Any new check that failed and the reason (input to Plan 07 paper update if applicable)
</output>
