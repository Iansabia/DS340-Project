---
phase: 17-model-rerun-paper-number-audit-pitch-standard-conversion
plan: 04
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REPL-07]
must_haves:
  truths:
    - "STATE.md Decisions section contains a Phase 17 entry naming the PPO root cause, the canonical-results convention, and the pitch-standard adoption"
    - "ROADMAP.md Phase 17 entry is marked Complete with a one-line resolution summary"
    - "REQUIREMENTS.md traceability shows REPL-01 through REPL-07 all Complete"
    - "Phase 17 closes cleanly — no stale Pending markers in the milestone"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Project state decision log with Phase 17 closure entry"
      contains: "Phase 17"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 17 row marked Complete with resolution summary"
      contains: "Phase 17"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with REPL-01 through REPL-07 all Complete"
      contains: "REPL-01 | Phase 17 | Complete"
  key_links:
    - from: ".planning/STATE.md ### Decisions"
      to: ".planning/phases/17-*/17-02-PPO-DIAGNOSTIC.md"
      via: "one-paragraph PPO root-cause summary referencing the diagnostic file"
      pattern: "PPO|canonical|17-02-PPO-DIAGNOSTIC"
    - from: ".planning/REQUIREMENTS.md traceability"
      to: "REPL-01 through REPL-07 status column"
      via: "table edit Pending -> Complete"
      pattern: "REPL-0[1-7].*Complete"
---

<objective>
Close Phase 17 cleanly: append a diagnostic note to STATE.md documenting the PPO root cause and the new "canonical results JSON" file convention, mark the Phase 17 ROADMAP entry Complete with a one-line resolution summary, and flip REPL-01 through REPL-07 from Pending to Complete in REQUIREMENTS.md traceability.

Purpose: REPL-07 is the final ledger entry. Without it, Phase 17 looks open in STATE.md / ROADMAP.md / REQUIREMENTS.md and future phases would re-discover the same questions ("which JSON do I cite? where did the −$7,724 come from?"). This plan documents the answers in the canonical project-state files so the next contributor — human or Claude — finds them in 30 seconds, not 30 minutes.

Output: updated STATE.md (Decisions section + Current Position), ROADMAP.md (Phase 17 row Complete), REQUIREMENTS.md (REPL-01 through REPL-07 Complete in traceability table).
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-02-PPO-DIAGNOSTIC.md
@.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-01-SUMMARY.md
@.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-02-SUMMARY.md
@.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-03-SUMMARY.md

<state_md_structure>
<!-- Existing STATE.md frontmatter and section structure (do NOT duplicate; APPEND to existing). -->

```yaml
---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: Extended Evidence & Submission
status: in-progress
stopped_at: ...
last_updated: "2026-04-24T..."
last_activity: ...
progress:
  total_phases: 9
  completed_phases: 8
  total_plans: 21
  completed_plans: 20
  percent: 95
---
```

After Phase 17 closes:
- `total_phases: 10` (Phase 17 added)
- `completed_phases: 10` (all complete)
- `total_plans: 25` (21 + 4 from Phase 17)
- `completed_plans: 25`
- `percent: 100`
- `status: complete`
- `last_activity` updated to today + Plan 17-04 completion line

The `### Decisions` section is an unordered bullet list under `## Accumulated Context > ### Decisions`. New entries are appended chronologically.
</state_md_structure>

<roadmap_md_structure>
<!-- Phase 17 entry shape in ROADMAP.md (currently lines ~274-290). Update Plans subsection from TBD to actual plan list. -->

Current shape:
```markdown
### Phase 17: Model Rerun + Paper Number Audit + Pitch-Standard Conversion

**Goal**: Resolve the PPO data inconsistency...
**Depends on**: Phase 16...
**Requirements**: REPL-01, REPL-02, REPL-03, REPL-04, REPL-05, REPL-06, REPL-07
**Success Criteria** (what must be TRUE):
  1. ...
  ...

**Plans**: TBD

Plans:
- [ ] TBD (run `/gsd:plan-phase 17` to break down)
```

Target shape after this plan:
```markdown
### Phase 17: Model Rerun + Paper Number Audit + Pitch-Standard Conversion

**Goal**: ...
**Depends on**: ...
**Requirements**: REPL-01, REPL-02, REPL-03, REPL-04, REPL-05, REPL-06, REPL-07
**Success Criteria** (what must be TRUE): ...

**Plans**: 4/4 complete

Plans:
- [x] 17-01-PLAN.md — Canonical model rerun + PPO diagnostic (REPL-01, REPL-02) — YYYY-MM-DD, commit ABCDEF
- [x] 17-02-PLAN.md — Paper number audit + dollar-to-bps conversion (REPL-03, REPL-04) — YYYY-MM-DD, commit ABCDEF
- [x] 17-03-PLAN.md — Slide + validator update (REPL-05, REPL-06) — YYYY-MM-DD, commit ABCDEF
- [x] 17-04-PLAN.md — STATE/ROADMAP closure note (REPL-07) — YYYY-MM-DD, commit ABCDEF

**Resolution summary:** [one-line, e.g., "PPO root cause: legacy run_backtest.py uses {X}; canonical tier3 path now single source of truth at experiments/results/canonical/headline.json. Pitch-standard (Sharpe + bps) adopted as house style."]
```

ROADMAP.md also has a phase-tracking table (lines ~225-235). Add a row:
```markdown
| 17. Model Rerun + Paper Number Audit + Pitch-Standard | 4/4 | Complete | YYYY-MM-DD | - |
```
</roadmap_md_structure>

<requirements_md_structure>
<!-- REQUIREMENTS.md traceability table currently has REPL-01 through REPL-07 marked Pending (lines ~353-359). -->

Current state (each row):
```
| REPL-01 | Phase 17 | Pending |
```

Target state:
```
| REPL-01 | Phase 17 | Complete |
```
</requirements_md_structure>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Phase 17 decision entry to STATE.md and update frontmatter progress counters</name>
  <read_first>
    - .planning/STATE.md (full file — frontmatter + Current Position + Accumulated Context > Decisions)
    - .planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-02-PPO-DIAGNOSTIC.md (root-cause one-paragraph summary source)
    - .planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-01-SUMMARY.md (canonical numbers actually produced)
    - .planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-02-SUMMARY.md (paper edit count)
    - .planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-03-SUMMARY.md (slide + validator outcomes)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
Step A: Update frontmatter.
- `total_phases: 9` -> `total_phases: 10`
- `completed_phases: 8` -> `completed_phases: 10` (Phase 16 complete; Phase 17 complete)
- `total_plans: 21` -> `total_plans: 25` (21 prior + 4 Phase 17)
- `completed_plans: 20` -> `completed_plans: 25`
- `percent: 95` -> `percent: 100`
- `status: in-progress` -> `status: complete`
- `last_updated: ...` -> today (UTC ISO-8601)
- `last_activity: ...` -> "{today} -- Plan 17-04 executed (Phase 17 closure: canonical results convention adopted, pitch-standard headlines locked)"
- `stopped_at: ...` -> "Phase 17 complete — paper has canonical numerics in pitch-standard format; April 27 submission ready"

Step B: Update Current Position section.
- Phase: change to "17 of 17 (Model Rerun + Paper Number Audit + Pitch-Standard Conversion — COMPLETE)"
- Plan: "4 of 4 complete (17-01 canonical rerun + diagnostic; 17-02 paper audit + bps conversion; 17-03 slide + validator; 17-04 closure)"
- Status: "Phase 17 fully shipped. PAPER_DRAFT.md numerics fully reconciled to experiments/results/canonical/headline.json; pitch-standard headlines (Sharpe + bps) adopted; check_paper.sh extended with 3 REPL-06 regression checks (19+/19+ OK). April 27 submission is locked."
- Progress bar: `[██████████] 100%`

Step C: Append Phase 17 decision entries to `## Accumulated Context > ### Decisions`. Add the following bullets at the end of the existing list (preserve all prior entries verbatim):

```markdown
- [Phase 17-01-canonical-rerun]: experiments/results/canonical/headline.json is the single source of truth for every numeric claim in PAPER_DRAFT.md. Produced by experiments/run_canonical.py with seed=42, position_size=$100, threshold=0.02, train=6802/test=1673. The 9-model entry contains rmse, mae, directional_accuracy, total_pnl, num_trades, win_rate, sharpe_per_trade, sharpe_annualized, alpha_bps_per_trade, max_drawdown_pct. No paper or slide may cite a JSON file outside experiments/results/canonical/.
- [Phase 17-02-PPO-diagnostic]: PPO 600× magnitude divergence root cause: {ROOT CAUSE FROM 17-02-PPO-DIAGNOSTIC.md — fill in verbatim from the diagnostic file's "Root Cause" section}. The unreproducible "−$7,724" paper claim from earlier drafts is documented as not present in any extant JSON file (likely a transcription error of the legacy −$87,724 figure). Disputed files moved to experiments/results/archive/backtest_ppo_*.json.
- [Phase 17-02-paper-audit]: scripts/audit_paper_numbers.py is the automated cross-reference tool — runs paper-vs-canonical-JSON regex extraction and outputs match/mismatch/unresolvable Markdown log. Re-run any time canonical numbers change.
- [Phase 17-02-pitch-standard]: Paper headlines converted from dollar P&L to per-trade Sharpe + per-trade alpha (bps) — the professional quant pitch standard. Per-trade alpha formula: (total_pnl / num_trades / position_size) × 10,000. Worked: LR = $201.69 / 1549 / $100 × 10000 = 13.0 bps. PPO+AE = $4.61 / 899 / $100 × 10000 = 0.5 bps. Abstract, Tables 2 + 8, §5.8, §8 all updated. Dollar P&L stays in tables only.
- [Phase 17-03-slide-validator]: slides_deck.html Results slide leads with bps + Sharpe (canonical PPO numbers, side panels per-pair Sharpe ≈ 3.2 and 11/11 walk-forward preserved). scripts/check_paper.sh extended with 3 REPL-06 checks: abstract_mentions_sharpe, abstract_cites_sharpe_value, orphan_dollar_paragraphs (no headline-section paragraph cites a dollar amount without a Sharpe or bps companion). Total: 19+/19+ OK.
- [Phase 17-04-closure]: Pitch-standard adopted as the project's house style for all future result presentations. Canonical results JSON pattern (one file per phase under experiments/results/canonical/) is the convention going forward. v1.1 milestone fully shipped 100%.
```

The `{ROOT CAUSE FROM 17-02-PPO-DIAGNOSTIC.md}` placeholder must be filled in verbatim by reading the diagnostic file's "Root Cause" section before writing STATE.md. Do not leave the placeholder.
  </action>
  <verify>
    <automated>grep -c "Phase 17-01-canonical-rerun\|Phase 17-02-PPO-diagnostic\|Phase 17-02-pitch-standard\|Phase 17-03-slide-validator\|Phase 17-04-closure" .planning/STATE.md | awk '{exit ($1 < 5)}' && grep -c "percent: 100\|status: complete" .planning/STATE.md | awk '{exit ($1 < 2)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "percent: 100" .planning/STATE.md` returns 1
    - `grep -c "status: complete" .planning/STATE.md` returns 1
    - `grep -c "total_phases: 10" .planning/STATE.md` returns 1
    - `grep -c "completed_phases: 10" .planning/STATE.md` returns 1
    - `grep -c "total_plans: 25\|completed_plans: 25" .planning/STATE.md` returns 2
    - `grep -c "Phase 17-01-canonical-rerun" .planning/STATE.md` returns 1
    - `grep -c "Phase 17-02-PPO-diagnostic" .planning/STATE.md` returns 1
    - `grep -c "Phase 17-02-pitch-standard" .planning/STATE.md` returns 1
    - `grep -c "Phase 17-03-slide-validator" .planning/STATE.md` returns 1
    - `grep -c "Phase 17-04-closure" .planning/STATE.md` returns 1
    - `grep -c "experiments/results/canonical/headline\.json" .planning/STATE.md` returns >= 1 (canonical convention named)
    - `grep -c "{ROOT CAUSE\|TBD\|XXX\|placeholder" .planning/STATE.md` returns 0 (no unfilled placeholders)
    - `grep -cE "\\[██████████\\]" .planning/STATE.md` returns >= 1 (progress bar at 100%)
  </acceptance_criteria>
  <done>STATE.md frontmatter shows 100% completion, Current Position reflects Phase 17 closure, Accumulated Context > Decisions has 5+ new Phase 17 entries with the PPO root cause filled in verbatim from 17-02-PPO-DIAGNOSTIC.md (no placeholder text), and the canonical-results JSON convention is named explicitly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mark Phase 17 Complete in ROADMAP.md and flip REPL-01 through REPL-07 to Complete in REQUIREMENTS.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 17 entry currently shows "Plans: TBD"; phase-tracking table at lines ~225-235)
    - .planning/REQUIREMENTS.md traceability table (lines ~353-359 contain REPL-01 through REPL-07 with Pending status)
    - .planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/ (one SUMMARY.md per plan; needed for commit hashes)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Step A: Update ROADMAP.md Phase 17 entry.

1. Locate the `### Phase 17:` block (~line 274). Change `**Plans**: TBD` to `**Plans**: 4/4 complete`.

2. Replace the existing `Plans:` list (currently `- [ ] TBD (run /gsd:plan-phase 17 to break down)`) with the four completed plans, each with date and commit hash. Format mirrors Phase 16's entries:
```markdown
Plans:
- [x] 17-01-PLAN.md — Canonical model rerun + PPO diagnostic (REPL-01, REPL-02) — YYYY-MM-DD, commits {hash1} ({short message})
- [x] 17-02-PLAN.md — Paper number audit + dollar-to-bps conversion (REPL-03, REPL-04) — YYYY-MM-DD, commits {hash2}
- [x] 17-03-PLAN.md — Slide + validator update (REPL-05, REPL-06) — YYYY-MM-DD, commits {hash3}
- [x] 17-04-PLAN.md — STATE/ROADMAP closure note (REPL-07) — YYYY-MM-DD, commit {hash4}
```

3. Append a `**Resolution summary:**` block after the Plans list:
```markdown
**Resolution summary:** PPO 600× magnitude divergence root-caused to {ROOT CAUSE from 17-02-PPO-DIAGNOSTIC.md}; canonical results JSON convention established at `experiments/results/canonical/headline.json`; pitch-standard format (per-trade Sharpe + alpha in bps) adopted as house style for all future result presentations. Paper, slide, and validator all reconcile to canonical/headline.json.
```

4. Update the phase-tracking table (line ~225). Find the existing table:
```
| 15. Live Commodity-Matching Engineering Fixes | 3/3 | Complete    | 2026-04-24 | - |
```
Add a Phase 17 row after Phase 16 (or after Phase 15 if Phase 16 isn't yet in the table — check current state):
```
| 17. Model Rerun + Paper Number Audit + Pitch-Standard | 4/4 | Complete | YYYY-MM-DD | - |
```

5. Update the v1.1 milestone status block at the top (~line 84-96). The `## v1.1 Extended Evidence & Submission` checklist may need a new Phase 17 line:
```markdown
- [x] **Phase 17: Model Rerun + Paper Number Audit + Pitch-Standard Conversion** - Canonical results JSON, paper numerics audit, dollar-to-bps headline conversion (completed YYYY-MM-DD)
```

The `{ROOT CAUSE from 17-02-PPO-DIAGNOSTIC.md}` placeholder must be filled in verbatim by reading the diagnostic file. Hashes are filled by reading `git log --oneline -5` after each plan committed (the executor for 17-04 has access to the prior plan commits since they were sequential).

Step B: Update REQUIREMENTS.md traceability.

The traceability table (lines ~353-359) currently shows:
```
| REPL-01 | Phase 17 | Pending |
| REPL-02 | Phase 17 | Pending |
| REPL-03 | Phase 17 | Pending |
| REPL-04 | Phase 17 | Pending |
| REPL-05 | Phase 17 | Pending |
| REPL-06 | Phase 17 | Pending |
| REPL-07 | Phase 17 | Pending |
```

Replace with:
```
| REPL-01 | Phase 17 | Complete |
| REPL-02 | Phase 17 | Complete |
| REPL-03 | Phase 17 | Complete |
| REPL-04 | Phase 17 | Complete |
| REPL-05 | Phase 17 | Complete |
| REPL-06 | Phase 17 | Complete |
| REPL-07 | Phase 17 | Complete |
```

Also update the REPL-XX checklist items at the top of the v1.1 requirements list (lines ~260-266 in the Phase 17 section). Each currently shows `- [ ] **REPL-XX**: ...`. Flip the checkbox to `- [x]`. Preserve all REPL text verbatim.

Step C: Update the "Last updated" footer in REQUIREMENTS.md and ROADMAP.md.
```bash
# REQUIREMENTS.md last line currently: *Last updated: 2026-04-17*
# ROADMAP.md last line currently: *Last updated: 2026-04-24 (Phase 16 plans created)*
# Update both to today + brief description.
```

Step D: Validate.
```bash
# All 7 REPL requirements Complete
grep -cE "REPL-0[1-7] \| Phase 17 \| Complete" .planning/REQUIREMENTS.md   # must be 7
grep -cE "REPL-0[1-7] \| Phase 17 \| Pending" .planning/REQUIREMENTS.md    # must be 0
# Phase 17 marked complete in ROADMAP
grep -c "Plans.*4/4 complete\|17\..*4/4.*Complete" .planning/ROADMAP.md   # >= 1
grep -c "Phase 17 plans" .planning/ROADMAP.md                              # may or may not match
grep -c "TBD (run \`/gsd:plan-phase 17\`)" .planning/ROADMAP.md           # 0 (TBD line removed)
# v1.1 milestone shows Phase 17 checked
grep -cE "\\[x\\] \\*\\*Phase 17" .planning/ROADMAP.md                     # >= 1
# Phase-tracking table has Phase 17 row
grep -cE "^\\| 17\\." .planning/ROADMAP.md                                 # >= 1
# REPL checkboxes flipped
grep -cE "^- \\[x\\] \\*\\*REPL-0[1-7]" .planning/REQUIREMENTS.md           # 7
grep -cE "^- \\[ \\] \\*\\*REPL-0[1-7]" .planning/REQUIREMENTS.md           # 0
```
  </action>
  <verify>
    <automated>grep -cE "REPL-0[1-7] \| Phase 17 \| Complete" .planning/REQUIREMENTS.md | awk '{exit ($1 != 7)}' && grep -cE "REPL-0[1-7] \| Phase 17 \| Pending" .planning/REQUIREMENTS.md | awk '{exit ($1 != 0)}' && grep -c "TBD (run" .planning/ROADMAP.md | awk '{exit ($1 != 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "REPL-0[1-7] \| Phase 17 \| Complete" .planning/REQUIREMENTS.md` returns 7 (all 7 traceability rows updated)
    - `grep -cE "REPL-0[1-7] \| Phase 17 \| Pending" .planning/REQUIREMENTS.md` returns 0 (no Pending REPL entries left)
    - `grep -cE "^- \\[x\\] \\*\\*REPL-0[1-7]" .planning/REQUIREMENTS.md` returns 7 (all REPL checkboxes flipped to checked)
    - `grep -cE "^- \\[ \\] \\*\\*REPL-0[1-7]" .planning/REQUIREMENTS.md` returns 0 (no unchecked REPL items left)
    - `grep -c "Plans.*4/4 complete\|\\*\\*Plans\\*\\*: 4/4 complete" .planning/ROADMAP.md` returns >= 1 (Phase 17 plan count updated)
    - `grep -c "TBD (run" .planning/ROADMAP.md` returns 0 (TBD placeholder removed)
    - `grep -cE "^\\| 17\\." .planning/ROADMAP.md` returns >= 1 (Phase 17 row in tracking table)
    - `grep -cE "\\[x\\] \\*\\*Phase 17" .planning/ROADMAP.md` returns >= 1 (v1.1 milestone checklist marked)
    - `grep -c "Resolution summary" .planning/ROADMAP.md` returns >= 1 (resolution summary block present)
    - `grep -c "experiments/results/canonical/headline\.json" .planning/ROADMAP.md` returns >= 1 (canonical convention named)
    - `grep -c "{ROOT CAUSE\|TBD\|XXX\|placeholder" .planning/ROADMAP.md` returns 0 (no unfilled placeholders)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 17 entry shows 4/4 complete with each plan listed and a Resolution summary block; the phase-tracking table has a Phase 17 Complete row; the v1.1 milestone checklist has Phase 17 checked. REQUIREMENTS.md traceability shows REPL-01 through REPL-07 all Complete (7 rows), all REPL checkboxes flipped to [x], zero Pending REPL entries remain.</done>
</task>

</tasks>

<verification>
Plan-level checks:

```bash
# STATE.md updated
grep -c "percent: 100" .planning/STATE.md
grep -c "Phase 17-01-canonical-rerun\|Phase 17-04-closure" .planning/STATE.md   # >= 2
grep -c "experiments/results/canonical/headline\.json" .planning/STATE.md       # >= 1

# ROADMAP.md updated
grep -c "TBD (run" .planning/ROADMAP.md                                          # 0
grep -cE "^\\| 17\\." .planning/ROADMAP.md                                       # >= 1
grep -c "4/4 complete" .planning/ROADMAP.md                                      # >= 1
grep -c "Resolution summary" .planning/ROADMAP.md                                # >= 1

# REQUIREMENTS.md updated
grep -cE "REPL-0[1-7] \| Phase 17 \| Complete" .planning/REQUIREMENTS.md         # 7
grep -cE "REPL-0[1-7] \| Phase 17 \| Pending" .planning/REQUIREMENTS.md          # 0
grep -cE "^- \\[x\\] \\*\\*REPL-0[1-7]" .planning/REQUIREMENTS.md                # 7

# No unfilled placeholders
grep -c "{ROOT CAUSE\|TBD\|XXX" .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md   # 0
```
</verification>

<success_criteria>
- STATE.md frontmatter at 100% / status: complete; Current Position reflects Phase 17 closure; ### Decisions has 5+ Phase 17 entries with PPO root cause filled verbatim
- ROADMAP.md Phase 17 entry: 4/4 complete plans listed, Resolution summary block, phase-tracking table row, v1.1 milestone checklist [x] mark
- REQUIREMENTS.md: REPL-01 through REPL-07 all Complete (7 rows in traceability + 7 [x] checkboxes; zero Pending)
- Zero unfilled placeholders ({ROOT CAUSE, TBD, XXX) in any of the three files
- Phase 17 ledger fully closed; April 27 submission lock confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-04-SUMMARY.md` documenting:
- Final STATE.md frontmatter values (total_phases, completed_phases, total_plans, completed_plans, percent, status)
- Number of new bullets added to STATE.md ### Decisions section
- The verbatim PPO root cause as filled in from 17-02-PPO-DIAGNOSTIC.md
- Confirmation that REQUIREMENTS.md shows 7/7 REPL Complete and ROADMAP.md Phase 17 marked Complete

Then create `.planning/phases/17-model-rerun-paper-number-audit-pitch-standard-conversion/17-PHASE-SUMMARY.md` (the phase-level summary) consolidating all four plans:
- One-line description of each plan's outcome
- Final canonical numbers actually produced (LR pnl, XGB pnl, PPO+AE pnl, all in dollars + bps)
- check_paper.sh final OK count
- Pointer to 17-02-PPO-DIAGNOSTIC.md and 17-03-NUMBER-AUDIT.md
</output>
