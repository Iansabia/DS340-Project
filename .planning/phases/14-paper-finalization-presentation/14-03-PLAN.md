---
phase: 14-paper-finalization-presentation
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - README.md
  - slides.md
  - slides.pdf
  - PAPER_DRAFT.md
autonomous: false
requirements:
  - POL-11
  - POL-12
must_haves:
  truths:
    - "A README.md exists at repo root with a reproduction-command table covering every paper table/figure"
    - "Every `python experiments/...` command in the README actually exists on disk as a script"
    - "slides.md exists as a Marp source with ≥6 required sections (Team, Problem, Methods, Challenge, Results, Conclusions)"
    - "slides.pdf exists at repo root, renders 6-8 pages, and runs ≤ 4:00 in a dry-run"
    - "PAPER_DRAFT.md Appendix A points at README.md instead of duplicating commands"
    - "The human verification checkpoint confirms final paper + slides are submission-ready"
  artifacts:
    - path: "README.md"
      provides: "Project overview + quick-start + full reproduction table"
      contains: "python experiments/"
    - path: "slides.md"
      provides: "Marp source for 4-minute lightning talk"
      contains: "marp: true"
    - path: "slides.pdf"
      provides: "Rendered 6-8 page PDF for classroom-computer presentation"
    - path: "PAPER_DRAFT.md"
      provides: "Appendix A points at README.md"
      contains: "See README.md"
  key_links:
    - from: "README.md reproduction table"
      to: "experiments/*.py scripts"
      via: "Each table cell runs `python experiments/<script>.py`"
      pattern: "python experiments/"
    - from: "slides.md"
      to: "slides.pdf"
      via: "`marp slides.md -o slides.pdf` render command"
      pattern: "marp slides\\.md"
    - from: "PAPER_DRAFT.md Appendix A"
      to: "README.md"
      via: "In-prose pointer"
      pattern: "README\\.md"
---

<objective>
Ship the three remaining deliverables: a repo-root `README.md` with exact reproduction commands for every paper table and figure, a `slides.md` + `slides.pdf` for the 4-minute lightning talk, and a final human verification that the paper is cover-to-cover clean.

Purpose: POL-11 and POL-12 are the last two requirements. The plan is marked `autonomous: false` because both the final PDF review and the slides timing dry-run require human judgment that cannot be grep-verified. Plans 14-01 and 14-02 must complete first: the README references the final table numbering (Table 9, Table 10 from 14-02) and figure numbering (Fig. 11 from 14-02), and the slides reference the regenerated Fig. 1 from 14-01.

Output: A complete, submission-ready repository with a discoverable README, a rendered slide PDF, and human sign-off that the paper is ready to ship.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-paper-finalization-presentation/14-RESEARCH.md
@.planning/phases/14-paper-finalization-presentation/14-01-SUMMARY.md
@.planning/phases/14-paper-finalization-presentation/14-02-SUMMARY.md
@PAPER_DRAFT.md
@CLAUDE.md

<interfaces>
<!-- The exact reproduction table structure. Every row must correspond to a real
     script visible via `ls experiments/` or `ls scripts/`. Confirmed paths:

    experiments/verify_headline.py              → Table 2
    experiments/run_walk_forward.py             → Table 3, Table 3b, Table 4, Fig 1, Fig 3
    experiments/run_category_breakdown.py       → Per-category numbers (no dedicated table — informs §5.3)
    scripts/run_data_scaling.py                 → Table 5, Fig 2 (NOTE: lives under scripts/, not experiments/)
    experiments/run_experiment1_comparison.py   → Table 6 (XGBoost sweep — confirm flag)
    experiments/run_transaction_costs.py        → Table 7, Fig 4
    experiments/run_shap_analysis.py            → Fig 5
    experiments/run_backtest.py                 → Fig 6 (equity curves)
    experiments/run_bootstrap_ci.py             → Fig 7
    experiments/run_experiment2_lookback.py     → Fig 8
    experiments/run_experiment3_threshold.py    → Fig 9
    experiments/run_tft.py + extract_tft_heatmap.py  → Fig 10 (TFT VSN)
    experiments/run_feature_ablation.py         → Table 9 (renumbered per 14-02)
    experiments/run_ensemble_sweep.py           → Table 10, Fig 11 (renumbered per 14-02)
    experiments/run_live_reconciliation.py      → §5.9
    scripts/regenerate_figures.py               → all figures at IEEE style (from 14-01)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with full reproduction table; verify every script path exists</name>
  <files>README.md, PAPER_DRAFT.md</files>
  <read_first>
    - experiments/ directory listing (`ls experiments/*.py`)
    - scripts/ directory listing (`ls scripts/*.py`) — confirms `run_data_scaling.py` lives here
    - PAPER_DRAFT.md Appendix A (lines 670-690) — current command sketch being superseded
    - PAPER_DRAFT.md post-14-02 Table numbering (Tables 1-10, Fig 1-11)
    - .planning/phases/14-paper-finalization-presentation/14-02-SUMMARY.md — final renumbering scheme
    - .planning/phases/14-paper-finalization-presentation/14-RESEARCH.md (README.md audit section, lines 186-205; Reproduction Commands Inventory, lines 582-589)
    - CLAUDE.md — team names, paper title, stack details to mirror in README preamble
    - requirements.txt — for the "install dependencies" section
  </read_first>
  <action>
    **Part A — Create `README.md` at repo root.** Exact content:

    ```markdown
    # Complexity Is Not an Edge

    Cross-platform prediction-market arbitrage on Kalshi and Polymarket, DS340 final project, Boston University, Spring 2026.

    **Authors:** Ian Sabia (U33871576), Alvin Jang (U64760665).

    ## What is this?

    An empirical study asking whether increasing model complexity improves arbitrage detection across Kalshi and Polymarket. We train four tiers of models (Linear Regression, XGBoost → GRU, LSTM, TFT → PPO → PPO + autoencoder) on a common evaluation protocol and find that **the simplest models consistently dominate**. The paper (`PAPER_DRAFT.md`) contains the full methodology, results, and discussion.

    ## Quick start

    ```bash
    git clone https://github.com/iansabia/DS340-Project.git
    cd DS340-Project
    python3.12 -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt
    export PYTHONPATH=$(pwd)
    ```

    Python 3.12+ is required. All experiments run on CPU; no GPU needed. Total runtime for the full reproduction pipeline is ≈ 2-3 hours on a single CPU core.

    ## Reproducing every paper table and figure

    | Paper object | Command | Output |
    |---|---|---|
    | Table 2 (headline backtest) | `python experiments/verify_headline.py` | `experiments/results/tier1/*.json` |
    | Table 3 + Table 3b + Table 4 + Fig. 1 + Fig. 3 (walk-forward) | `python experiments/run_walk_forward.py --windows 12` | `experiments/results/walk_forward/*.json` + `experiments/figures/walk_forward_pnl.png` + `experiments/figures/walk_forward_sharpe.png` |
    | §5.3 per-category breakdown | `python experiments/run_category_breakdown.py` | `experiments/results/category/*.json` |
    | Table 5 + Fig. 2 (data-scaling curve) | `python scripts/run_data_scaling.py --bars-per-pair 250` | `experiments/results/data_scaling/*.json` + `experiments/results/data_scaling/pnl_at_2pp_vs_data.png` |
    | Table 6 (XGBoost hyperparameter sweep) | `python experiments/run_experiment1_comparison.py --xgb-sweep` | `experiments/results/experiment1/*.json` |
    | Table 7 + Fig. 4 (transaction costs) | `python experiments/run_transaction_costs.py` | `experiments/figures/transaction_cost_sensitivity.png` |
    | Table 8 (honest Sharpe accounting) | Derived from Table 2 outputs — fields `sharpe_per_trade` and `sharpe_per_pair` in `experiments/results/tier1/summary.json` | — |
    | §5.9 live vs backtest reconciliation | `python experiments/run_live_reconciliation.py` | `experiments/results/reconciliation/*` |
    | Table 9 (feature ablation, LOGO) | `python experiments/run_feature_ablation.py` | `experiments/results/ablation/*.json` |
    | Table 10 + Fig. 11 (ensemble variants + weight sweep) | `python experiments/run_ensemble_sweep.py` | `experiments/results/ensemble/summary.json` + `experiments/figures/ensemble_weight_sweep.png` |
    | Fig. 5 (SHAP bar plot) | `python experiments/run_shap_analysis.py` | `experiments/figures/shap_bar_plot.png` |
    | Fig. 6 (equity curves) | `python experiments/run_backtest.py` | `experiments/figures/backtest_equity_curves.png` |
    | Fig. 7 (bootstrap RMSE CI) | `python experiments/run_bootstrap_ci.py` | `experiments/figures/bootstrap_ci_rmse.png` |
    | Fig. 8 (lookback sweep — Experiment 2) | `python experiments/run_experiment2_lookback.py` | `experiments/figures/experiment2_lookback_pnl.png` |
    | Fig. 9 (threshold heatmap — Experiment 3) | `python experiments/run_experiment3_threshold.py` | `experiments/figures/experiment3_threshold_heatmap.png` |
    | Fig. 10 (TFT VSN heatmap) | `python experiments/run_tft.py && python experiments/extract_tft_heatmap.py` | `experiments/figures/tft_variable_importance.png` |
    | All figures (re-render in IEEE style) | `python scripts/regenerate_figures.py` | `experiments/figures/*.png` + `experiments/results/data_scaling/pnl_at_2pp_vs_data.png` |

    ## Paper integrity check

    ```bash
    bash scripts/check_paper.sh
    ```

    Exits 0 when the paper passes all POL-04 through POL-10 grep validators (abstract ≤ 250 words, no duplicate table numbers, no dead cross-references, no residual placeholders).

    ## Live paper-trading system

    The autonomous system deployed on BU SCC is documented separately; see `docs/SCC_DEPLOYMENT.md` if present or the §4.4 Live System Architecture section of `PAPER_DRAFT.md`.

    ## Project structure

    ```
    src/              # Model code, feature pipeline, matching, live system
    experiments/      # Runner scripts for each paper table/figure
    scripts/          # Ops scripts (data scaling, figure regeneration, paper checks)
    tests/            # pytest suite
    data/raw/         # Raw API dumps (Kalshi + Polymarket)
    data/processed/   # Aligned feature dataframes
    experiments/results/  # JSON and CSV outputs from experiment runs
    experiments/figures/  # PNG figures (IEEE style, 300 DPI)
    .planning/        # GSD phase plans and research documents
    ```
    ```

    **Part B — Verify every `python experiments/...` / `python scripts/...` command resolves to a real file.**

    Run:
    ```bash
    grep -oE "python (experiments|scripts)/[a-z_]+\.py" README.md | sort -u | while read cmd; do
      path=$(echo "$cmd" | sed 's/python //')
      if [[ -f "$path" ]]; then echo "OK  $path"; else echo "MISS $path"; fi
    done
    ```
    Every line must say `OK`. If any `MISS` appears, fix the README entry to point at the actual script (e.g., if `run_experiment1_comparison.py` does not accept `--xgb-sweep`, remove that flag from the Table 6 row and note "sweep configured via env vars" instead).

    **Part C — Deprecate PAPER_DRAFT.md Appendix A.**

    Read PAPER_DRAFT.md lines 670-690. Replace the Appendix A command list with a short pointer:

    ```
    ## Appendix A: Code Availability

    Full source code and reproduction commands are available at https://github.com/iansabia/DS340-Project. See `README.md` at the repository root for exact command-line invocations mapped to every paper table and figure.
    ```

    (Keep the section header `## Appendix A: Code Availability` identical.)

    Commit with message: `docs(14-03): add README with full reproduction table; deprecate Appendix A (POL-11)`
  </action>
  <verify>
    <automated>bash -c "N=\$(grep -c 'python experiments/\\|python scripts/' README.md); M=0; for p in \$(grep -oE '(experiments|scripts)/[a-z_0-9]+\\.py' README.md | sort -u); do [ -f \"\$p\" ] || { echo \"MISS \$p\"; M=\$((M+1)); }; done; echo \"commands=\$N missing=\$M\"; [ \$N -ge 12 ] && [ \$M -eq 0 ]"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` passes
    - `grep -c "python experiments/\|python scripts/" README.md` returns ≥ 12 (covers all tables and figures)
    - Every script path referenced in README exists on disk (the verify command's `missing=0` confirms this)
    - README references the renumbered Table 9 and Table 10: `grep -c "Table 9\|Table 10" README.md` returns ≥ 2
    - README references Fig. 11: `grep -c "Fig\\. 11" README.md` returns ≥ 1
    - README mentions both the paper check script and the regeneration script: `grep -c "check_paper.sh" README.md` returns ≥ 1 AND `grep -c "regenerate_figures.py" README.md` returns ≥ 1
    - PAPER_DRAFT.md Appendix A is shortened: `awk '/^## Appendix A/,/^## /' PAPER_DRAFT.md | wc -l` returns ≤ 10 (the old list was 18+ lines)
    - PAPER_DRAFT.md Appendix A points at README: `awk '/^## Appendix A/,/^## /' PAPER_DRAFT.md | grep -c "README.md"` returns ≥ 1
    - `bash scripts/check_paper.sh` still exits 0 (regression check — 14-02 work not broken)
  </acceptance_criteria>
  <done>README.md is complete with verified commands; Appendix A is a pointer; POL-11 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Create slides.md (Marp source) and render slides.pdf for 4-minute lightning talk</name>
  <files>slides.md, slides.pdf</files>
  <read_first>
    - PAPER_DRAFT.md §1 Introduction (for Problem slide wording)
    - PAPER_DRAFT.md §4 Methodology (for Methods slide — 4 tiers of models)
    - PAPER_DRAFT.md §4.5 Challenges Overcome (for Challenge slide — silent-failure bugs)
    - PAPER_DRAFT.md §5.1 Headline Model Comparison (for Results slide numbers — XGBoost +$201.63 etc.)
    - PAPER_DRAFT.md §8 Conclusions (for Conclusions slide)
    - .planning/phases/14-paper-finalization-presentation/14-RESEARCH.md (POL-12 section lines 388-462 for Marp recipe and slide skeleton)
    - experiments/figures/walk_forward_pnl.png (confirm file exists — used on Results slide)
  </read_first>
  <action>
    **Part A — Create `slides.md` at repo root.** Exact content (6 required sections + 1 title = 7 slides, targeting 4:00 @ ~34s/slide):

    ```markdown
    ---
    marp: true
    theme: default
    paginate: true
    size: 16:9
    ---

    # Complexity Is Not an Edge

    Cross-platform prediction-market arbitrage on Kalshi and Polymarket

    Ian Sabia & Alvin Jang — DS340 Spring 2026

    ---

    ## Problem

    - Kalshi (CFTC-regulated) and Polymarket (on-chain) list the same events at different prices
    - Discrepancies can persist for hours — a potential statistical-arbitrage opportunity
    - **Central question: does increasing model complexity improve arbitrage detection?**

    ---

    ## Methods

    - **Four model tiers:** LR / XGBoost → GRU / LSTM / TFT → PPO → PPO + autoencoder
    - **59 engineered features** including 13 academic microstructure estimators (Amihud, Corwin–Schultz, Kyle, Roll, favorite–longshot)
    - **5 evaluation regimes:** single-split backtest, 11-window walk-forward, per-category, data-scaling curve, live paper trading
    - **Autonomous live system** on BU SCC: 15-min trade cycle, 10,154 closed positions

    ---

    ## Challenge

    Three silent-failure bugs looked like model problems but were infrastructure:

    - Kalshi `/events` returned 429 on 40% of calls **silently** — starved the pair universe
    - Polymarket singular `condition_id=` returned *random* markets (plural `condition_ids=` works)
    - `live_NNNN` pair_id drift across three code paths → resolved via content-addressed IDs

    **Lesson:** monitor the data pipeline before tuning the model.

    ---

    ## Results

    ![width:600px](experiments/figures/walk_forward_pnl.png)

    - XGBoost (**+$201.63**) ≈ LR (**+$201.69**) > LSTM > GRU >> PPO (**−$7,724**)
    - Every walk-forward window profitable; per-pair annualized Sharpe **≈ 3.2**
    - Alpha lives in the **matching pipeline and oil/commodities class**, not the models

    ---

    ## Conclusions

    - **Simplest models win at this data scale** — a direct empirical answer to the research question
    - The negative result on PPO is real evidence, not a bug
    - The autonomous live system is still accumulating data; the ranking may flip at 500+ bars/pair
    - **Project lesson:** evaluation regime matters more than model family
    ```

    **Part B — Render `slides.pdf`.**

    Check if `marp-cli` is available: `which marp || npm list -g @marp-team/marp-cli`.

    If available:
    ```bash
    marp slides.md -o slides.pdf --allow-local-files
    ```

    If not available, install first (requires Node.js):
    ```bash
    npm install -g @marp-team/marp-cli
    marp slides.md -o slides.pdf --allow-local-files
    ```

    If Node.js is also missing and install would take >15 minutes, fallback: use the Docker marp image:
    ```bash
    docker run --rm --init -v $PWD:/home/marp/app -e LANG=$LANG marpteam/marp-cli slides.md -o slides.pdf --allow-local-files
    ```

    If BOTH Node.js AND Docker are unavailable (unlikely on Ian's Mac), manually export by opening slides.md in VS Code with the Marp extension and using "Export slide deck...". This is a last-resort manual fallback; document it in the SUMMARY.

    Verify the PDF has 7 pages (title + 6 required sections):
    ```bash
    python -c "from PyPDF2 import PdfReader; print(len(PdfReader('slides.pdf').pages))"
    ```
    Expected output: `7` (accept 6-8 as pass — paginate may add a buffer).

    Commit with message: `feat(14-03): add Marp slides source and rendered PDF for 4-minute lightning talk (POL-12)`
  </action>
  <verify>
    <automated>bash -c "test -f slides.md && test -f slides.pdf && python -c \"from PyPDF2 import PdfReader; n=len(PdfReader('slides.pdf').pages); print(n); import sys; sys.exit(0 if 6 <= n <= 8 else 1)\""</automated>
  </verify>
  <acceptance_criteria>
    - `test -f slides.md` passes
    - `test -f slides.pdf` passes
    - `grep -c "^marp: true" slides.md` returns 1
    - `grep -c "^## " slides.md` returns ≥ 6 (Problem, Methods, Challenge, Results, Conclusions, + at least one more)
    - `grep -c "^# Complexity Is Not an Edge" slides.md` returns 1 (title slide)
    - slides.md references the required 6 sections: `grep -cE "^## (Problem|Methods|Challenge|Results|Conclusions)" slides.md` returns 5
    - slides.pdf has 6-8 pages: `python -c "from PyPDF2 import PdfReader; print(len(PdfReader('slides.pdf').pages))"` returns a number in [6, 8]
    - slides.md references the regenerated Fig. 1: `grep -c "walk_forward_pnl.png" slides.md` returns ≥ 1
    - slides.pdf file size > 30KB (rules out empty/failed render): `stat -f "%z" slides.pdf 2>/dev/null || stat -c "%s" slides.pdf` returns a number > 30000
  </acceptance_criteria>
  <done>slides.md source + slides.pdf render both exist; PDF has the expected page count; content covers all 6 required sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — final paper + slides cover-to-cover review</name>
  <files>PAPER_DRAFT.md, slides.pdf, README.md</files>
  <read_first>
    - PAPER_DRAFT.md (full read-through)
    - slides.pdf (open in PDF viewer)
    - README.md (full read-through)
    - .planning/phases/14-paper-finalization-presentation/14-02-SUMMARY.md (residual items to watch)
  </read_first>
  <action>This task is a human-verification checkpoint. See <what-built> for context and <how-to-verify> for the checklist the human must execute. The executor MUST pause here and wait for the `approved` signal from the user before marking the task complete.</action>
  <verify>
    <automated>bash scripts/check_paper.sh &amp;&amp; pytest tests/plotting/test_ieee_style.py -q &amp;&amp; python -c "from PyPDF2 import PdfReader; n=len(PdfReader('slides.pdf').pages); assert 6 &lt;= n &lt;= 8, f'slides.pdf page count {n} outside [6,8]'; print('OK')"</automated>
  </verify>
  <what-built>
    Plans 14-01, 14-02, and 14-03 Tasks 1-2 are complete:
    - All 11 paper figures regenerated at IEEE style, 300 DPI, with units on axes
    - Fig. 2 carries the "plateau at N=6,802" cap annotation on-figure
    - Abstract trimmed to ≤250 words with per-pair Sharpe ≈ 3.2 as headline
    - Stale "0.59 / 4.3" contribution claim replaced with per-pair framing
    - Tables 6/7 collisions renumbered to Tables 9/10 throughout
    - [Insert Figure] placeholder replaced; "Figure 2b" ghost reference resolved
    - §6.4 Limitations expanded with 3 new items (live-cohort, category-tagging, crypto regime)
    - References alphabetized; Cont et al. 2014 entry added
    - README.md with full reproduction table committed at repo root
    - slides.md + slides.pdf rendered for 4-minute lightning talk
    - `scripts/check_paper.sh` runs all POL-* grep validators green
  </what-built>
  <how-to-verify>
    **Automated sanity check (run before human review):**
    ```bash
    bash scripts/check_paper.sh && \
    pytest tests/plotting/test_ieee_style.py -q && \
    python -c "from PyPDF2 import PdfReader; print('slides pages:', len(PdfReader('slides.pdf').pages))"
    ```
    Expected: "ALL CHECKS PASSED", 6 passed, "slides pages: 6-8".

    **Human verification checklist (Ian):**

    1. **Render paper to PDF** (optional but recommended):
       ```bash
       pandoc PAPER_DRAFT.md -o paper.pdf --pdf-engine=xelatex 2>/dev/null || \
         pandoc PAPER_DRAFT.md -o paper.pdf
       open paper.pdf
       ```
       Read cover-to-cover. Mark any residual issue on a physical checklist.

    2. **Verify figures embed correctly in context.** For each figure reference in the paper body, skim the surrounding prose and confirm the figure caption accurately describes what the reader will see. Pay special attention to:
       - Fig. 1 (walk-forward P&L) — colors distinguishable for 4+ model series
       - Fig. 2 (data scaling) — cap annotation visible on-figure (not just in caption)
       - Fig. 9 (threshold heatmap) — cividis colormap readable
       - Fig. 10 (TFT VSN) — labels not truncated
       - Fig. 11 (ensemble sweep) — x-axis spans 0.0-1.0, y-axis has P&L units

    3. **Verify §5.9 numbers are internally consistent** with §5.11 ensemble reporting. Per-trade Sharpe 0.437 (unfiltered) and 0.455 (filtered) should agree between §4.4 text and §5.11 body.

    4. **Slides dry-run with a timer (Ian).**
       ```bash
       open slides.pdf
       ```
       Speak each slide out loud against a stopwatch. Target: ≤ 4:00 total (≈ 34s/slide). If over, cut one bullet per long slide; do NOT cut slides (all 6 sections are rubric-required).

    5. **Verify README reproduction spot-check.** From a fresh terminal:
       ```bash
       source .venv/bin/activate
       export PYTHONPATH=$(pwd)
       python experiments/verify_headline.py --help
       python experiments/run_ensemble_sweep.py --help
       python scripts/run_data_scaling.py --help
       ```
       Each must exit 0 (or print help text). If any script crashes on `--help`, fix the README command or the script.

    6. **Alvin review pass (readability).** Ask Alvin to skim §1 Introduction + §8 Conclusions for typos, awkward phrasing, and claims that don't match the data. This is the last readability gate before submission.

    **Any issue found during human review must be fixed before approval.** If fixes are substantive (e.g., a figure needs to be regenerated), re-run the relevant Task 1 or 2 section, then return to this checkpoint.
  </how-to-verify>
  <resume-signal>
    Respond with one of:
    - **`approved`** — both authors signed off, paper + slides ready to submit
    - **`issues: <description>`** — list residual issues; Claude will create targeted fix tasks and re-run this checkpoint
    - **`blocked: <reason>`** — cannot complete review right now; session can resume later
  </resume-signal>
  <acceptance_criteria>
    - Ian has read the full paper cover-to-cover and accepted
    - Alvin has read §1 + §8 and accepted
    - Slides dry-run measured ≤ 4:00 on a timer
    - All 6 required sections appear in slides.pdf (Team / Problem / Methods / Challenge / Results / Conclusions)
    - At least 3 of the 6 README script invocations were spot-checked with `--help` and exit 0
    - `bash scripts/check_paper.sh` exits 0 (regression check — nothing the human did broke prior automated guarantees)
    - User has typed `approved` in response
  </acceptance_criteria>
  <done>Both authors have approved the paper and slides as submission-ready.</done>
</task>

</tasks>

<verification>
Full plan verification:
```bash
bash scripts/check_paper.sh && \
test -f README.md && grep -q "python experiments/" README.md && \
test -f slides.md && test -f slides.pdf && \
python -c "from PyPDF2 import PdfReader; assert 6 <= len(PdfReader('slides.pdf').pages) <= 8"
```

Plus human approval on Task 3.
</verification>

<success_criteria>
- README.md complete with verified commands for Tables 2-10 and Figs 1-11
- PAPER_DRAFT.md Appendix A points at README.md
- slides.md source exists with 6 required sections; slides.pdf renders 6-8 pages
- Slides dry-run confirmed ≤ 4 minutes
- Paper cover-to-cover review complete; both authors signed off
- POL-11 and POL-12 satisfied
- Phase 14 complete; ready for `/gsd:verify-work`
</success_criteria>

<output>
After approval, create `.planning/phases/14-paper-finalization-presentation/14-03-SUMMARY.md` documenting:
- Any issues found in the human review and how they were resolved
- Measured slides dry-run duration (under 4:00)
- Command used to render the final paper PDF (for Alvin to reproduce)
- The git commit SHA of the final submission-ready state
- Phase 14 completion timestamp
</output>
