---
phase: 15-live-commodity-matching-engineering-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md
autonomous: true
requirements:
  - COM-01
must_haves:
  truths:
    - "A written root-cause explanation exists on disk naming the exact pipeline stage where daily WTI / crude / diesel / heating-oil / gasoline series are dropped"
    - "The diagnostic references live Kalshi /series evidence, not speculation — counts how many daily/weekly commodity series the API actually exposes right now"
    - "The diagnostic enumerates every plausible drop point (Kalshi /series category coverage, /events 429, Polymarket-side absence, quality-filter over-rejection, category-classifier miscoding) with verdict + evidence per hypothesis"
    - "The diagnostic identifies at least one concrete, actionable fix that Plan 15-03 will apply, with exact file(s) and function(s) to patch"
  artifacts:
    - path: ".planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md"
      provides: "Root-cause analysis of commodity discovery gap (COM-01)"
      contains: "Root-cause table, Kalshi /series evidence, fix recommendations"
  key_links:
    - from: "15-01-DIAGNOSTIC.md fix section"
      to: "Plan 15-03 task actions"
      via: "File paths + function names named in diagnostic are what 15-03 modifies"
      pattern: "src/live/market_discovery.py|src/live/contract_classifier.py|src/matching/quality_filter.py"
---

<objective>
Produce a written root-cause diagnostic explaining why daily WTI / crude / diesel / heating-oil / gasoline Kalshi markets do not reach `data/live/active_matches.json` despite being visible on the Kalshi consumer app. This is the "write the test that demonstrates the bug first" step — commit the diagnostic before any code change lands in Plan 15-03.

Purpose: Close COM-01 with evidence, not speculation. The diagnostic becomes the source-of-truth handoff for 15-03's fix tasks and the permanent record required by COM-05.

Output: `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md` with a root-cause table keyed by hypothesis, verdict per hypothesis, and a "Fix recommendations" section naming concrete file paths and functions.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@PAPER_DRAFT.md

<interfaces>
<!-- Key pipeline entry points the diagnostic must investigate. -->
<!-- Discovery flow: src/live/collector.py -> market_discovery.fetch_active_kalshi_markets -> upsert_active_matches -> filter_active_matches -> pair_mapping.json. -->

From src/live/market_discovery.py (line 244-249, 322):
```python
KALSHI_BASE_URL = "https://api.elections.kalshi.com/trade-api/v2"
# NOTE: current discovery categories — "Commodities" is NOT in this tuple.
KALSHI_DISCOVERY_CATEGORIES = ("Economics", "Crypto", "Financials", "Politics", "Climate")

def fetch_active_kalshi_markets(
    categories: tuple[str, ...] | list[str] = KALSHI_DISCOVERY_CATEGORIES,
    max_series_per_category: int = 500,
) -> list[dict]:
```

From src/matching/quality_filter.py (line 375, 536):
```python
def filter_active_match(match: dict) -> tuple[bool, str | None]:
    # Rules 1, 2, 3, 3b, 3c, 3d, similarity floor, stale ticker, year mismatch.
    # NO asset-class / category-consistency rule currently exists.

def filter_active_matches(matches: list[dict]) -> tuple[list[dict], dict]:
```

From src/live/contract_classifier.py (line 482):
```python
def classify_contract(self, ticker: str, title: str) -> str | None:
    # Returns category tag used for category-aware trading thresholds.
```

active_matches.json entry schema (from data/live/active_matches.json):
```python
{'kalshi_ticker': str, 'poly_id': str, 'evicted': bool | None,
 'evicted_at': int | None, 'prev_kalshi_ticker': str | None,
 'prev_poly_id': str | None}
# NOTE: kalshi_title, poly_title, similarity are stripped in the on-disk
# schema. The filter reads them from the in-memory pre-eviction representation.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate live Kalshi commodity series via the /series API</name>
  <files>.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md</files>
  <read_first>
    - src/live/market_discovery.py (lines 240-420, the fetch_active_kalshi_markets function + KALSHI_DISCOVERY_CATEGORIES tuple + the 429-retry helper)
    - src/live/collector.py (lines 1-120, to confirm which function kicks off discovery and whether it re-uses KALSHI_DISCOVERY_CATEGORIES)
    - PAPER_DRAFT.md lines ~640-660 (§6.4 item 9, the canonical problem statement — grep for "KXWTIMAX-26DEC31-T130")
    - data/live/active_matches.json (first ~5 entries, to confirm on-disk schema shape)
  </read_first>
  <action>
    Use the Kalshi public `/series` endpoint (`https://api.elections.kalshi.com/trade-api/v2/series`) to enumerate the actual universe of commodity-related series CURRENTLY live on Kalshi. Run these specific probes:

    1. Query `/series?category=Commodities` — record whether the category exists, the count returned, and the first 20 series tickers. The current discovery config `KALSHI_DISCOVERY_CATEGORIES = ("Economics", "Crypto", "Financials", "Politics", "Climate")` does NOT include "Commodities", which is hypothesis H1.

    2. Query `/series?category=Financials` — record counts; this is where WTI commodity series historically lived under Kalshi's previous taxonomy. Count specifically how many of the returned tickers start with any of: `KXWTI`, `KXWTID`, `KXWTIW`, `KXWTIMAX`, `KXAAAGASD`, `KXAAAGASW`, `KXAAAGASM`, `KXBRENTMON`, `KXDIESEL`, `KXHEATINGOIL`, `KXCRUDE`, `KXGASOLINE`.

    3. For each commodity series ticker found in steps 1-2, query `/events?series_ticker=<TICKER>&status=open&with_nested_markets=true&limit=200`. Count returned markets. Record which series return ≥ 1 open market vs which return 0.

    4. Using `data/live/active_matches.json`, reproduce and verify these known-on-disk counts (copy verbatim into the diagnostic): total entries = 113,287; oil-adjacent entries = 395 (380 evicted, 15 active); all 15 active oil entries share prefix `KXWTIMAX-26DEC31-T` with strikes T100..T220; zero daily WTI / crude / diesel / heating-oil / gasoline series reach this file.

    5. Compute the "gap set" = (commodity series live on Kalshi per steps 1-3) MINUS (commodity series present in active_matches.json per step 4). This is the concrete list of markets that the consumer app shows but the pipeline never sees.

    Write findings into `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md` under a top-level `## Evidence from Kalshi /series` section. Include the raw counts, the full list of commodity-prefix series found, and the gap set as a markdown table.

    Copy these diagnostic numbers verbatim into the Evidence section:
    - 113,287 total entries in active_matches.json
    - 68,944 populated kalshi_ticker, 44,343 evicted stubs
    - 395 oil-adjacent entries: 380 evicted, 15 active
    - All 15 active oil entries = KXWTIMAX-26DEC31-T{100..220} year-end binary-strike contracts
    - Zero daily WTI, crude, diesel, heating-oil, or gasoline series
    - Concrete false match: KXWTIMAX-26DEC31-T130 ↔ poly_id=0x885a6abefad122348b4fbd503473d7fd1f9035d0438cf988a7591620f316a859 ("Will Bitcoin reach $130,000 by December 31, 2026?") at sentence-embedding similarity 0.707
  </action>
  <verify>
    <automated>test -f .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md && grep -q "Evidence from Kalshi /series" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md && grep -q "KXWTIMAX-26DEC31-T130" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md && grep -q "0x885a6abefad122348b4fbd503473d7fd1f9035d0438cf988a7591620f316a859" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md` exists
    - File contains section header `## Evidence from Kalshi /series`
    - File contains the literal string `KXWTIMAX-26DEC31-T130`
    - File contains the literal poly_id `0x885a6abefad122348b4fbd503473d7fd1f9035d0438cf988a7591620f316a859`
    - File contains the literal count `113,287` (or `113287`)
    - File contains a markdown table listing commodity-prefix series found on Kalshi live `/series` with at least 5 rows
    - `grep -c "KXWTI\|KXAAAGAS\|KXBRENT\|KXDIESEL\|KXCRUDE\|KXHEATINGOIL\|KXGASOLINE" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md` returns ≥ 8
  </acceptance_criteria>
  <done>An evidence-backed inventory of live Kalshi commodity series exists on disk, along with the gap set vs active_matches.json. The problem is now quantified, not hand-waved.</done>
</task>

<task type="auto">
  <name>Task 2: Rank root-cause hypotheses with verdict + fix recommendations</name>
  <files>.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md</files>
  <read_first>
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md (the file Task 1 just created — append, don't overwrite)
    - src/live/market_discovery.py (full file, all 844 lines — especially lines 240-420 covering categories and /events retry)
    - src/matching/quality_filter.py (full file, all 642 lines — especially lines 375-533, filter_active_match)
    - src/live/contract_classifier.py (lines 270-310 and 480-550, the ticker-prefix to category mapping)
    - data/live/active_matches.json (spot-check 10 evicted oil-adjacent entries to see `evicted_at` timestamps + `prev_*` fields — are they all evicted at the same time, implying one filter rule is responsible?)
  </read_first>
  <action>
    Append a `## Root-Cause Hypotheses` section to `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md`. Evaluate each of the four candidate hypotheses using the evidence gathered in Task 1. For each hypothesis, produce: (a) one-sentence statement, (b) VERDICT (CONFIRMED / REJECTED / PARTIAL), (c) supporting evidence with exact file:line references, (d) fix recommendation.

    **H1 — Discovery category gap**: `KALSHI_DISCOVERY_CATEGORIES` in `src/live/market_discovery.py` line 249 is `("Economics", "Crypto", "Financials", "Politics", "Climate")` with NO "Commodities" entry. If Kalshi has moved commodity series (WTI / Brent / gasoline / diesel / heating oil) into a dedicated "Commodities" category, the discovery loop never sees them. Fix recommendation: add `"Commodities"` to the tuple at `src/live/market_discovery.py:249`.

    **H2 — Kalshi /events rate limiting silently 429s commodity series**: The April 2026 SCAL fix patched `/series` pagination but `/events` may still be flaky. Check `_kalshi_events_get_with_retry` at `src/live/market_discovery.py:262` — does it log 429 skips? Do the logs show commodity series being skipped? Fix recommendation: increase `KALSHI_EVENTS_BASE_DELAY` (line 259) or add a longer backoff for commodity series specifically.

    **H3 — Polymarket has no counterpart for daily WTI / crude / gasoline contracts**: The consumer app shows KXWTID / KXWTIW markets but Polymarket's Gamma API may return no matching markets. If this is TRUE, it is not a bug — it is a documented limitation. Verify by querying Polymarket Gamma for any market with keywords "WTI", "crude", "oil price", "gasoline" in the April 2026 resolution window. Fix recommendation: document as limitation in §6.4 item 9 supplement; no code change.

    **H4 — Quality filter over-rejects commodity pairs**: Inspect `filter_active_match` at `src/matching/quality_filter.py:375`. Do any of the existing rules (Rule 1-3d, similarity floor, year mismatch) pattern-match on daily oil tickers incorrectly? Spot-check: for the 380 evicted oil-adjacent entries, is the eviction reason always the same, or spread across rules? If concentrated, the offending rule is a suspect. Fix recommendation: narrow the offending rule's scope or add a whitelist for commodity prefixes.

    **H5 — Contract classifier miscodes commodity tickers**: Inspect `classify_contract` at `src/live/contract_classifier.py:482`. Does the ticker-prefix extractor at line 275 (`KXWTI-26APR08-T105.99 -> KXWTI`) correctly route daily oil tickers to the "commodities" category? If it returns None / "financials" / wrong tag, category-aware trading thresholds in `src/live/strategy.py` may never accept commodity pairs. Fix recommendation: extend the classifier's prefix lookup table with explicit entries for `KXWTI`, `KXWTID`, `KXWTIW`, `KXBRENTMON`, `KXAAAGASD`, `KXAAAGASW`, `KXAAAGASM`, `KXDIESEL`, `KXHEATINGOIL`, `KXCRUDE`, `KXGASOLINE`.

    Then add a `## Fix Recommendations for Plan 15-03` section at the bottom. Rank the fixes by confidence (which hypothesis was CONFIRMED) and list them as a checklist with exact file paths and line numbers. This is the input that Plan 15-03 reads to decide what to change.

    Concrete asset-class token lists to reference throughout (these also feed Plan 15-02):
    - Commodity tokens: `["WTI", "CRUDE", "OIL", "DIESEL", "GAS", "GASOLINE", "HEATINGOIL", "BRENT"]`
    - Crypto tokens: `["BITCOIN", "BTC", "ETHEREUM", "ETH", "DOGECOIN", "DOGE"]`
    - Kalshi commodity series prefixes: `["KXWTI", "KXWTID", "KXWTIW", "KXWTIMAX", "KXAAAGASD", "KXAAAGASW", "KXAAAGASM", "KXBRENTMON", "KXDIESEL", "KXHEATINGOIL", "KXCRUDE", "KXGASOLINE"]`
  </action>
  <verify>
    <automated>grep -q "Root-Cause Hypotheses" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md && grep -q "Fix Recommendations for Plan 15-03" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md && grep -cE "VERDICT|Verdict|verdict" .planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md | awk '$1 >= 5 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File contains section `## Root-Cause Hypotheses` with at least 5 hypotheses labeled H1 through H5
    - Each hypothesis has a VERDICT (CONFIRMED / REJECTED / PARTIAL) — `grep -c "VERDICT" 15-01-DIAGNOSTIC.md` returns ≥ 5
    - File contains section `## Fix Recommendations for Plan 15-03`
    - File contains the literal string `KALSHI_DISCOVERY_CATEGORIES`
    - File contains the literal string `filter_active_match`
    - File contains the literal string `classify_contract`
    - File references `src/live/market_discovery.py` with a line number
    - File references `src/matching/quality_filter.py` with a line number
    - At least one hypothesis is CONFIRMED (otherwise the phase has nothing to fix) — grep for `VERDICT.*CONFIRMED` returns ≥ 1
  </acceptance_criteria>
  <done>The diagnostic file names the specific file(s) and function(s) Plan 15-03 must modify, with verdict evidence. COM-01 is closed.</done>
</task>

</tasks>

<verification>
- File `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-DIAGNOSTIC.md` exists and contains both the `## Evidence from Kalshi /series` and `## Root-Cause Hypotheses` sections
- At least one hypothesis is marked CONFIRMED
- Fix Recommendations section names concrete file paths that Plan 15-03 will modify
- Canonical false-match (KXWTIMAX-26DEC31-T130 ↔ Bitcoin poly_id) is documented verbatim
</verification>

<success_criteria>
- COM-01 is closed: a written root-cause explanation exists on disk, grounded in evidence from live Kalshi `/series` calls + on-disk active_matches.json inspection
- The diagnostic is actionable — Plan 15-03 can read it and know exactly which files and functions to edit
- File is committed to git under `.planning/phases/15-*/15-01-DIAGNOSTIC.md`
</success_criteria>

<output>
After completion, create `.planning/phases/15-live-commodity-matching-engineering-fixes/15-01-SUMMARY.md` summarizing which hypothesis was confirmed and listing the concrete fixes handed off to Plan 15-03.
</output>
