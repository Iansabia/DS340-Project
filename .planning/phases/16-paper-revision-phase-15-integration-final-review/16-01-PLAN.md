---
phase: 16-paper-revision-phase-15-integration-final-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PAPER_DRAFT.md
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
must_haves:
  truths:
    - "Abstract cites live-validated 1,224-position commodity cohort (not backtest-only qualifier) AND stays ≤ 250 words"
    - "§5.9 contains a post-fix oil subsection with verbatim Phase 15 numbers (1,224 positions, per-series breakdown, +$1.96 P&L, 36.0% WR, 12h window)"
    - "§6.4 item 9 retains original engineering-gap text AND is augmented with a 'Resolved post-submission in Phase 15' note pointing to §5.9"
    - "`bash scripts/check_paper.sh` still exits 0 with all checks OK after the three edits"
  artifacts:
    - path: "PAPER_DRAFT.md"
      provides: "Abstract (REV-01), §5.9 post-fix oil subsection (REV-02), §6.4 item 9 resolution note (REV-03)"
      contains: "1,224"
  key_links:
    - from: "Abstract (line ~12)"
      to: "§5.9 post-fix subsection"
      via: "see §5.9 cross-reference"
      pattern: "see §5\\.9"
    - from: "§6.4 item 9"
      to: "§5.9 post-fix subsection"
      via: "see §5.9 for live validation"
      pattern: "see §5\\.9 for live validation"
---

<objective>
Integrate Phase 15 live-commodity-matching results into PAPER_DRAFT.md. This plan makes three surgical edits to the paper: (1) soften the abstract's oil qualifier to cite the 1,224-position live cohort, (2) add a post-fix oil subsection to §5.9, (3) append a one-line "Resolved post-submission in Phase 15" note to §6.4 item 9 (preserving the original engineering-gap text).

Purpose: The paper currently claims oil edge is "backtest evidence; live oil-trading remains unobserved in our deployment window" (abstract line 12) and §6.4 item 9 documents the discovery gap as an open limitation. Phase 15 closed that gap with 1,224 live commodity positions. Honest science requires updating the paper to reflect that empirical follow-up, without overclaiming (the 12h window is too short to claim a robust live edge).

Output: PAPER_DRAFT.md with the three edits applied, `scripts/check_paper.sh` still green (16/16 OK), abstract word count ≤ 250.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md
@.planning/phases/15-live-commodity-matching-engineering-fixes/15-VERIFICATION.md
@PAPER_DRAFT.md
@scripts/check_paper.sh
@CLAUDE.md

<interfaces>
<!-- Key numbers the executor MUST use verbatim. DO NOT re-derive. -->
<!-- Source: 15-03-SUMMARY.md "Post-Window Validation Numbers (Verbatim)" section. -->

Phase 15 validation numbers (authoritative):
- Total closed non-KXWTIMAX commodity positions: 1,224
- Aggregate P&L: +$1.96 (paper trading, no slippage, no partial fills)
- Win rate: 36.0% (441 / 1,224)
- Window start: 2026-04-24T01:28Z (first closed position post-deployment)
- Window end: 2026-04-24T13:00Z (last closed position; ~12 hours, NOT full 24)
- Per-series breakdown:
  - KXBRENTW = 486  (weekly Brent range)
  - KXWTI    = 409  (daily WTI on-day)
  - KXWTIW   = 213  (weekly WTI range)
  - KXBRENTMON = 76 (monthly Brent)
  - KXBRENTD = 16  (daily Brent)
  - KXAAAGASD = 11 (daily AAA retail gasoline)
  - KXAAAGASW = 7  (weekly AAA retail gasoline)
  - KXAAAGASM = 6  (monthly AAA retail gasoline)

Phase 15 fix commits (authoritative):
- 38d7970 — "fix(live-discovery): apply 15-01 fix recommendations for commodity gap (COM-03)"
  - Adds "Commodities" to KALSHI_DISCOVERY_CATEGORIES in src/live/market_discovery.py
  - Extends _RULES in src/features/category.py with Brent family + daily-WTI variants
- d217ff1 — "data(live): regenerate active_matches + pair_mapping after Phase 15 discovery fix (COM-03, COM-04)"
  - Regenerates data/live/active_matches.json + pair_mapping.json
  - Includes 200-slot commodity reservation in src/live/collector.py _load_live_pairs

Current PAPER_DRAFT.md line 12 (full abstract, 253 words including "## Abstract" heading row per scripts/check_paper.sh awk range). Relevant sentences:

1. "The system is also deployed as an autonomous paper-trader." (redundant with "live paper trading" two sentences later — DROP CANDIDATE, saves 9 words)
2. "On 6,802 train / 1,673 test rows across 144 matched pairs, XGBoost achieves +\$201.63 simulated P&L at a 2-percentage-point fee, essentially **tied** with Linear Regression at +\$201.69" (DROP "essentially" saves 1 word)
3. Final sentence (the target of REV-01): "The alpha lives in the matching pipeline and the oil/commodities asset class (backtest evidence; live oil-trading remains unobserved in our deployment window, see §5.9) — not in the models."

Current PAPER_DRAFT.md §6.4 item 9 (line 648, exact text preserved):

"9. **Live commodity-matching pipeline is incomplete.** A post-submission audit of `data/live/active_matches.json` (April 23) found that of 395 oil-adjacent entries, 380 have been evicted by the quality filter and only 15 remain active — all year-end `KXWTIMAX` binary-strike contracts, not daily WTI / crude / diesel / heating-oil / gasoline series that appear on the Kalshi consumer app. A residual false match (`KXWTIMAX-26DEC31-T130` aligned with a Bitcoin-$130K Polymarket market at similarity 0.707) indicates rule 10 (category-consistency) still misfires on numerically coincident strikes across asset classes. The backtest oil edge (Finding 6, §5.3) therefore remains **unvalidated on live data**, not because the edge is absent, but because the current live pipeline does not trade the instruments where the edge was measured. This is an engineering limitation, not a finding."

Current §5.9 ends at line 442 (paper-trading caveats paragraph). New subsection 5.9.1 (or just a labeled paragraph block) inserts AFTER the existing "Paper-trading caveats" paragraph at line 442 and BEFORE the `---` section separator at line 444.

scripts/check_paper.sh word-count check (line 30):
  WC=$(awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' "$PAPER" | wc -w | tr -d ' ')
  if (( WC <= 250 )); then [OK]

Note the awk excludes the `## Abstract` heading itself (via `next`) and stops at the first `---` separator. Current run yields 253. After REV-01 insert (+6 words) without trims it would be 259. After dropping "The system is also deployed as an autonomous paper-trader." (9 words) and "essentially" (1 word) it lands at 249.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REV-01 — Abstract: swap qualifier and trim to stay ≤ 250 words</name>
  <files>PAPER_DRAFT.md</files>
  <read_first>
    - PAPER_DRAFT.md lines 10-16 (current abstract and keywords)
    - scripts/check_paper.sh lines 29-36 (POL-04 word-count gate)
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md (post-window validation numbers — just to confirm "1,224" and "12-hour" framing)
  </read_first>
  <action>
    Make three coordinated edits to the abstract paragraph at PAPER_DRAFT.md line 12. These must be applied together in a single Edit call so the word budget is respected atomically.

    **Edit 1a — DROP redundant sentence.** In the abstract paragraph, find and REMOVE the sentence:

      " The system is also deployed as an autonomous paper-trader."

    (note the leading space). This sentence is redundant with "live paper trading" in the next sentence's enumeration of five evaluation regimes. Saves 9 words.

    **Edit 1b — TRIM one filler word.** In the sentence "XGBoost achieves +\$201.63 simulated P&L at a 2-percentage-point fee, essentially **tied** with Linear Regression at +\$201.69", remove the word "essentially " (with trailing space). Result: "XGBoost achieves +\$201.63 simulated P&L at a 2-percentage-point fee, **tied** with Linear Regression at +\$201.69". Saves 1 word.

    **Edit 1c — REPLACE oil qualifier (REV-01 core).** Find the exact final sentence of the abstract:

      "The alpha lives in the matching pipeline and the oil/commodities asset class (backtest evidence; live oil-trading remains unobserved in our deployment window, see §5.9) — not in the models."

    Replace with:

      "The alpha lives in the matching pipeline and the oil/commodities asset class (live-validated post-submission in Phase 15: 1,224 commodity positions closed in a 12-hour window after the discovery-gap fix; see §5.9) — not in the models."

    Net word delta: -9 (Edit 1a) - 1 (Edit 1b) + 6 (Edit 1c) = -4. Target post-edit word count (per `awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' PAPER_DRAFT.md | wc -w`) is ~249.

    **Do not** touch the Keywords line (line 14). **Do not** touch any other part of the paper in this task — §5.9 and §6.4 are separate tasks.

    Preserve all markdown formatting (bold markers, dollar escapes `\$`, dash styles `—`, non-breaking puncuation). Use exact-string Edit calls, not regex, to avoid corruption.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && WC=$(awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' PAPER_DRAFT.md | wc -w | tr -d ' ') && echo "abstract_words=$WC" && [ "$WC" -le 250 ] && grep -c "1,224 commodity positions closed in a 12-hour window" PAPER_DRAFT.md | grep -q "^1$" && ! grep -q "backtest evidence; live oil-trading remains unobserved" PAPER_DRAFT.md && ! grep -q "The system is also deployed as an autonomous paper-trader" PAPER_DRAFT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' PAPER_DRAFT.md | wc -w` yields an integer ≤ 250
    - `grep -c "1,224 commodity positions closed in a 12-hour window" PAPER_DRAFT.md` equals 1
    - `grep -c "live-validated post-submission in Phase 15" PAPER_DRAFT.md` equals 1
    - `grep -c "backtest evidence; live oil-trading remains unobserved" PAPER_DRAFT.md` equals 0 (old qualifier fully removed)
    - `grep -c "The system is also deployed as an autonomous paper-trader" PAPER_DRAFT.md` equals 0
    - `grep -c "essentially \*\*tied\*\*" PAPER_DRAFT.md` equals 0
    - Abstract still ends with "— not in the models." (final sentence intact)
  </acceptance_criteria>
  <done>REV-01 satisfied: abstract cites 1,224-position live cohort, old "backtest-only" qualifier gone, word count ≤ 250, scripts/check_paper.sh POL-04 check still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REV-02 — §5.9 post-fix oil subsection with verbatim Phase 15 numbers</name>
  <files>PAPER_DRAFT.md</files>
  <read_first>
    - PAPER_DRAFT.md lines 385-444 (current §5.9 contents, especially lines 440-442 including the "WTI oil contracts absent" finding and the "Paper-trading caveats" paragraph)
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md "Post-Window Validation Numbers (Verbatim)" section
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-VERIFICATION.md (for redundancy check on the numbers)
  </read_first>
  <action>
    Add a new subsection "5.9.1 Post-Submission Commodity-Matching Fix (Phase 15, April 24)" to PAPER_DRAFT.md §5.9.

    **Insertion point:** After the "Paper-trading caveats." paragraph that currently ends at PAPER_DRAFT.md line 442 (ending with "...consistent with our 2 pp simulation assumption being conservative."), AND BEFORE the horizontal-rule `---` at line 444.

    **IMPORTANT — preserve the existing "Finding: WTI oil contracts absent." paragraph at line 440 AS-IS.** That paragraph is the honest historical record of what the April 22 snapshot looked like; it must remain intact for scientific integrity. The new subsection appends to §5.9 as a follow-up, not a replacement.

    **Exact content to insert** (copy verbatim — all numbers are sourced from 15-03-SUMMARY.md; do NOT re-derive):

    ```markdown
    #### 5.9.1 Post-Submission Commodity-Matching Fix (Phase 15, April 24)

    The §5.9 reconciliation window above (April 14–22) closed with zero WTI oil positions, which we reported as an unresolved engineering limitation (§6.4 item 9). Between the April 22 snapshot and submission, we diagnosed and patched the root cause: `KALSHI_DISCOVERY_CATEGORIES` in `src/live/market_discovery.py` omitted the string `"Commodities"`, and Kalshi had migrated daily WTI / Brent / grain / metal series into that category after an internal taxonomy change. The series were never reaching the discovery sweep. A single-line tuple edit (commit `38d7970`), combined with a classifier extension in `src/features/category.py` for the Brent family plus daily-WTI variants, and a 200-slot commodity reservation inside `src/live/collector.py::_load_live_pairs` to prevent similarity-cap eviction (data regen commit `d217ff1`), unblocked the pipeline end-to-end.

    **Post-fix live validation (12-hour window, April 24).** After deployment on the BU SCC, a 12-hour observation window (`2026-04-24T01:28Z` through `2026-04-24T13:00Z`) closed **1,224 non-`KXWTIMAX` commodity positions**, exceeding the pre-registered validation target (≥ 10 closed positions) by 122×. Aggregate paper-trading P&L over the window was **+\$1.96**; win rate was **36.0%** (441 / 1,224). Per-series breakdown:

    | Kalshi series | Closed positions | Description |
    |---|---|---|
    | `KXBRENTW` | 486 | Weekly Brent range |
    | `KXWTI` | 409 | Daily WTI on-day |
    | `KXWTIW` | 213 | Weekly WTI range |
    | `KXBRENTMON` | 76 | Monthly Brent |
    | `KXBRENTD` | 16 | Daily Brent |
    | `KXAAAGASD` | 11 | Daily AAA retail gasoline |
    | `KXAAAGASW` | 7 | Weekly AAA retail gasoline |
    | `KXAAAGASM` | 6 | Monthly AAA retail gasoline |
    | **Total** | **1,224** | |

    **Interpretation caveats.** This 12-hour window is a *proof-of-life* result, not a robust live edge measurement. Three honest caveats bound what can be claimed: (a) the window is less than half a full trading day and spans one market regime; (b) the +\$1.96 aggregate P&L is dollar-positive but economically near-flat across 1,224 positions (≈ \$0.0016 per trade), dramatically lower than the backtest oil near-expiry edge (Finding 6, §5.3: +\$0.41/trade at 76.5% win rate) — the live window includes *full-contract-lifecycle* positions, not only the near-expiry subset the backtest measured, so a lower win rate is expected rather than contradictory; (c) the paper-trading caveats from §5.9 above (no slippage, no partial fills, no margin) apply unchanged. What this result *does* establish: the discovery / matching / collector / trading-cycle stack now trades daily and weekly WTI and Brent end-to-end, which was not true at the April 22 snapshot. Future work can now measure live oil-edge stability over longer windows on this same code path.
    ```

    After inserting this block, ensure the `---` separator at the old line 444 remains exactly one blank line after the new subsection's closing paragraph.

    Do NOT modify the existing "Finding: WTI oil contracts absent." paragraph at line 440 — it remains as historical record.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && grep -c "^#### 5\.9\.1 Post-Submission Commodity-Matching Fix" PAPER_DRAFT.md | grep -q "^1$" && grep -c "1,224 non-\`KXWTIMAX\` commodity positions" PAPER_DRAFT.md | grep -q "^1$" && grep -c "2026-04-24T01:28Z" PAPER_DRAFT.md | grep -q "^1$" && grep -c "\`KXBRENTW\`" PAPER_DRAFT.md | grep -q "^1$" && grep -c "commit \`38d7970\`" PAPER_DRAFT.md | grep -q "^1$" && grep -c "commit \`d217ff1\`" PAPER_DRAFT.md | grep -q "^1$" && grep -c "WTI oil contracts absent" PAPER_DRAFT.md | grep -q "^1$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^#### 5\.9\.1 Post-Submission Commodity-Matching Fix" PAPER_DRAFT.md` equals 1 (heading inserted exactly once)
    - `grep -c "1,224 non-\`KXWTIMAX\` commodity positions" PAPER_DRAFT.md` equals 1
    - `grep -c "2026-04-24T01:28Z" PAPER_DRAFT.md` equals 1 (window start timestamp present)
    - `grep -c "2026-04-24T13:00Z" PAPER_DRAFT.md` equals 1 (window end timestamp present)
    - `grep -cE "\`KXBRENTW\`.*486" PAPER_DRAFT.md` equals 1 (table row present)
    - `grep -cE "\`KXWTI\`.*409" PAPER_DRAFT.md` ≥ 1
    - `grep -c "commit \`38d7970\`" PAPER_DRAFT.md` equals 1
    - `grep -c "commit \`d217ff1\`" PAPER_DRAFT.md` equals 1
    - `grep -c "36.0%" PAPER_DRAFT.md` ≥ 2 (new subsection adds one occurrence; other occurrences may already exist in §5.3 etc.)
    - `grep -c "WTI oil contracts absent" PAPER_DRAFT.md` equals 1 (original limitation text preserved)
    - Subsection sits inside §5.9 (appears BEFORE the §5.10 heading): `awk '/^### 5\.9 /,/^### 5\.10 /' PAPER_DRAFT.md | grep -c "Post-Submission Commodity-Matching Fix"` equals 1
  </acceptance_criteria>
  <done>REV-02 satisfied: §5.9 contains a new 5.9.1 subsection with verbatim Phase 15 numbers, per-series table, aggregate P&L and win rate, 12-hour window timestamps, and commit hashes. Original "WTI oil contracts absent" paragraph preserved as historical record.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: REV-03 — §6.4 item 9 append "Resolved post-submission in Phase 15" note</name>
  <files>PAPER_DRAFT.md</files>
  <read_first>
    - PAPER_DRAFT.md lines 628-650 (current §6.4 Limitations, especially item 9 at line 648 which must be preserved)
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md (for commit hash accuracy)
    - .planning/REQUIREMENTS.md lines 244-254 (REV-03 requirement definition — "augmented with a one-line note")
  </read_first>
  <action>
    Append a one-line resolution note to §6.4 item 9 in PAPER_DRAFT.md. The existing text (the full paragraph at line 648 describing the engineering limitation, the 380/395 evicted entries, the KXWTIMAX binary strikes, the Bitcoin-$130K false match, and the "backtest oil edge remains unvalidated on live data" summary sentence) **must be preserved verbatim** — this is critical for honest historical record.

    **Insertion:** Find the exact final sentence of item 9:

      "This is an engineering limitation, not a finding."

    **Append immediately after** that sentence (same paragraph, within the same bullet item, after a single space):

      " **Resolved post-submission in Phase 15 (commit `38d7970` discovery fix + `d217ff1` pair_mapping regeneration).** The `KALSHI_DISCOVERY_CATEGORIES` tuple in `src/live/market_discovery.py` was missing `\"Commodities\"` — Kalshi migrated daily WTI/Brent/grain/metal series into that category, silently dropping them from discovery. After adding `\"Commodities\"` and extending the category classifier, a 12-hour validation window closed 1,224 commodity positions (KXWTI=409, KXBRENTW=486, KXWTIW=213, KXBRENTMON=76, and others); see §5.9 for breakdown."

    Note: the appended text starts with a leading space (to separate from "not a finding."), uses bold markdown for the lead-in phrase, escapes double-quotes around `"Commodities"` with backslashes to render as literal quotes in markdown, and cross-references §5.9 (which now has the 5.9.1 subsection created in Task 2).

    Result structure for item 9 (schematic):

      9. **Live commodity-matching pipeline is incomplete.** [ORIGINAL TEXT ABOUT 395/380 EVICTIONS, KXWTIMAX, BITCOIN FALSE MATCH, FINDING 6 UNVALIDATED] ... This is an engineering limitation, not a finding. **Resolved post-submission in Phase 15 (commit `38d7970` ... d217ff1 ...).** [NEW RESOLUTION DETAIL]... see §5.9 for breakdown.

    Do NOT touch §6.4 items 1-8. Do NOT modify any other part of the paper in this task.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && grep -c "This is an engineering limitation, not a finding\. \*\*Resolved post-submission in Phase 15" PAPER_DRAFT.md | grep -q "^1$" && grep -c "commit \`38d7970\` discovery fix" PAPER_DRAFT.md | grep -q "^1$" && grep -c "silently dropping them from discovery" PAPER_DRAFT.md | grep -q "^1$" && grep -c "see §5\.9 for breakdown" PAPER_DRAFT.md | grep -q "^1$" && grep -c "A post-submission audit of \`data/live/active_matches\.json\`" PAPER_DRAFT.md | grep -q "^1$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "This is an engineering limitation, not a finding\. \*\*Resolved post-submission in Phase 15" PAPER_DRAFT.md` equals 1 (new note immediately follows the old sentence)
    - `grep -c "commit \`38d7970\` discovery fix" PAPER_DRAFT.md` equals 1 (note: Task 2 also uses `38d7970` but in "commit `38d7970`" form not "commit `38d7970` discovery fix" — this phrase is unique to Task 3)
    - `grep -c "see §5\.9 for breakdown" PAPER_DRAFT.md` equals 1
    - `grep -c "A post-submission audit of \`data/live/active_matches\.json\`" PAPER_DRAFT.md` equals 1 (original engineering-gap text preserved — this is the opening of item 9's historical record)
    - `grep -c "unvalidated on live data" PAPER_DRAFT.md` ≥ 1 (original "backtest oil edge remains unvalidated on live data" sentence preserved)
    - The resolution note appears inside §6.4, verified by: `awk '/^### 6\.4 Limitations/,/^### 6\.[5-9]|^## /' PAPER_DRAFT.md | grep -c "Resolved post-submission in Phase 15"` equals 1
    - **Regression gate:** `bash scripts/check_paper.sh` exits 0 (all checks including abstract word count, Fig 2 cap annotation, survivorship disclaimer, live-cohort in 6.4, no dead cross-refs, no residual TODOs). This is the combined gate for REV-01+REV-02+REV-03 together.
  </acceptance_criteria>
  <done>REV-03 satisfied: §6.4 item 9 preserves the original engineering-gap paragraph verbatim and appends a one-line resolution note with commit hashes, root-cause summary, and cross-reference to the new §5.9.1 subsection. Running scripts/check_paper.sh exits 0 with all 16 checks OK, confirming the three REV-01/02/03 edits did not break any Phase 14 regression guardrails.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `bash scripts/check_paper.sh` exits 0 with "ALL CHECKS PASSED" — abstract word count ≤ 250 (POL-04 still holds after REV-01's net +6 then -10 swap), references still ≥ 14 and alphabetical, Table 6/7/9/10 uniquely numbered, per-pair Sharpe headline, Fig 2 cap annotation present, survivorship-bias and live-cohort text in §6.4 preserved, AI disclosure still present, no residual TODOs, no dead cross-refs.
2. `grep -c "1,224" PAPER_DRAFT.md` ≥ 3 (abstract once, §5.9.1 multiple, §6.4 item 9 once).
3. Abstract reads honestly — no overclaiming (12-hour window explicitly stated), no backsliding on "complexity is a liability" thesis.
4. §5.9 retains April 22 "oil contracts absent" paragraph as historical record AND has new 5.9.1 subsection with the post-fix cohort.
5. §6.4 item 9 retains original text AND has resolution note.
</verification>

<success_criteria>
- REV-01 satisfied: abstract qualifier replaced, word count ≤ 250.
- REV-02 satisfied: §5.9.1 subsection with 1,224 positions, per-series table, +$1.96 P&L, 36.0% WR, 12h window, commit hashes.
- REV-03 satisfied: §6.4 item 9 resolution note appended (original text preserved).
- `scripts/check_paper.sh` still exits 0 (no regression on Phase 14 guardrails).
- No inadvertent edits to FINDINGS.md (Plan 16-02 owns FINDINGS.md).
</success_criteria>

<output>
After completion, create `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-01-SUMMARY.md` documenting:
- The three edits applied (abstract swap + trim, §5.9.1 insertion, §6.4 item 9 append)
- Pre-edit and post-edit abstract word counts
- `scripts/check_paper.sh` exit status and 16/16 OK
- Commits (expect 1 atomic commit covering all three tasks, or 3 atomic commits one per task — executor's discretion per CLAUDE.md atomic-commit discipline)
</output>
