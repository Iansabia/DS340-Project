---
phase: 16-paper-revision-phase-15-integration-final-review
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md
  - .planning/STATE.md
  - PAPER_DRAFT.md
  - FINDINGS.md
autonomous: false
requirements:
  - REV-06
  - REV-07
must_haves:
  truths:
    - "A rendered PDF (or pandoc-able markdown) of the post-16-01+16-02 paper exists and is accessible to Ian"
    - "Alvin has reviewed §1 Introduction and §8 Conclusions and returned either edits or an explicit 'no changes' signal"
    - "Ian has completed a cover-to-cover read and surfaced any residual TODO / stale-number / broken-reference findings"
    - "STATE.md records the review outcome with dates and dispositions"
    - "`bash scripts/check_paper.sh` still exits 0 after any final-polish edits (regression gate)"
    - "No residual TODO/FIXME/XXX/[Insert/TBD strings in PAPER_DRAFT.md"
  artifacts:
    - path: ".planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md"
      provides: "Human-review checklist enumerating what Alvin and Ian each look for, with render instructions and approval mechanism"
      contains: "Alvin"
    - path: ".planning/STATE.md"
      provides: "Decisions log entry dated YYYY-MM-DD recording Alvin + Ian review outcomes"
      contains: "Phase 16"
  key_links:
    - from: ".planning/STATE.md Decisions section"
      to: "16-03-REVIEW-CHECKLIST.md"
      via: "Phase 16 review dates and outcomes"
      pattern: "Alvin reviewed|Ian cover-to-cover"
---

<objective>
Close Phase 16 by executing the human-only review that the Phase 14 plan deferred. Plan 16-01 and 16-02 made the content edits; this plan produces a review checklist artifact, pauses for Ian and Alvin to actually read the paper, captures their outcomes in STATE.md, and applies any final-polish edits they request before marking the phase complete.

Purpose: The paper has been mechanically edited (abstract word count, references alphabetical, Phase 15 numbers integrated). What remains is irreducibly human: does the prose flow? Is there a stale number neither grep-check nor tests caught? Is §1 a sensible onramp and §8 a satisfying landing? REV-06 and REV-07 are the "two humans read the paper with submission eyes" gate that the April 27 deadline requires.

Output: A committed review checklist, a signed-off STATE.md entry, a final-polish commit if any edits are requested, and confirmation that `scripts/check_paper.sh` still exits 0.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-paper-revision-phase-15-integration-final-review/16-01-PLAN.md
@.planning/phases/16-paper-revision-phase-15-integration-final-review/16-02-PLAN.md
@PAPER_DRAFT.md
@FINDINGS.md
@scripts/check_paper.sh
@CLAUDE.md

<interfaces>
<!-- This plan consumes the outputs of 16-01 and 16-02. Before Task 1 runs, confirm both are committed. -->

Wave 1 plan outputs this plan depends on:
- 16-01 → PAPER_DRAFT.md updated (abstract, §5.9.1 subsection, §6.4 item 9 resolution note)
- 16-02 → FINDINGS.md updated (Finding 6 companion, Finding 27)

Render options (Ian will pick one):
- Option A: `pandoc PAPER_DRAFT.md -o /tmp/paper_check.pdf --pdf-engine=xelatex` — requires MacTeX / XeLaTeX
- Option B: `pandoc PAPER_DRAFT.md -o /tmp/paper_check.pdf` — default LaTeX engine
- Option C: VSCode markdown preview / GitHub markdown rendering if pandoc not installed — lower fidelity but readable for prose review
- Option D: Marp (already used for slides in Phase 14) — `marp PAPER_DRAFT.md --pdf -o /tmp/paper_check.pdf` — needs marp-cli, may not render tables identically

Check markdown renders in a usable form BEFORE asking Alvin for his pass. If pandoc is not installed on Ian's machine, the checklist should name VSCode preview as the fallback.

REQUIREMENTS.md authoritative lines 253-254:
- REV-06: "Co-author (Alvin) readability review on §1 Introduction and §8 Conclusions. Captured via commit containing either edits applied or STATE.md note 'Alvin reviewed YYYY-MM-DD: no changes requested.'"
- REV-07: "Cover-to-cover final read by Ian. Render paper to PDF, read front-to-back with submission eyes. Any residual TODO / stale number / broken cross-reference fixed in a final polish commit."

Three acceptable disposition branches at the checkpoint:
1. "Both clean" — Alvin no changes + Ian no changes → STATE.md note, commit, phase complete.
2. "Changes needed" — one or both has edits → executor applies them in a final-polish commit, then re-presents the checkpoint.
3. "Blocked" — Alvin unreachable before deadline, or Ian wants to defer → STATE.md note records reason, Phase 16 is marked partial (REV-06 and/or REV-07 flagged as deferred).

Deadline context: April 27, 2026 submission (3 days from 2026-04-24 plan creation). If Alvin is unreachable the day of submission, Ian must decide whether to ship the paper as-is and document REV-06 as partially-satisfied-by-solo-review, or pull the paper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Produce 16-03-REVIEW-CHECKLIST.md artifact + attempt rendering</name>
  <files>.planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/16-paper-revision-phase-15-integration-final-review/16-01-PLAN.md (so checklist items align with what 16-01 changed)
    - .planning/phases/16-paper-revision-phase-15-integration-final-review/16-02-PLAN.md (so checklist items align with what 16-02 changed)
    - PAPER_DRAFT.md (current post-16-01 state, for section references)
    - FINDINGS.md (current post-16-02 state)
    - scripts/check_paper.sh (to quote the automated guardrails in the checklist)
  </read_first>
  <action>
    Create a new file `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md` with the structure below. This artifact is the sole deliverable of Task 1 — it is what the Task 2 checkpoint presents to Ian.

    Additionally, BEFORE writing the checklist file, attempt to render the paper to PDF:

      pandoc PAPER_DRAFT.md -o /tmp/paper_check.pdf 2>&1 | tee /tmp/paper_render.log

    Capture the return code. If pandoc succeeds, include the rendered path (`/tmp/paper_check.pdf`) in the checklist. If pandoc fails (missing pandoc, missing LaTeX engine, etc.), include the fallback instructions in the checklist and do NOT block on the failure — Ian can fall back to the VSCode markdown preview.

    **Required sections of 16-03-REVIEW-CHECKLIST.md** (verbatim structure, fill in specifics):

    ```markdown
    # Phase 16 Final Review Checklist

    **Phase:** 16 — Paper Revision: Phase 15 Integration + Final Review
    **Created:** {YYYY-MM-DD by executor}
    **Reviewers:** Ian Sabia (cover-to-cover / REV-07), Alvin Jang (readability §1 + §8 / REV-06)
    **Deadline:** April 27, 2026
    **Paper state:** Post-16-01 (abstract, §5.9.1, §6.4 item 9) + post-16-02 (Finding 6 companion, Finding 27)

    ## Rendering the Paper

    {ONE of these, depending on pandoc result:}

    - {If pandoc succeeded:} The paper has been rendered to `/tmp/paper_check.pdf` — open that file in Preview or your PDF viewer.
    - {If pandoc failed:} Pandoc render failed (log at `/tmp/paper_render.log`). Fallback options:
      - VSCode: open `PAPER_DRAFT.md`, `Cmd+Shift+V` for live preview
      - GitHub: push to a branch and view on github.com (renders markdown + tables well)
      - Manual pandoc install: `brew install pandoc` then `pandoc PAPER_DRAFT.md -o /tmp/paper_check.pdf`

    ## Alvin's Review (REV-06) — §1 Introduction + §8 Conclusions

    **What to check:**

    - [ ] §1.1 Problem Statement reads as a clear onramp (no jargon without definition)
    - [ ] §1.2 Motivation distinguishes academic and applied audiences clearly
    - [ ] §1.3 Background explanations are correct (prediction-market mechanics, spread definition)
    - [ ] §1.4 Contributions list matches the paper's actual content (does every bullet map to a section?)
    - [ ] §8.1–8.3 Conclusions restate the central answer in clean prose
    - [ ] No sentence is hard to parse on a first read

    **How to respond:**

    - If no changes: Slack / email Ian "Reviewed §1 + §8, no changes requested." Ian will capture the note in STATE.md.
    - If edits: send a diff or a list of specific line-range edits. Executor applies them in a final-polish commit.

    ## Ian's Cover-to-Cover Read (REV-07)

    **What to look for (from Phase 14 retrospective + Phase 16 integration):**

    - [ ] Abstract reads cleanly at ≤ 250 words (verify with: `awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' PAPER_DRAFT.md | wc -w`)
    - [ ] Phase 15 numbers in §5.9.1 match `15-03-SUMMARY.md` verbatim (spot-check KXBRENTW=486, KXWTI=409, total=1,224, +\$1.96, 36.0%)
    - [ ] §6.4 item 9 resolution note cross-references §5.9 and both sections render as links (or plain text `§5.9`)
    - [ ] Tables 1–10 numbering still sequential and no orphan "Table X" references in prose
    - [ ] Figures 1–11 all referenced in prose somewhere (Appendix B index sanity)
    - [ ] No stale numbers: old "0.58" Sharpe, old "4.3" annualization — verify with `grep -E "0\.59 annualize|annualizes to 4\.3" PAPER_DRAFT.md` returns nothing
    - [ ] No TODO / FIXME / XXX / [Insert / TBD markers — `grep -cE "TODO|FIXME|XXX|\[Insert|TBD" PAPER_DRAFT.md` equals 0
    - [ ] Finding 6 backtest table (76.5% WR, +\$0.41/trade) preserved alongside new live companion paragraph
    - [ ] Finding 27 (Silent Category Starvation) present and cross-references Finding 8
    - [ ] §8 Conclusions flows from §6 Discussion without repeating §5 numerically
    - [ ] Regression guardrail still green: `bash scripts/check_paper.sh` prints "ALL CHECKS PASSED"

    **How to signal completion to the checkpoint (Task 2):**

    Report to the executor with one of three structured signals (see Task 2 resume-signal block).

    ## Automated Guardrails (should stay green throughout)

    Run these after any final-polish edits:

    ```bash
    bash scripts/check_paper.sh                                                                         # POL-04/05/06/07/08/09/10 regression
    awk '/^## Abstract/{f=1;next} /^---$/{if(f)exit} f' PAPER_DRAFT.md | wc -w                          # ≤ 250
    grep -cE "TODO|FIXME|XXX|\[Insert|TBD" PAPER_DRAFT.md                                               # = 0
    grep -c "1,224" PAPER_DRAFT.md                                                                      # ≥ 3 (abstract + §5.9.1 multiple + §6.4 item 9)
    grep -c "^## Finding 27:" FINDINGS.md                                                               # = 1
    grep -c "Silent Category Starvation" FINDINGS.md                                                    # = 1
    ```

    ## Approval Disposition (one of three)

    At the Task 2 checkpoint, Ian reports the combined Alvin + self-review outcome. Allowed dispositions:

    1. **Both clean.** "Alvin reviewed YYYY-MM-DD: no changes. Ian cover-to-cover YYYY-MM-DD: no changes." → Executor writes the note to STATE.md and commits. Phase 16 complete.
    2. **Changes requested.** Ian describes the edits (from himself, Alvin, or both). Executor applies them in a final-polish commit, re-runs `scripts/check_paper.sh`, and re-presents the checkpoint for final approval.
    3. **Blocked / Deferred.** Alvin unreachable or Ian defers. Executor records the reason in STATE.md, marks REV-06 and/or REV-07 as deferred in REQUIREMENTS.md (box unchecked with "deferred: <reason>" note), and exits. Phase 16 is partial — user decides whether to ship the paper with partial human review.

    ---

    *Artifact generated by Plan 16-03 Task 1. Consumed by Plan 16-03 Task 2 checkpoint.*
    ```

    After writing the file, verify it exists and is well-formed: `test -f .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md`.

    The pandoc render attempt output (`/tmp/paper_render.log`) does not need to be committed — it is an ephemeral diagnostic. Only the checklist file is committed.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && test -f .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md && grep -c "^## Alvin's Review (REV-06)" .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md | grep -q "^1$" && grep -c "^## Ian's Cover-to-Cover Read (REV-07)" .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md | grep -q "^1$" && grep -c "Approval Disposition" .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md | grep -q "^1$" && grep -c "bash scripts/check_paper.sh" .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md | grep -q "^[1-9]" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md` exists
    - Contains "Alvin's Review (REV-06)" section with checklist items for §1 and §8
    - Contains "Ian's Cover-to-Cover Read (REV-07)" section with at least 8 checklist items including abstract word count, Phase 15 number spot-checks, no-TODO grep, and `scripts/check_paper.sh` green
    - Contains "Approval Disposition" section enumerating the three dispositions (both clean / changes / blocked)
    - Contains either a reference to successfully rendered `/tmp/paper_check.pdf` OR explicit VSCode / GitHub fallback instructions if pandoc failed
    - Does NOT commit the render log `/tmp/paper_render.log` (ephemeral; /tmp isn't in the repo anyway — this is a no-op sanity)
  </acceptance_criteria>
  <done>Review checklist artifact committed; executor has attempted to render the paper to PDF and documented either the rendered path or fallback instructions in the checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Ian reports combined Alvin + self-review outcome</name>
  <files>.planning/STATE.md, PAPER_DRAFT.md (if changes needed), FINDINGS.md (if changes needed)</files>
  <read_first>
    - .planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md (produced in Task 1)
    - Any specific edits Ian describes at the checkpoint
  </read_first>
  <what-built>
    Plans 16-01 and 16-02 integrated Phase 15's 1,224-position commodity cohort into the paper (abstract + §5.9.1 + §6.4 item 9 + Finding 6 companion + Finding 27). Plan 16-03 Task 1 produced the review checklist artifact and attempted a PDF render. All mechanical paper-quality regressions (`scripts/check_paper.sh`) should already be green at this point.
  </what-built>
  <how-to-verify>
    Ian's responsibilities at this checkpoint:

    1. **Render the paper** using one of the options in `16-03-REVIEW-CHECKLIST.md` (pandoc → `/tmp/paper_check.pdf`, VSCode preview, or GitHub preview).
    2. **Alvin's readability review (REV-06):**
       - Send Alvin the rendered paper (or a link if it's on a branch)
       - Ask him to read §1 Introduction + §8 Conclusions
       - Collect his response: "no changes" or a list of specific edits
    3. **Cover-to-cover read (REV-07):**
       - Read PAPER_DRAFT.md front-to-back with submission eyes
       - Run every check in the "Automated Guardrails" block of the checklist
       - Note any residual TODO / stale number / broken reference
    4. **Report outcome to executor** using one of the three disposition templates below.

    Expected automated state at this checkpoint (pre-approval sanity):
      bash scripts/check_paper.sh          → "ALL CHECKS PASSED", exit 0
      grep -c "1,224" PAPER_DRAFT.md       → ≥ 3
      grep -c "^## Finding 27:" FINDINGS.md → 1
      grep -cE "TODO|FIXME|XXX|\[Insert|TBD" PAPER_DRAFT.md → 0
  </how-to-verify>
  <action>
    This is a human-verification checkpoint. The executor does NOT take action until Ian reports a disposition signal via `<resume-signal>` below. On receipt of Signal A (both clean), Signal B (changes requested), or Signal C (deferred), the executor performs the actions documented in the `<resume-signal>` block verbatim (STATE.md append + REQUIREMENTS.md updates + ROADMAP.md updates + atomic commit). The executor MUST re-run `bash scripts/check_paper.sh` after any polish edits in Signal B to ensure no regression. The executor MUST NOT time out or guess a disposition — the checkpoint is blocking.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && bash scripts/check_paper.sh && grep -c "Phase 16" .planning/STATE.md | awk '$1>=1 {exit 0} {exit 1}' && grep -cE "TODO|FIXME|XXX|\[Insert|TBD" PAPER_DRAFT.md | awk '$1==0 {exit 0} {exit 1}' && echo OK</automated>
  </verify>
  <resume-signal>
    Ian reports back with exactly ONE of these three disposition signals:

    **Signal A — "Both clean" (happy path):**
      "Alvin reviewed YYYY-MM-DD: no changes requested. Ian cover-to-cover YYYY-MM-DD: no changes. Approve Phase 16 closeout."

      Executor action on Signal A:
        1. Append a Decisions entry to `.planning/STATE.md` of the form:
           `- [Phase 16-03]: Alvin reviewed YYYY-MM-DD: no changes requested (§1 + §8 readability pass). Ian cover-to-cover YYYY-MM-DD: no changes requested. REV-06 + REV-07 closed. Paper ready for April 27 submission.`
        2. Flip REV-06 and REV-07 to `[x]` complete in `.planning/REQUIREMENTS.md`.
        3. Update ROADMAP.md Phase 16 status to "Complete" and tick any outstanding plan checkboxes.
        4. Verify `bash scripts/check_paper.sh` still green one final time.
        5. Commit the STATE + REQUIREMENTS + ROADMAP update as a single atomic commit with message `docs(16-03): close Phase 16 — Alvin + Ian review complete, REV-06/REV-07 satisfied`.
        6. Phase 16 is complete.

    **Signal B — "Changes requested":**
      "Alvin requested: <list of edits>. Ian found: <list of edits>. Apply these and re-present."

      Executor action on Signal B:
        1. Apply the edits as specific Edit calls to PAPER_DRAFT.md and/or FINDINGS.md
        2. Re-run `bash scripts/check_paper.sh` — it MUST still exit 0 after the polish edits
        3. Re-run the "Automated Guardrails" grep suite from the checklist
        4. Commit the polish as `docs(16-03): apply final-review polish edits from Alvin + Ian`
        5. Re-present this same checkpoint to Ian with the updated state. Loop until Signal A or Signal C.

    **Signal C — "Blocked / Deferred":**
      "Alvin unreachable, ship as-is" OR "Ian defers cover-to-cover read — submitting on Alvin-only OR submitting without either review."

      Executor action on Signal C:
        1. Append a Decisions entry to `.planning/STATE.md` documenting WHICH review is deferred and WHY
        2. Mark the deferred REV-06 or REV-07 in REQUIREMENTS.md with a "deferred: <reason>" suffix instead of flipping to `[x]`
        3. Commit as `docs(16-03): defer REV-06/REV-07 — <reason>`
        4. Phase 16 closed as PARTIAL — user knowingly ships the paper with incomplete human review.

    The checkpoint does not self-close — Ian MUST send one of the three signals. Executor does not time-out.
  </resume-signal>
  <acceptance_criteria>
    After the checkpoint resolves (any disposition):
    - `grep -c "Phase 16" .planning/STATE.md` ≥ 1 (STATE.md has a Phase 16 closeout or deferral note)
    - `grep -cE "Alvin reviewed|Ian cover-to-cover|Alvin.*deferred|Ian.*deferred|Phase 16.*deferred" .planning/STATE.md` ≥ 1 (one of the three dispositions recorded)
    - `bash scripts/check_paper.sh` exits 0 (regardless of disposition — no disposition allows breaking the regression gate)
    - `grep -cE "TODO|FIXME|XXX|\[Insert|TBD" PAPER_DRAFT.md` equals 0 (no residual placeholders slip through to submission)
    - If Signal A or Signal B: `grep -c "REV-06.*Complete\|REV-07.*Complete\|- \[x\] \*\*REV-06" .planning/REQUIREMENTS.md` ≥ 1 per completed requirement
    - If Signal C: `grep -cE "deferred|REV-06.*deferred|REV-07.*deferred" .planning/REQUIREMENTS.md` ≥ 1 for each deferred item
    - Final commit exists in `git log --oneline` with message matching one of the three disposition templates (`docs(16-03): close Phase 16` / `docs(16-03): apply final-review polish edits` / `docs(16-03): defer REV-06/REV-07`)
  </acceptance_criteria>
  <done>
    Phase 16 closed with one of three dispositions:
    (A) Both reviews clean, REV-06 + REV-07 satisfied, paper ready for submission.
    (B) Polish edits applied, re-reviewed, then (A) or (C).
    (C) One or both reviews deferred with documented reason, REV-06/REV-07 flagged as partial in REQUIREMENTS.md.
    In all three dispositions, `scripts/check_paper.sh` exits 0 and no residual TODO placeholders remain in PAPER_DRAFT.md.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Review checklist artifact exists at `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-REVIEW-CHECKLIST.md`.
2. STATE.md contains a dated Phase 16 entry (Signal A, B, or C).
3. REQUIREMENTS.md reflects REV-06 and REV-07 status — either `[x]` complete (A or B terminal) or "deferred" (C).
4. PAPER_DRAFT.md has zero residual TODO/FIXME/XXX/[Insert/TBD markers.
5. `bash scripts/check_paper.sh` exits 0.
6. ROADMAP.md Phase 16 row updated.
</verification>

<success_criteria>
- REV-06 satisfied (or deferred with documented reason): Alvin has reviewed §1 + §8, outcome captured in STATE.md.
- REV-07 satisfied (or deferred with documented reason): Ian cover-to-cover read complete, outcome captured in STATE.md.
- Paper integrity: `scripts/check_paper.sh` green, no TODOs, 1,224 appears at least 3 times, Finding 27 present.
- Phase 16 marked complete or partial in STATE.md / ROADMAP.md / REQUIREMENTS.md.
</success_criteria>

<output>
After completion (Signal A terminal), create `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-03-SUMMARY.md` documenting:
- Final disposition (A, B, or C)
- Review dates for Alvin and Ian
- Any polish edits applied (empty in Signal A, list in Signal B-then-A, partial in Signal C)
- Final `scripts/check_paper.sh` output
- Final PAPER_DRAFT.md line count and word count of abstract
- Commit hashes for all Phase 16 plans (16-01, 16-02, 16-03)
</output>
