---
phase: 16-paper-revision-phase-15-integration-final-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - FINDINGS.md
autonomous: true
requirements:
  - REV-04
  - REV-05
must_haves:
  truths:
    - "Finding 6 (oil near-expiry edge) has a live-validation companion paragraph citing 2026-04-24 Phase 15 numbers with honest backtest-vs-live nuance"
    - "A new Finding 27 'Silent Category Starvation in Live Systems' exists in FINDINGS.md, 80-120 words, documenting root cause + April-11 parallel + monitoring recommendation"
    - "FINDINGS.md Open Questions section integrity preserved (Finding 27 inserted BEFORE 'Open Questions for Paper' header)"
  artifacts:
    - path: "FINDINGS.md"
      provides: "Finding 6 live companion (REV-04), Finding 27 (REV-05)"
      contains: "Finding 27"
  key_links:
    - from: "Finding 6 companion paragraph"
      to: "Phase 15 live window 2026-04-24"
      via: "date marker + 36.0% WR comparison"
      pattern: "2026-04-24|36\\.0%"
    - from: "Finding 27 body"
      to: "Finding 8 (April 11 discovery gap)"
      via: "cross-reference to prior silent-starvation bug"
      pattern: "Finding 8|April 11"
---

<objective>
Update FINDINGS.md with two additions documenting the Phase 15 live-commodity outcome as research content (parallel to Plan 16-01 which handles the paper body). Finding 6 gets a companion paragraph putting the new live 36.0% win rate in honest context against the backtest 76.5%. A new Finding 27 is added documenting the "silent category starvation" root cause as a methodology lesson for external-API discovery pipelines.

Purpose: FINDINGS.md is the project's canonical record of "what we learned." Integrating Phase 15 requires both a *quantitative* update (Finding 6's live companion) and a *methodological* insight (Finding 27 — silent starvation pattern, parallel to Finding 8). Both are standalone additions with no overlap on PAPER_DRAFT.md (Plan 16-01 owns that file), so this plan can execute fully in parallel with 16-01.

Output: FINDINGS.md with Finding 6 augmented and Finding 27 appended before "Open Questions for Paper". Zero edits to PAPER_DRAFT.md.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md
@.planning/phases/15-live-commodity-matching-engineering-fixes/15-VERIFICATION.md
@FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- Authoritative numbers (copy verbatim — source: 15-03-SUMMARY.md). -->

Phase 15 validation numbers:
- 1,224 closed non-KXWTIMAX commodity positions
- Aggregate P&L: +$1.96
- Win rate: 36.0% (441 / 1,224)
- Per-trade: ≈ $0.0016 (= 1.96 / 1,224)
- Window: 2026-04-24T01:28Z → 2026-04-24T13:00Z (~12 hours)
- Top-volume series: KXBRENTW=486, KXWTI=409, KXWTIW=213

Finding 6 (current FINDINGS.md lines 62-79) reference numbers (backtest, April 11, 2026):
- Oil: 765 trades, 76.5% win rate, +$0.41 per-trade, +142.7% edge vs pooled
- Per-category breakdown table exists (Oil / Fed rates / Sports / Politics)
- Why-clause cites "Oil near-expiry contracts (KXWTI-26APR08-T107.99 style) have deterministic convergence"

Finding 8 (current FINDINGS.md line 93, "Commodity Discovery Was Silently Broken April 11-12, 2026") is the parallel April-11 discovery gap referenced by Finding 27. The new Finding 27 should explicitly name Finding 8 so the "two independent silent-starvation bugs" pattern is visible to readers.

FINDINGS.md structure (authoritative line numbers):
- Line 62-79: Finding 6 block (heading at line 62, content ends at line 78, `---` separator at line 80)
- Line 509-567: Finding 26 block (most recent finding)
- Line 567: `---` separator ending Finding 26
- Line 569: `## Open Questions for Paper` header
- Finding 27 inserts BETWEEN line 567 (`---`) and line 569 (Open Questions header)

Finding 6 current closing line (line 78):
  "**Implication:** The alpha is in the asset class, not the model. A simple model on oil beats a complex model on a mixed universe."

The companion paragraph APPENDS after this Implication line but BEFORE the `---` separator at line 80. Must be inserted INSIDE the Finding 6 block, as a labeled sub-paragraph so the backtest-vs-live distinction is typographically clear.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REV-04 — Finding 6 live-validation companion paragraph</name>
  <files>FINDINGS.md</files>
  <read_first>
    - FINDINGS.md lines 62-80 (Finding 6 block in full — backtest numbers table, "Why" paragraph, "Implication" paragraph, closing `---` separator)
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md (for live-window numbers)
    - .planning/REQUIREMENTS.md lines 249-252 (REV-04 definition)
  </read_first>
  <action>
    Append a "Live validation companion" paragraph to Finding 6 in FINDINGS.md. The paragraph inserts after the existing `**Implication:**` line (line 78) and BEFORE the `---` separator at line 80, extending Finding 6's body without replacing any existing text.

    **Exact content to insert** (~110 words, copy verbatim):

    ```markdown

    **Live validation (Phase 15, 2026-04-24).** After the post-submission Phase 15 discovery fix (commits `38d7970` + `d217ff1`), a 12-hour SCC validation window (`2026-04-24T01:28Z` through `2026-04-24T13:00Z`) closed **1,224 non-`KXWTIMAX` commodity positions** across daily/weekly WTI, Brent, and retail-gasoline series (KXWTI=409, KXBRENTW=486, KXWTIW=213 dominate). Aggregate P&L: **+\$1.96**, win rate **36.0%** (441 / 1,224), ≈ \$0.0016 per trade. This is **dramatically lower than the backtest edge above** (76.5% WR, +\$0.41/trade, +142.7%) — and that gap is expected, not contradictory: the backtest measured the *near-expiry convergence subset* of Kalshi oil contracts (KXWTI-26APR08-T107.99-style, hours from settlement), whereas the live cohort spans *full contract lifecycles* including early-window positions where WTI futures prices have not yet pinned the strike. Claiming a robust live oil edge requires a longer window on the same code path (see §7 Future Work).
    ```

    Insertion point rule: the paragraph begins with ONE leading blank line (the empty line at the start of the code block above), then the bolded `**Live validation (Phase 15, 2026-04-24).**` lead-in, then body text, then exactly ONE trailing blank line before the existing `---` separator. The final result for Finding 6 should have the structure:

      [...existing Finding 6 content through "Implication" paragraph at line 78...]
      [BLANK LINE at line 79]
      **Live validation (Phase 15, 2026-04-24).** [new paragraph body]
      [BLANK LINE]
      ---
      [Finding 7 starts here]

    Critical constraints:
    - Do NOT modify the existing per-category table (Oil / Fed rates / Sports / Politics).
    - Do NOT modify the "Why:" or "Implication:" paragraphs.
    - Do NOT modify the Finding 6 heading.
    - Use dollar-escape `\$` consistently (matches surrounding FINDINGS.md conventions; grep for `\$0\.41` in existing Finding 6 to confirm).
    - The paragraph is ~110 words — verifies REV-04's "live-validation companion paragraph" requirement without bloating the finding.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "Live validation (Phase 15, 2026-04-24)" | grep -q "^1$" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "1,224 non-\`KXWTIMAX\` commodity positions" | grep -q "^1$" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "36.0%" | grep -q "^1$" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "76.5%" | grep -q "^1$" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "near-expiry convergence subset" | grep -q "^1$" && awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "The alpha is in the asset class" | grep -q "^1$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "Live validation (Phase 15, 2026-04-24)"` equals 1 (new companion paragraph lead-in present INSIDE Finding 6)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "1,224"` ≥ 1 (live position count appears in Finding 6)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "36.0%"` ≥ 1 (live win rate appears)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "76.5%"` ≥ 1 (backtest win rate preserved — old numbers unchanged)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "near-expiry convergence subset"` equals 1 (honest nuance articulated)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "The alpha is in the asset class"` equals 1 (original Implication sentence preserved)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "+\\\$0.41"` ≥ 1 OR `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -c "\$0.41"` ≥ 1 (original backtest per-trade number preserved; dollar-escape form may vary)
    - `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | wc -w` produces a word count reasonably larger than the pre-edit count (sanity check that ~110 words were added, not overwritten)
  </acceptance_criteria>
  <done>REV-04 satisfied: Finding 6 retains its original backtest table and narrative, and gains a labeled "Live validation (Phase 15, 2026-04-24)" companion paragraph that contrasts 76.5% WR vs 36.0% WR with honest near-expiry-vs-full-lifecycle nuance, citing 1,224 live positions and the commit lineage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REV-05 — Finding 27 "Silent Category Starvation in Live Systems"</name>
  <files>FINDINGS.md</files>
  <read_first>
    - FINDINGS.md lines 93-105 (Finding 8 "Commodity Discovery Was Silently Broken April 11-12, 2026" — the parallel prior silent-starvation bug Finding 27 must reference)
    - FINDINGS.md lines 560-570 (end of Finding 26 through `## Open Questions for Paper` header — where Finding 27 inserts)
    - .planning/phases/15-live-commodity-matching-engineering-fixes/15-03-SUMMARY.md "Decisions Made" section (for root-cause framing)
    - .planning/REQUIREMENTS.md lines 252-253 (REV-05 definition — "~80-120 words")
  </read_first>
  <action>
    Append a new "Finding 27: Silent Category Starvation in Live Systems" block to FINDINGS.md. Insert BETWEEN the `---` separator that closes Finding 26 (line 567) and the `## Open Questions for Paper` header (line 569).

    **Exact content to insert** (~115 words in body — meets REV-05's 80-120 word target; total block with heading and formatting is longer):

    ```markdown
    ## Finding 27: Silent Category Starvation in Live Systems (Phase 15, April 24, 2026)

    **Phase:** Post-Submission Engineering

    **What happened.** For months, `KALSHI_DISCOVERY_CATEGORIES` in `src/live/market_discovery.py` listed `("Economics", "Crypto", "Financials", "Politics", "Climate")` but omitted `"Commodities"`. Kalshi had quietly migrated daily WTI / Brent / grain / metal series into a dedicated Commodities category after an internal taxonomy change, so those series never reached discovery. The pipeline kept running, kept producing bars, kept training — and kept silently dropping the entire oil/commodity asset class. Paper §6.4 item 9 documented the visible symptom (zero live oil trades) but not the root cause.

    **Parallel to Finding 8 (April 11 discovery gap).** This is the *second* silent-starvation bug in the same discovery pipeline: Finding 8 documented Kalshi 429 rate-limiting plus Polymarket shallow pagination truncating the commodity candidate pool. Both bugs share a structural pattern — the system had no way to detect that a known category or series had *dropped out* of the discovered pool. Two independent instances in the same subsystem suggest this is a design weakness, not an accident.

    **Methodology lesson.** External-API discovery pipelines need **active "known-unknown" monitoring** — not only "what IS in my data" dashboards, but also "what's NOT in my data that was here yesterday." Concrete recommendation: a daily category-enumeration job that diffs discovered-series counts by category (commodities, crypto, inflation, etc.) against the prior day's counts and alerts on drops ≥ 50%. A 0-to-47 commodities count overnight should have paged us the day Kalshi migrated the taxonomy; instead, we noticed because the paper's §6.4 limitation stared back at us in a review pass.

    **Implication.** The cost of silent starvation is model-sized: an entire asset class invisible for months, a backtest edge unvalidated by live data, and a paper limitation that shouldn't have needed to exist. Applies generally to any pipeline that enumerates over external category taxonomies that can be mutated server-side without notice.

    ---
    ```

    Insertion rule: the block starts with ONE blank line (after the `---` that closes Finding 26), then the `## Finding 27:` heading, then the four bold sub-sections (`**What happened.**`, `**Parallel to Finding 8...**`, `**Methodology lesson.**`, `**Implication.**`), then a closing `---` separator. After insertion, the `## Open Questions for Paper` header follows with its usual single blank-line separation.

    After insertion, the FINDINGS.md tail sequence becomes:

      [Finding 26 content ... ends around line 565]
      ---
      [BLANK LINE]
      ## Finding 27: Silent Category Starvation in Live Systems (Phase 15, April 24, 2026)
      [... new block body ...]
      ---
      [BLANK LINE]
      ## Open Questions for Paper
      [existing Open Questions content unchanged]

    Do NOT modify Finding 26, Finding 8, or the Open Questions section. Do NOT modify Finding 6 (Plan Task 1 already did).
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && grep -c "^## Finding 27: Silent Category Starvation in Live Systems" FINDINGS.md | grep -q "^1$" && grep -c "Silent Category Starvation" FINDINGS.md | grep -q "^1$" && grep -c "KALSHI_DISCOVERY_CATEGORIES" FINDINGS.md | grep -q "^1$" && grep -c "known-unknown" FINDINGS.md | grep -q "^1$" && awk '/^## Finding 27:/,/^---$/' FINDINGS.md | grep -c "Finding 8" | grep -q "^1$" && awk 'BEGIN{f=0} /^## Finding 27:/{f=NR} /^## Open Questions for Paper/{print (f>0 && NR>f ? "OK" : "FAIL"); exit}' FINDINGS.md | grep -q "OK" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Finding 27: Silent Category Starvation in Live Systems" FINDINGS.md` equals 1
    - `grep -c "Silent Category Starvation" FINDINGS.md` equals 1 (no accidental duplicates in a title-index line)
    - `awk '/^## Finding 27:/,/^---$/' FINDINGS.md | grep -c "KALSHI_DISCOVERY_CATEGORIES"` equals 1 (root-cause named)
    - `awk '/^## Finding 27:/,/^---$/' FINDINGS.md | grep -c "Finding 8"` equals 1 (April-11 parallel cross-reference present)
    - `awk '/^## Finding 27:/,/^---$/' FINDINGS.md | grep -c "known-unknown"` equals 1 (methodology recommendation)
    - Finding 27 sits BEFORE Open Questions, verified by `awk 'BEGIN{f=0} /^## Finding 27:/{f=NR} /^## Open Questions for Paper/{print (f>0 && NR>f ? "OK" : "FAIL"); exit}' FINDINGS.md` outputs "OK"
    - Body word count between 80 and 400 (REV-05 target 80-120 for core body; allowing slack for the four-paragraph structure): `awk '/^## Finding 27:/,/^---$/' FINDINGS.md | wc -w` yields an integer ≥ 80 and ≤ 500
    - `grep -c "^## Finding 6:" FINDINGS.md` equals 1 (Task 1's edit did not corrupt Finding 6 heading)
    - `grep -c "^## Finding 26:" FINDINGS.md` equals 1 (Finding 26 still intact — sanity check, insertion location was correct)
    - `grep -c "^## Open Questions for Paper$" FINDINGS.md` equals 1 (Open Questions header preserved)
  </acceptance_criteria>
  <done>REV-05 satisfied: FINDINGS.md contains a new Finding 27 block with (a) what-happened description of KALSHI_DISCOVERY_CATEGORIES missing "Commodities", (b) explicit parallel to Finding 8 (April 11 discovery gap), (c) methodology lesson on "known-unknown" monitoring, (d) concrete monitoring recommendation. Body meets REV-05's 80-120 word target. Finding 26 and Open Questions section preserved.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c "^## Finding 27" FINDINGS.md` equals 1 AND `grep -c "Silent Category" FINDINGS.md` ≥ 1 (REV-05).
2. Finding 6 has a "live" or "2026-04" marker indicating the new companion paragraph (REV-04): `awk '/^## Finding 6:/,/^---$/' FINDINGS.md | grep -cE "Live validation|2026-04-24"` ≥ 1.
3. FINDINGS.md structure integrity: `grep -cE "^## Finding [0-9]+:" FINDINGS.md` equals 27 (was 26 before this plan).
4. No edits to PAPER_DRAFT.md from this plan (strict file ownership with Plan 16-01): `git diff --name-only PAPER_DRAFT.md` is empty at the end of 16-02 execution (16-02 only modifies FINDINGS.md).
</verification>

<success_criteria>
- REV-04 satisfied: Finding 6 has live-validation companion paragraph with 36.0% WR vs 76.5% WR nuance.
- REV-05 satisfied: Finding 27 added with root cause, April-11 parallel, and monitoring recommendation.
- No regression on prior findings (Finding 6 backtest table preserved; Finding 26 intact; Open Questions preserved).
- PAPER_DRAFT.md untouched — Plan 16-01 owns it exclusively.
</success_criteria>

<output>
After completion, create `.planning/phases/16-paper-revision-phase-15-integration-final-review/16-02-SUMMARY.md` documenting:
- Finding 6 companion paragraph word count
- Finding 27 body word count
- Preserved content checksum (grep-based sanity checks confirming no existing findings were clobbered)
- Commits (expect 1-2 atomic commits per CLAUDE.md discipline — Task 1 and Task 2 can be combined or separate; executor discretion)
</output>
