---
phase: 11-tft-training
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - experiments/figures/tft_variable_importance.png
  - PAPER_DRAFT.md
  - FINDINGS.md
autonomous: true
requirements: [TFT-05, TFT-07, TFT-08]

must_haves:
  truths:
    - "VSN heatmap PNG exists at experiments/figures/tft_variable_importance.png"
    - "Attention entropy audit result is documented in FINDINGS.md as Finding 26"
    - "Table 2 in PAPER_DRAFT.md contains a TFT row with actual numbers (or documented negative result)"
    - "Paper section 4.1 reflects the correct number of model tiers (5 if TFT converged, or note in Discussion if not)"
    - "Paper section 6.2.3 is updated with actual TFT result — confirming or revising the GRU/LSTM hypothesis"
    - "Either outcome (success or negative) produces a complete paper finding that extends complexity-vs-performance analysis"
  artifacts:
    - path: "experiments/figures/tft_variable_importance.png"
      provides: "VSN feature-weight heatmap (differentiator figure)"
      min_lines: 0
    - path: "FINDINGS.md"
      provides: "Finding 26 — TFT result (convergence or documented negative)"
    - path: "PAPER_DRAFT.md"
      provides: "Updated Table 2 TFT row; updated §4.1 and §6.2.3"
  key_links:
    - from: "experiments/results/tier2/TFT.json"
      to: "PAPER_DRAFT.md Table 2"
      via: "Numbers read from JSON, entered into markdown table"
      pattern: "\\| TFT "
    - from: "src/models/tft.py _audit_attention()"
      to: "FINDINGS.md Finding 26"
      via: "entropy and max_variable_weight values from TFT.json attention_audit"
      pattern: "entropy"
    - from: "experiments/figures/tft_variable_importance.png"
      to: "PAPER_DRAFT.md"
      via: "Figure reference in section text"
      pattern: "tft_variable_importance"
---

<objective>
Produce the VSN heatmap figure, document the TFT result in FINDINGS.md as Finding 26, and integrate the TFT outcome into the paper — both the headline Table 2 and the complexity-vs-performance discussion sections. Covers both convergence and negative-result branches.

Purpose: Whether TFT converged or not, Phase 13 (Ensemble Formalization) needs to know the definitive TFT outcome; the paper needs an updated Table 2; and the VSN heatmap provides a differentiator figure regardless of P&L performance.

Output: `experiments/figures/tft_variable_importance.png`, updated `FINDINGS.md` (Finding 26), updated `PAPER_DRAFT.md` (Table 2 TFT row, §4.1, §6.2.3).
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-tft-training/11-RESEARCH.md
@.planning/phases/11-tft-training/11-01-SUMMARY.md
@experiments/results/tier2/TFT.json
@FINDINGS.md
@PAPER_DRAFT.md
</context>

<interfaces>
<!-- Key contracts from prior plan. -->

From experiments/results/tier2/TFT.json (written by Plan 11-01):
```json
{
  "model": "TFT",
  "converged": true | false,
  "rmse": <float>,
  "rmse_std": <float>,
  "seed_rmses": [...],
  "total_pnl": <float>,
  "gru_baseline_rmse": 0.2928,
  "beats_gru": true | false,
  "attention_audit": {
    "entropy": <float>,
    "max_variable_weight": <float>,
    "threshold_entropy": <float>,
    "is_degenerate": true | false
  },
  "directional_accuracy": <float>,
  "win_rate": <float>,
  "num_trades": <int>,
  "sharpe_ratio": <float>,
  "note": "..."
}
```

From PAPER_DRAFT.md Table 2 (current, no TFT row):
```markdown
**Table 2: Single-split backtest results (2 pp fees, 1,673-row test set).**

| Tier | Model | RMSE | Dir.Acc | P&L ($) | Win Rate | Sharpe | Trades |
|------|-------|------|---------|---------|----------|--------|--------|
| 1 | XGBoost | 0.280 | 66.0% | +201.63 | 51.4% | 0.436 | 1516 |
| 1 | Linear Regression | 0.278 | 66.3% | +201.69 | 51.4% | 0.437 | 1542 |
| 2 | LSTM | 0.295 | 65.9% | +182.72 | 50.0% | 0.387 | 1542 |
| 2 | GRU | 0.293 | 65.0% | +174.11 | 48.8% | 0.372 | 1516 |
| 0 | Naive ... | ...
```

PAPER_DRAFT.md §6.2.3 discusses GRU/LSTM hypothesis:
  "Our honest prediction: by 250 bars/pair, GRU should close the gap..."
  (line ~498 area — read PAPER_DRAFT.md around line 484 before editing)

GRU baseline comparison (TFT-04 gate):
  GRU RMSE = 0.2928, P&L = +$212.50
  LSTM RMSE = 0.2915, P&L = +$221.84

VSN heatmap API (from 11-RESEARCH.md §Pattern 3):
```python
with torch.no_grad():
    raw_out = model.predict(train_loader, mode="raw", return_x=False)
interpretation = model.interpret_output(raw_out, reduction="mean")
encoder_vars = interpretation["encoder_variables"].detach().cpu().numpy()
feature_names = model.encoder_variables
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract VSN heatmap and document Finding 26</name>
  <files>experiments/figures/tft_variable_importance.png, FINDINGS.md</files>

  <read_first>
    - experiments/results/tier2/TFT.json (read ALL fields — esp. converged, rmse, attention_audit)
    - .planning/phases/11-tft-training/11-01-SUMMARY.md (what actually happened during training)
    - .planning/phases/11-tft-training/11-RESEARCH.md §Pattern 3 (VSN heatmap extraction code)
    - FINDINGS.md (read last 30 lines to find correct Finding number, confirm it's 26)
    - src/models/tft.py (to load model for heatmap extraction)
  </read_first>

  <action>
    Step A — Determine convergence outcome from TFT.json:
    Read /experiments/results/tier2/TFT.json. Check "converged" field.
    All subsequent steps branch on this value.

    Step B — Generate VSN heatmap regardless of convergence outcome:

    Create a script experiments/extract_tft_heatmap.py (~40 LOC) that:
    1. Loads train.parquet, computes derived features
    2. Re-trains TFTPredictor with seed=42 (same as run_tft.py) for max_epochs=30
       (or re-uses a cached model if TFTPredictor.save() was called in run_tft.py)
    3. Extracts VSN weights via model._model.predict(train_loader, mode="raw", return_x=False)
       followed by model._model.interpret_output(raw_out, reduction="mean")
    4. Plots horizontal bar chart sorted by importance (top-15 features only if >15)
    5. Saves to experiments/figures/tft_variable_importance.png at 300 DPI

    NOTE: If TFT did not converge (exception during fit), produce a placeholder heatmap
    showing "TFT did not converge — VSN weights unavailable" as text in the figure.
    This ensures the artifact path exists regardless.

    Run: cd "/Users/iansabia/Desktop/DS340 Project" && PYTHONPATH=. .venv/bin/python experiments/extract_tft_heatmap.py

    Step C — Write FINDINGS.md Finding 26:

    Finding 26 should appear immediately after Finding 23 (the last finding).
    Finding number: confirm by reading FINDINGS.md; if 24 or 25 already exist, use the next available.
    Use "Finding 26" (or actual next number) consistently.

    Template for CONVERGENCE case:
    ```markdown
    ---

    ## Finding 26: TFT Single-Split Results (Phase 11, <DATE>)
    **Phase:** Phase 11 TFT Training

    **Result:** TFT CONVERGED — val RMSE {actual_rmse:.4f} beats GRU baseline (0.2928).

    **Dataset:** 6,802 training rows, 1,673 test rows, 144 pairs. Single-split.

    **Hyperparameters:** hidden_size=8, attention_head_size=1, dropout=0.3, lstm_layers=1,
    max_encoder_length=6, QuantileLoss([0.1, 0.5, 0.9]), GroupNormalizer(transformation=None).

    **Results across 3 seeds:**
    | Seed | RMSE | P&L ($) |
    |------|------|---------|
    | 42   | {seed_rmses[0]:.4f} | {seed_pnls[0]:.2f} |
    | 7    | {seed_rmses[1]:.4f} | {seed_pnls[1]:.2f} |
    | 123  | {seed_rmses[2]:.4f} | {seed_pnls[2]:.2f} |
    | Mean | {avg_rmse:.4f} | {avg_pnl:.2f} |

    **Attention audit:** entropy={entropy:.3f} (threshold={threshold:.3f}),
    max_variable_weight={max_weight:.3f}, is_degenerate={is_degenerate}.

    **Ranking update:** {insert position in Table 2 after actual numbers}

    **Implication:** TFT with {N} parameters ({samples_per_param:.1f} samples/param) at N=6,802
    rows {beats/does not beat} the GRU baseline. This {extends/refutes} the simplicity-wins thesis
    to the transformer architecture tier.
    ```

    Template for NEGATIVE RESULT case:
    ```markdown
    ---

    ## Finding 26: TFT Negative Result at N=6,802 (Phase 11, <DATE>)
    **Phase:** Phase 11 TFT Training

    **Result:** TFT DID NOT CONVERGE — {failure_reason from TFT.json}

    **Hyperparameters attempted:** hidden_size=8, attention_head_size=1, dropout=0.3,
    lstm_layers=1, max_encoder_length=6, QuantileLoss([0.1, 0.5, 0.9]).

    **Interpretation:** At N=6,802 rows and 144 pairs, TFT's transformer-attention mechanism
    requires more data than is available in this dataset. The simplicity-wins thesis now extends
    to transformer architectures, not only recurrent networks.

    **For paper:** "We attempted TFT (hidden_size=8, 3-quantile loss) and found it did not
    converge within the pre-specified 30-epoch budget at N=6,802 training rows. This extends
    the complexity-is-a-liability finding from recurrent to transformer architectures."
    ```

    Use ALL actual numbers from TFT.json — no fabrication.
  </action>

  <verify>
    <automated>
      test -f "/Users/iansabia/Desktop/DS340 Project/experiments/figures/tft_variable_importance.png" && echo "heatmap exists"
      grep -q "Finding 26" "/Users/iansabia/Desktop/DS340 Project/FINDINGS.md" && echo "Finding 26 written" || echo "MISSING"
    </automated>
  </verify>

  <done>
    - experiments/figures/tft_variable_importance.png exists (300 DPI, >= 1KB)
    - FINDINGS.md contains Finding 26 with actual numbers from TFT.json
    - Finding 26 covers the correct branch (convergence or negative result)
    - All numbers sourced from TFT.json — no fabricated values
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PAPER_DRAFT.md — Table 2 TFT row, §4.1, §6.2.3</name>
  <files>PAPER_DRAFT.md</files>

  <read_first>
    - experiments/results/tier2/TFT.json (actual numbers — read before editing paper)
    - .planning/phases/11-tft-training/11-01-SUMMARY.md (convergence outcome)
    - PAPER_DRAFT.md lines 134-160 (§4.1 Model Tiers)
    - PAPER_DRAFT.md lines 210-230 (Table 2 and surrounding analysis text)
    - PAPER_DRAFT.md lines 484-510 (§6.2.3 GRU and LSTM discussion)
  </read_first>

  <action>
    Read TFT.json first. Determine: converged=True or False.

    BRANCH A — TFT CONVERGED (converged=True):

    Change 1 — §4.1 Model Tiers (around line 134):
    Current: "trains four tiers of models — regression baselines (LR, XGBoost), sequence models (GRU, LSTM), PPO..."
    Change to: "trains five model families — regression baselines (LR, XGBoost), sequence models (GRU, LSTM, TFT), PPO..."
    Also add TFT bullet after the LSTM bullet:
    "- *TFT* (PyTorch Forecasting, hidden_size=8, attention_head_size=1, dropout=0.3, 3-quantile QuantileLoss, GroupNormalizer per-pair)."

    Change 2 — Table 2 (around line 212):
    Add TFT row after LSTM row using actual numbers from TFT.json:
    | 2 | TFT | {rmse:.3f} | {directional_accuracy:.1f}% | +{total_pnl:.2f} | {win_rate:.1f}% | {sharpe_ratio:.3f} | {num_trades} |
    NOTE: Format to match existing table style (2 decimal places for RMSE, etc.)

    Change 3 — Analysis paragraph after Table 2:
    Update the observation about "three observations" to add a fourth (or update the third):
    "**Fourth**, TFT with pre-specified small-data hyperparameters achieves RMSE={rmse:.3f},
    {beating GRU at 0.2928 / not beating GRU at 0.2928}. The VSN attention entropy ({entropy:.3f})
    {is above / is below} the degenerate threshold ({threshold:.3f}), indicating the attention
    mechanism {is / is not} learning meaningful feature distinctions despite N=6,802 rows."

    Change 4 — §6.2.3 (around line 484):
    Update prediction at line ~498:
    "Our honest prediction: by 250 bars/pair..." paragraph — update to reference TFT result:
    "We trained TFT at N=6,802 rows (Phase 11, April 2026) with pre-specified small-data
    hyperparameters (hidden_size=8). TFT achieved RMSE={rmse:.3f} vs. GRU's 0.2928,
    {confirming that transformer attention adds no value at this data scale / showing that
    TFT can match recurrent architectures even at small N}. This extends the simplicity-wins
    thesis to transformer architectures."

    Replace or update the sentence "1000 bars/pair, TFT (which we did not train) becomes
    justifiable..." to "TFT was trained at N=6,802 rows; at 1000 bars/pair, a larger TFT
    configuration (hidden_size=32+) would become more justifiable..."

    BRANCH B — TFT DID NOT CONVERGE (converged=False):

    Change 1 — §4.1 Model Tiers:
    DO NOT change the tier count. Add a note in parentheses:
    "We attempted TFT (Temporal Fusion Transformer) with pre-specified small-data
    hyperparameters; it did not converge at N=6,802 rows (see §6 Discussion)."

    Change 2 — Table 2:
    Add a TFT row with a note:
    | 2 | TFT | — | — | — | — | — | — |
    Add a table footnote: "TFT: did not converge at N=6,802 (30-epoch budget, hidden_size=8). See §6.4."

    Change 3 — §6.2.3:
    Replace "TFT (which we did not train) becomes justifiable..." with:
    "We attempted TFT with hidden_size=8 and 3-quantile loss at N=6,802 rows (Phase 11).
    TFT did not converge: {failure_reason from TFT.json}. This extends the
    simplicity-wins finding to the transformer tier — at this data scale, even a
    minimal TFT configuration cannot train stably. Larger TFT configurations become
    justifiable at 1,000+ bars/pair."

    BOTH BRANCHES — VSN heatmap figure reference:
    Find the figure section (around line 200 or wherever figures are discussed).
    Add a reference to the VSN heatmap:
    "Figure X: TFT Variable Selection Network (VSN) encoder importance weights, showing
    which of the 51 features TFT's attention mechanism weights most heavily
    (see experiments/figures/tft_variable_importance.png)."

    ABSTRACT UPDATE (both branches):
    Find the abstract (line ~12). The abstract currently says "four tiers of models".
    If converged: change to "five model families" or add "(including TFT)" after LSTM.
    If not converged: add a parenthetical: "(TFT was attempted but did not converge at N=6,802)"
    Keep abstract <= 250 words (currently ~412 — do not expand further).
  </action>

  <verify>
    <automated>
      grep -q "TFT" "/Users/iansabia/Desktop/DS340 Project/PAPER_DRAFT.md" && echo "TFT appears in paper"
      grep -q "^| 2 | TFT " "/Users/iansabia/Desktop/DS340 Project/PAPER_DRAFT.md" && echo "TFT row in Table 2" || grep -q "TFT.*did not converge\|TFT | —" "/Users/iansabia/Desktop/DS340 Project/PAPER_DRAFT.md" && echo "TFT negative result in Table 2"
      grep -q "tft_variable_importance\|VSN" "/Users/iansabia/Desktop/DS340 Project/PAPER_DRAFT.md" && echo "heatmap referenced in paper"
    </automated>
  </verify>

  <done>
    - PAPER_DRAFT.md Table 2 contains a TFT row (with actual numbers if converged, or documented negative)
    - §4.1 updated: 5 model families if converged, or TFT attempt noted if not
    - §6.2.3 updated with actual TFT result replacing the "we did not train TFT" placeholder
    - VSN heatmap is referenced somewhere in the paper body
    - All numbers sourced from experiments/results/tier2/TFT.json — no fabricated values
    - Paper does NOT expand beyond current length (no new sections for TFT alone)
  </done>
</task>

</tasks>

<verification>
Phase 11-02 complete when:
- [ ] experiments/figures/tft_variable_importance.png exists
- [ ] FINDINGS.md contains "Finding 26" with TFT result and actual numbers
- [ ] PAPER_DRAFT.md Table 2 has a TFT row (real numbers or documented negative row)
- [ ] PAPER_DRAFT.md §4.1 reflects correct model count (5 or 4+note)
- [ ] PAPER_DRAFT.md §6.2.3 no longer says "TFT (which we did not train)"
- [ ] grep -q "TFT" PAPER_DRAFT.md passes
- [ ] All numbers in paper match experiments/results/tier2/TFT.json exactly
</verification>

<success_criteria>
1. VSN heatmap PNG exists (success outcome: shows actual feature weights; negative outcome: placeholder figure)
2. Finding 26 documents the TFT outcome completely — Phase 13 can read it to decide TFT variant inclusion
3. Paper is updated for both Table 2 and the discussion sections — no placeholder remains
4. Phase 13 unblocked: TFT.json converged=True triggers 5th ensemble variant; converged=False keeps 4-variant ensemble
</success_criteria>

<output>
After completion, create `.planning/phases/11-tft-training/11-02-SUMMARY.md`

Include:
- VSN heatmap path and top-5 most important features (if available)
- Paper sections updated with line numbers
- TFT final verdict: converged or negative result (for Phase 13 planning)
- Any deviations from plan
</output>
