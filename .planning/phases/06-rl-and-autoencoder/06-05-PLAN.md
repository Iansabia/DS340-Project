---
phase: 06-rl-and-autoencoder
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-04
files_modified:
  - src/models/__init__.py
  - experiments/run_baselines.py
  - experiments/results/tier3/ppo_raw.json
  - experiments/results/tier3/ppo_filtered.json
autonomous: true
requirements:
  - MOD-08
  - MOD-09
  - MOD-10

must_haves:
  truths:
    - "run_baselines.py --tier 3 trains PPO-Raw and PPO-Filtered with 3 seeds each and saves results"
    - "run_baselines.py --tier all produces a full cross-tier table (Tier 1 + 2 + 3)"
    - "Tier 3 result JSONs match the schema of Tier 1/2 JSONs (model_name, metrics, timestamp, extra)"
    - "PPO-Raw and PPO-Filtered appear in __init__.py exports alongside existing models"
    - "AnomalyDetectorAutoencoder appears in __init__.py exports"
    - "Cross-tier comparison table includes all 8 models (4 Tier 1 + 2 Tier 2 + 2 Tier 3)"
  artifacts:
    - path: "experiments/run_baselines.py"
      provides: "Extended harness with --tier 3 and --tier all"
      contains: "tier.*3"
    - path: "experiments/results/tier3/ppo_raw.json"
      provides: "PPO-Raw evaluation results"
      contains: "PPO-Raw"
    - path: "experiments/results/tier3/ppo_filtered.json"
      provides: "PPO-Filtered evaluation results"
      contains: "PPO-Filtered"
    - path: "src/models/__init__.py"
      provides: "Updated exports with all Phase 6 classes"
      contains: "PPORawPredictor"
  key_links:
    - from: "experiments/run_baselines.py"
      to: "src/models/ppo_raw.py"
      via: "import PPORawPredictor"
      pattern: "from src\\.models\\.ppo_raw import PPORawPredictor"
    - from: "experiments/run_baselines.py"
      to: "src/models/ppo_filtered.py"
      via: "import PPOFilteredPredictor"
      pattern: "from src\\.models\\.ppo_filtered import PPOFilteredPredictor"
    - from: "experiments/run_baselines.py"
      to: "src/models/autoencoder.py"
      via: "import AnomalyDetectorAutoencoder"
      pattern: "from src\\.models\\.autoencoder import AnomalyDetectorAutoencoder"
---

<objective>
Wire all Phase 6 models into the experiment harness, export them from __init__.py, train with 3 seeds each, and produce the full cross-tier comparison table.

Purpose: This plan closes the loop on MOD-08/09/10 by producing actual trained results and integrating them into the same evaluation infrastructure used by Tier 1 and Tier 2. The cross-tier table is the centerpiece deliverable for Phase 7's complexity-vs-performance analysis.

Output: Updated run_baselines.py with --tier 3 and --tier all, tier3/*.json result files, updated __init__.py.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rl-and-autoencoder/06-CONTEXT.md
@.planning/phases/06-rl-and-autoencoder/06-RESEARCH.md
@.planning/phases/06-rl-and-autoencoder/06-01-SUMMARY.md
@.planning/phases/06-rl-and-autoencoder/06-02-SUMMARY.md
@.planning/phases/06-rl-and-autoencoder/06-03-SUMMARY.md
@.planning/phases/06-rl-and-autoencoder/06-04-SUMMARY.md

<interfaces>
From src/models/__init__.py (current):
```python
from src.models.base import BasePredictor
from src.models.linear_regression import LinearRegressionPredictor
from src.models.xgboost_model import XGBoostPredictor
from src.models.naive import NaivePredictor
from src.models.volume import VolumePredictor
from src.models.gru import GRUPredictor
from src.models.lstm import LSTMPredictor

__all__ = [
    "BasePredictor", "LinearRegressionPredictor", "XGBoostPredictor",
    "NaivePredictor", "VolumePredictor", "GRUPredictor", "LSTMPredictor",
]
```

From experiments/run_baselines.py (current):
```python
# --tier choices: ["1", "2", "both"]
# TIER2_SEEDS = [42, 123, 456]
# DEFAULT_RESULTS_DIR_TIER2 = Path("experiments/results/tier2")
# _MODEL_ORDER = ["Naive...", "Volume...", "Linear...", "XGBoost", "GRU", "LSTM"]
# build_models(tier) returns model instances
# run_tier2_with_seeds() handles multi-seed training
# format_comparison_table() renders the table
```

From experiments/results/tier2/gru.json (schema to match):
```json
{
  "model_name": "GRU",
  "metrics": {"rmse": ..., "mae": ..., "directional_accuracy": ..., "total_pnl": ..., "num_trades": ..., "win_rate": ..., "sharpe_ratio": ..., "sharpe_per_trade": ...},
  "timestamp": "...",
  "threshold": 0.02,
  "n_train_rows": 6802,
  "n_test_rows": 1673,
  "n_features": 31,
  "pnl_series": [...],
  "seeds": [42, 123, 456],
  "seed_rmses": [...],
  "mean_rmse": ...,
  "std_rmse": ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update __init__.py exports and extend run_baselines.py with --tier 3 and --tier all</name>
  <files>src/models/__init__.py, experiments/run_baselines.py</files>
  <read_first>
    - .planning/phases/06-rl-and-autoencoder/06-CONTEXT.md
    - src/models/__init__.py
    - experiments/run_baselines.py
    - src/models/ppo_raw.py
    - src/models/ppo_filtered.py
    - src/models/autoencoder.py
  </read_first>
  <action>
**Step 1: Update src/models/__init__.py**

Add imports and exports for all three Phase 6 classes:
```python
from src.models.ppo_raw import PPORawPredictor
from src.models.ppo_filtered import PPOFilteredPredictor
from src.models.autoencoder import AnomalyDetectorAutoencoder
```
Add to __all__: "PPORawPredictor", "PPOFilteredPredictor", "AnomalyDetectorAutoencoder"

**Step 2: Extend experiments/run_baselines.py**

2a. Add imports at top:
```python
from src.models.ppo_raw import PPORawPredictor
from src.models.ppo_filtered import PPOFilteredPredictor
from src.models.autoencoder import AnomalyDetectorAutoencoder
```

2b. Add constants:
```python
DEFAULT_RESULTS_DIR_TIER3 = Path("experiments/results/tier3")
TIER3_SEEDS = [42, 123, 456]  # same seeds as Tier 2
```

2c. Update _MODEL_ORDER to include PPO-Raw and PPO-Filtered at end:
```python
_MODEL_ORDER = [
    "Naive (Spread Closes)", "Volume (Higher Volume Correct)",
    "Linear Regression", "XGBoost", "GRU", "LSTM",
    "PPO-Raw", "PPO-Filtered",
]
```

2d. Update build_models() to handle tier="3":
```python
tier3 = [PPORawPredictor(random_state=42), PPOFilteredPredictor(random_state=42)]
if tier == "3": return tier3
if tier == "all": return tier1 + tier2 + tier3
```

2e. Update argparse choices: `choices=["1", "2", "3", "both", "all"]`
- "both" remains backward-compatible (Tier 1 + 2)
- "all" is new (Tier 1 + 2 + 3)

2f. Create `run_tier3_with_seeds()` function following run_tier2_with_seeds pattern:
- Accept df_train, df_test, feature_cols, threshold, results_dir, n_train, n_test, n_features, seeds, test_timestamps
- For PPO-Raw: iterate seeds, train PPORawPredictor(random_state=seed, total_timesteps=100_000) with fit/evaluate
- For PPO-Filtered: train ONE autoencoder first (seed=42), then iterate seeds for PPOFilteredPredictor(anomaly_detector=autoencoder, random_state=seed, total_timesteps=100_000)
- Collect per-seed RMSE, save last seed's full results + seed aggregation in extra field
- JSON schema matches Tier 2 (model_name, metrics, timestamp, extra with seeds, seed_rmses, mean_rmse, std_rmse)

2g. Update main() to handle tier=="3" and tier=="all":
- tier=="3": call run_tier3_with_seeds() with tier3_results_dir
- tier=="all": run all three tiers, combine results, print full table
- Both Tier 2 and Tier 3 use prepare_xy_for_seq (need group_id for pair-boundary windowing)

2h. Update format_comparison_table() tier title:
- tier=="3": "====== Tier 3 RL Results ======"
- tier=="all": "====== Full Cross-Tier Comparison ======"

**IMPORTANT:** PPO fit() requires group_id in X_train. Use prepare_xy_for_seq() (not prepare_xy()) for Tier 3, same as Tier 2.
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && python -c "from src.models import PPORawPredictor, PPOFilteredPredictor, AnomalyDetectorAutoencoder; print('imports OK')" && python -m experiments.run_baselines --help 2>&1 | grep -E "tier|3|all"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PPORawPredictor" src/models/__init__.py` matches
    - `grep "PPOFilteredPredictor" src/models/__init__.py` matches
    - `grep "AnomalyDetectorAutoencoder" src/models/__init__.py` matches
    - `python -m experiments.run_baselines --help` shows choices including "3" and "all"
    - `grep "run_tier3_with_seeds" experiments/run_baselines.py` matches
    - `grep "DEFAULT_RESULTS_DIR_TIER3" experiments/run_baselines.py` matches
    - `grep "PPO-Raw" experiments/run_baselines.py` matches in _MODEL_ORDER
  </acceptance_criteria>
  <done>__init__.py exports all Phase 6 classes. run_baselines.py accepts --tier 3 and --tier all, has run_tier3_with_seeds function, and updated model order.</done>
</task>

<task type="auto">
  <name>Task 2: Run --tier 3 training and produce tier3 JSON results + cross-tier table</name>
  <files>experiments/results/tier3/ppo_raw.json, experiments/results/tier3/ppo_filtered.json</files>
  <read_first>
    - .planning/phases/06-rl-and-autoencoder/06-CONTEXT.md
    - experiments/run_baselines.py
  </read_first>
  <action>
Run the Tier 3 experiment:

```bash
mkdir -p experiments/results/tier3
python -m experiments.run_baselines --tier 3
```

This will:
1. Load data/processed/train.parquet and test.parquet
2. Train PPO-Raw with seeds {42, 123, 456} (100,000 timesteps each, ~3 runs)
3. Train autoencoder once, then PPO-Filtered with seeds {42, 123, 456}
4. Save results to experiments/results/tier3/ppo_raw.json and ppo_filtered.json
5. Print Tier 3 comparison table

Then run the full cross-tier comparison:
```bash
python -m experiments.run_baselines --tier all
```

This produces the full 8-model comparison table (no retraining if results already saved).

**Expected output:** PPO-Raw likely shows 0 trades (hold-only) or poor Sharpe. PPO-Filtered likely shows some trades with below-XGBoost performance. Both are valid findings per CONTEXT.md.

**If PPO-Raw converges to all-hold (0 trades):** That IS the result. Do NOT tune hyperparameters. Document it. This answers the research question.

**Timeout:** The training may take 5-10 minutes per seed. If it exceeds 10 minutes per model, reduce total_timesteps to 50,000 in the run command (not in the code defaults -- keep 100,000 as default).

Capture the final comparison table output to a text file for the SUMMARY:
```bash
python -m experiments.run_baselines --tier all 2>&1 | tee experiments/results/tier3/cross_tier_comparison.txt
```
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && python -c "
import json, pathlib
t3 = pathlib.Path('experiments/results/tier3')
for f in ['ppo_raw.json', 'ppo_filtered.json']:
    d = json.loads((t3/f).read_text())
    assert 'model_name' in d and 'metrics' in d
    m = d['metrics']
    assert all(k in m for k in ('rmse','mae','directional_accuracy','total_pnl','num_trades','win_rate','sharpe_ratio'))
    print(f'{f}: {d[\"model_name\"]} RMSE={m[\"rmse\"]:.4f} trades={m[\"num_trades\"]} sharpe={m[\"sharpe_ratio\"]:.4f}')
print('All tier3 JSONs valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at experiments/results/tier3/ppo_raw.json with valid JSON containing model_name="PPO-Raw"
    - File exists at experiments/results/tier3/ppo_filtered.json with valid JSON containing model_name="PPO-Filtered"
    - Both JSONs contain metrics dict with keys: rmse, mae, directional_accuracy, total_pnl, num_trades, win_rate, sharpe_ratio
    - Both JSONs contain extra dict with keys: seeds, seed_rmses, mean_rmse, std_rmse
    - `python -m experiments.run_baselines --tier all` runs without error and prints 8-model table
  </acceptance_criteria>
  <done>Tier 3 results produced. PPO-Raw and PPO-Filtered trained with 3 seeds each. JSON files saved. Full cross-tier comparison table (8 models) printed and captured. Results honestly reflect RL performance (even if underwhelming).</done>
</task>

</tasks>

<verification>
- `python -m experiments.run_baselines --tier all` runs and prints full table with 8 models
- `ls experiments/results/tier3/` shows ppo_raw.json and ppo_filtered.json
- Both JSON files match the Tier 2 schema (same keys)
- `python -c "from src.models import PPORawPredictor, PPOFilteredPredictor, AnomalyDetectorAutoencoder"` imports cleanly
</verification>

<success_criteria>
All Phase 6 models are trained, evaluated, and integrated into the experiment harness. The cross-tier comparison table shows 8 models (4 Tier 1 + 2 Tier 2 + 2 Tier 3) with RMSE, MAE, directional accuracy, P&L, trades, win rate, and Sharpe. Results are honestly reported regardless of RL performance. tier3/*.json files exist with correct schema.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rl-and-autoencoder/06-05-SUMMARY.md`
</output>
