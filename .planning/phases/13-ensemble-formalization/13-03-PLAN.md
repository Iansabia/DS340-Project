---
phase: 13-ensemble-formalization
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - PAPER_DRAFT.md
  - FINDINGS.md
autonomous: false
requirements:
  - ENSM-07

must_haves:
  truths:
    - "PAPER_DRAFT.md contains a new section 5.11 Ensemble Formalization after section 5.10"
    - "Section 5.11 has an ensemble-variant comparison table with all 4 variants and concordance audit columns"
    - "Section 5.11 honestly states the weight sweep result (near-flat expected) and interprets concordance filter as the primary discriminator"
    - "Section 4.4 (Live System Architecture) is updated with evidence-based ensemble justification referencing the concordance audit numbers"
    - "FINDINGS.md contains Finding 26 documenting ensemble weight immateriality and concordance filter behavior"
    - "Paper references the weight-sweep figure: experiments/figures/ensemble_weight_sweep.png"
  artifacts:
    - path: "PAPER_DRAFT.md"
      provides: "Section 5.11 with ensemble table and weight-sweep figure reference; updated Section 4.4"
      contains: "5.11 Ensemble"
    - path: "FINDINGS.md"
      provides: "Finding 26: ensemble weight sensitivity"
      contains: "Finding 26"
  key_links:
    - from: "PAPER_DRAFT.md §5.11"
      to: "experiments/results/ensemble/summary.json"
      via: "Numbers in ensemble table sourced from summary.json — no fabrication"
      pattern: "5.11"
    - from: "PAPER_DRAFT.md §4.4"
      to: "PAPER_DRAFT.md §5.11"
      via: "§4.4 references concordance audit findings from §5.11"
      pattern: "4.4"
---

<objective>
Write paper section 5.11 "Ensemble Formalization" and update section 4.4 "Live System Architecture" with numbers from the ensemble sweep. Record Finding 26.

Purpose: Closes the paper evidence loop for Phase 13. The concordance audit numbers and weight-sweep result constitute a new honest finding: the live ensemble's weight choice is immaterial, and the concordance filter is the primary risk control (with quantified cost if P4 flag fired).

Output: PAPER_DRAFT.md (§5.11 added, §4.4 updated), FINDINGS.md (Finding 26 added)
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ensemble-formalization/13-RESEARCH.md
@.planning/phases/13-ensemble-formalization/13-02-SUMMARY.md

<!-- Numbers come from experiments/results/ensemble/summary.json — read it first -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PAPER_DRAFT.md §5.11 and update §4.4</name>
  <read_first>
    - experiments/results/ensemble/summary.json (ALL numbers — read before writing any prose)
    - PAPER_DRAFT.md lines around §5.10 Feature Ablation (find exact insertion point after §5.10)
    - PAPER_DRAFT.md §4.4 Live System Architecture (find current text to update)
    - .planning/phases/13-ensemble-formalization/13-RESEARCH.md §Paper Output section
    - FINDINGS.md (last ~30 lines to confirm Finding 25 is the last and Finding 26 is next)
  </read_first>
  <files>PAPER_DRAFT.md</files>
  <action>
Read experiments/results/ensemble/summary.json to extract all actual numbers before writing any prose. Do NOT fabricate or use placeholder numbers.

SECTION 5.11 — Insert after the end of section 5.10 Feature Ablation, before any existing §6 or appendix sections:

```markdown
### 5.11 Ensemble Formalization

The live trading system deploys an LR + XGBoost ensemble with a concordance filter: trades are skipped when the two models disagree on direction (sign disagreement). We formally evaluate four ensemble variants and audit the concordance filter's effect on reported performance.

**Ensemble Variants**

| Variant | Members | Concordance | Trades (filtered) | P&L (filtered) | P&L (unfiltered) | Rejection Rate | P&L (rejected) | P4 flag |
|---------|---------|-------------|-------------------|----------------|------------------|----------------|-----------------|---------|
| (a) LR alone | LR | none | [N from json] | $[X] | $[X] | 0% | — | — |
| (b) LR + XGB equal-weight | LR, XGB | strict | [N] | $[X] | $[X] | [R%] | $[X] | [Yes/No] |
| (c) LR + LSTM equal-weight | LR, LSTM | strict | [N] | $[X] | $[X] | [R%] | $[X] | [Yes/No] |
| (d) LR + XGB + LSTM strict | LR, XGB, LSTM | strict | [N] | $[X] | $[X] | [R%] | $[X] | [Yes/No] |

*Concordance filter: trade is taken only when all members agree on sign. P&L (rejected) is the counterfactual P&L had rejected trades been taken. P4 flag fires when rejected trades are profitable in aggregate.*

**Weight Sensitivity**

We sweep LR-weight from 0.0 to 1.0 in 0.1 increments for the LR + XGB variant, holding XGB-weight = 1 - LR-weight. Figure [N] shows P&L as a function of LR weight for both filtered and unfiltered concordance conditions.

[Insert Figure: experiments/figures/ensemble_weight_sweep.png — Caption: "Figure N: Ensemble weight sensitivity for LR+XGB. P&L is near-flat across all LR weight values, confirming that the weight choice is not the primary driver of performance."]

[IF weight sweep is near-flat ($span < $20):] The weight sweep is near-flat across all 11 tested values ($[min] to $[max] range), confirming that the LR-XGB weight choice is not material: LR (+$201.69) and XGBoost (+$201.63) are functionally tied on this dataset. The concordance filter is the dominant discriminator, not the weighting scheme.

[IF P4 flag fired for any variant:] Note: The concordance filter rejected trades that were profitable in aggregate for variant [X] (rejected P&L = $[Y]). This confirms P4 (concordance filter denominator trap): the filter improves reported Sharpe by selectively eliminating ambiguous trades, some of which had positive expected value. We report both filtered and unfiltered P&L throughout to prevent Sharpe inflation.

**Interpretation**

The live ensemble (variant b) is evidence-justified: the concordance filter reduces trade count from [N_unfiltered] to [N_filtered] (rejection rate [R%]) and the filtered P&L of $[X] is [higher/lower] than unfiltered $[Y]. [Honest statement based on actual numbers.] The ensemble weight is not cherry-picked: the weight sensitivity sweep shows the choice is immaterial across the tested range.
```

UPDATE SECTION 4.4 — Find the existing §4.4 Live System Architecture text and add the following paragraph (or replace the ensemble description if one already exists):

```markdown
The live system uses an LR + XGBoost ensemble with equal weights and a concordance filter (Section 5.11). Formal evaluation across four ensemble variants and an 11-point weight sensitivity sweep confirm this design: ensemble weight choice is not material (P&L range < $[X] across all tested weights), and the concordance filter is the primary risk control. The filter rejects [R%] of potential trades and [increases/reduces] net P&L by $[delta]. EnsemblePredictor is not wired into the live strategy during this evaluation period (v1.1 non-goal); the live system remains hardcoded to the current LR+XGB average for safety.
```

All dollar amounts, trade counts, and rejection rates MUST come directly from experiments/results/ensemble/summary.json. No approximations.
  </action>
  <verify>
    <automated>
      grep -q "5.11 Ensemble\|### 5.11" /Users/iansabia/Desktop/DS340\ Project/PAPER_DRAFT.md
    </automated>
  </verify>
  <acceptance_criteria>
    `grep -q "5.11 Ensemble\|### 5.11" PAPER_DRAFT.md` exits 0.
    `grep -q "4.4" PAPER_DRAFT.md` exits 0 and §4.4 contains "concordance filter" with actual numbers.
    No "[X]" or "[N]" placeholders remain — all replaced with real numbers from summary.json.
    Weight sweep figure referenced in text with path or caption.
  </acceptance_criteria>
  <done>
    §5.11 written with actual numbers and honest concordance audit. §4.4 updated with ensemble justification. No placeholder values remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Finding 26 to FINDINGS.md</name>
  <read_first>
    - FINDINGS.md (last 30 lines — confirm Finding 25 is the last entry, get formatting pattern)
    - experiments/results/ensemble/summary.json (numbers for finding)
  </read_first>
  <files>FINDINGS.md</files>
  <action>
Append Finding 26 to FINDINGS.md following the same format as Finding 25.

Finding 26 documents: ensemble weight immateriality and concordance filter behavior.

Required content:
- Title: "Finding 26: Ensemble Weight Is Immaterial; Concordance Filter Is the Primary Discriminator (Phase 13, April 2026)"
- Core finding: weight sweep was near-flat (or the actual range in dollars)
- Concordance audit: rejection rate, filtered vs unfiltered P&L per variant (b)
- P4 flag status: honest statement of whether rejected trades were profitable
- Implication for live system: the LR+XGB concordance filter is evidence-based risk control

Numbers MUST come from experiments/results/ensemble/summary.json. Do not fabricate.
  </action>
  <verify>
    <automated>
      grep -q "Finding 26" /Users/iansabia/Desktop/DS340\ Project/FINDINGS.md
    </automated>
  </verify>
  <acceptance_criteria>
    `grep -q "Finding 26" FINDINGS.md` exits 0.
    Finding 26 appears after Finding 25 in the file.
    Contains actual numbers (rejection rate, P&L values, weight sweep range).
    No placeholder text.
  </acceptance_criteria>
  <done>
    Finding 26 appended to FINDINGS.md with real numbers. Phase 13 empirical record is complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 13 deliverables:
    - src/models/ensemble.py (EnsemblePredictor class)
    - tests/models/test_ensemble.py (11 passing tests)
    - experiments/run_ensemble_sweep.py (4 variants + concordance audit + weight sweep)
    - experiments/results/ensemble/summary.json (4 variants, 11 weight-sweep points)
    - experiments/figures/ensemble_weight_sweep.png
    - PAPER_DRAFT.md §5.11 (with real numbers, no placeholders)
    - PAPER_DRAFT.md §4.4 updated
    - FINDINGS.md Finding 26
  </what-built>
  <how-to-verify>
    1. Check tests pass: `cd /path/to/project && .venv/bin/python -m pytest tests/models/test_ensemble.py -v`
       Expected: all tests pass.

    2. Check concordance audit output: `PYTHONPATH=. .venv/bin/python experiments/run_ensemble_sweep.py 2>&1 | head -40`
       Expected: Table with 4 variants, filtered vs unfiltered P&L, rejection rate. P4 WARNING if applicable.

    3. Check weight sweep figure: open experiments/figures/ensemble_weight_sweep.png
       Expected: Two lines (filtered / unfiltered), x-axis 0.0 to 1.0, y-axis in dollars. Check if near-flat.

    4. Check paper: open PAPER_DRAFT.md and navigate to §5.11
       Expected: Ensemble table with 4 rows, all numbers filled in (no "[X]" placeholders). §4.4 mentions concordance filter with numbers.

    5. Confirm ENSM-05 safety: `git diff src/live/strategy.py`
       Expected: Empty output.

    6. Read Finding 26 in FINDINGS.md.
       Expected: Honest assessment of weight sweep flatness and P4 flag status.

    If P4 flag fired (rejected trades profitable): confirm the paper honestly states this rather than burying it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues that need correction</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
```
.venv/bin/python -m pytest tests/models/test_ensemble.py -x -q
```
All pass.

```
grep -c "5.11\|Ensemble" PAPER_DRAFT.md
```
Non-zero count.

```
jq '.variants | length, .weight_sweep | length' experiments/results/ensemble/summary.json
```
Returns 4 and 11.

```
git diff src/live/strategy.py
```
Empty.
</verification>

<success_criteria>
- PAPER_DRAFT.md §5.11 exists with 4-variant ensemble table and all numbers from summary.json
- PAPER_DRAFT.md §4.4 updated with evidence-based ensemble justification
- FINDINGS.md Finding 26 added with actual numbers
- Weight sweep figure referenced in §5.11
- P4 flag outcome (profitable or not) honestly reported
- Human verification checkpoint passed
- ENSM-05 confirmed: strategy.py unmodified throughout all Phase 13 commits
</success_criteria>

<output>
After completion, create `.planning/phases/13-ensemble-formalization/13-03-SUMMARY.md`
</output>
