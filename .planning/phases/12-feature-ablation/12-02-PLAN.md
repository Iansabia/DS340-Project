---
phase: 12-feature-ablation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - PAPER_DRAFT.md
  - FINDINGS.md
autonomous: true
requirements: [ABLA-08]

must_haves:
  truths:
    - "PAPER_DRAFT.md contains a new `### 5.10 Feature Ablation` section after §5.9"
    - "§5.10 contains the full 12-row ablation table with all columns (no cherry-picking)"
    - "§5.10 references the pre-registration commitment and `.planning/ablation_protocol.md`"
    - "FINDINGS.md contains `## Finding 25` with the minimum sufficient set result"
    - "§5.10 includes a future-work hook about re-running ablation at 250+ bars/pair"
  artifacts:
    - path: "PAPER_DRAFT.md"
      provides: "§5.10 Feature Ablation section (~0.5 page)"
      contains: "### 5.10 Feature Ablation"
    - path: "FINDINGS.md"
      provides: "Finding 25: minimum sufficient feature set"
      contains: "## Finding 25"
  key_links:
    - from: "PAPER_DRAFT.md §5.10"
      to: "experiments/results/ablation/summary.json"
      via: "All numbers in §5.10 sourced from summary.json — NO fabrication"
      pattern: "grep '5.10' PAPER_DRAFT.md"
    - from: "FINDINGS.md Finding 25"
      to: "experiments/results/ablation/report.md"
      via: "Minimum sufficient set determination from ablation-holdout results"
      pattern: "Finding 25"
---

<objective>
Write paper section §5.10 "Feature Ablation" using actual numbers from experiments/results/ablation/summary.json, and add Finding 25 to FINDINGS.md documenting the minimum sufficient feature set.

Purpose: Converts the ablation experiment results into a paper-ready section that adds the parsimony finding to the complexity-vs-performance narrative.
Output: New §5.10 in PAPER_DRAFT.md and Finding 25 in FINDINGS.md.
</objective>

<execution_context>
@/Users/iansabia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iansabia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-feature-ablation/12-01-SUMMARY.md
@.planning/phases/12-feature-ablation/12-RESEARCH.md

<interfaces>
<!-- Structure of summary.json produced by Plan 01 -->
```json
{
  "train_proper_rows": 5780,
  "ablation_holdout_rows": 1020,
  "final_test_rows": 1673,
  "configs": [
    {
      "model": "LR",
      "dropped_group": "none",
      "feature_count": 51,
      "pnl": 232.67,
      "delta_pnl": 0.0,
      "rmse": 0.3063,
      "directional_accuracy": 0.667,
      "ci_lower": null,
      "ci_upper": null,
      "num_trades": 420,
      "num_bootstrap": 1000
    }
    // ... 11 more entries
  ]
}
```

<!-- Table format for report.md (also for §5.10) -->
| Model | Dropped Group | # Features | P&L @ 2pp | ΔP&L | 95% CI of ΔP&L | RMSE | Dir. Acc. |

<!-- Last paper section before the insertion point -->
### 5.9 Live vs Backtest Reconciliation
<!-- Insert ### 5.10 Feature Ablation after §5.9, before ## 6. Discussion -->

<!-- Last finding -->
## Finding 24: TFT Negative Result at N=6,802 (Phase 11, April 22, 2026)
<!-- Insert ## Finding 25 after Finding 24 -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read ablation results and write §5.10 + Finding 25</name>
  <read_first>
    - experiments/results/ablation/summary.json (ACTUAL numbers — read before writing any prose)
    - experiments/results/ablation/report.md (12-row table for direct paste)
    - PAPER_DRAFT.md lines 381-441 (end of §5.9, before §6 Discussion — insertion point)
    - FINDINGS.md lines 434-end (end of Finding 24 — insertion point)
    - .planning/phases/12-feature-ablation/12-RESEARCH.md (Section 9 paper integration spec, Section 7 expected outcome, Section 8 reporting format)
  </read_first>
  <files>PAPER_DRAFT.md, FINDINGS.md</files>
  <action>
CRITICAL: Read `experiments/results/ablation/summary.json` FIRST and extract all actual numbers before writing any paper text. Every P&L, CI, RMSE, and directional accuracy value in §5.10 MUST come from this file. Do NOT use placeholder numbers or the RESEARCH.md baseline numbers (those are pre-run estimates).

**Step 1 — Determine minimum sufficient set from actual results:**

From summary.json, for each dropped_group, check BOTH models:
- Is delta_pnl < -$10 for BOTH LR and XGBoost? → group is "load-bearing"
- Is the 95% CI entirely below zero for BOTH models? → strong evidence
- If CI straddles zero: "inconclusive, pending more data"
- Otherwise: "droppable"

Select the minimum sufficient set: all groups classified as load-bearing. Report the final-test P&L for the minimum set from summary.json (the "selected_set_final_test" entry if present, else note it was evaluated on ablation_holdout).

**Step 2 — Write §5.10 in PAPER_DRAFT.md:**

Insert AFTER the end of §5.9 (after its last paragraph) and BEFORE `## 6. Discussion`. The section must be ~0.5 page (not a centerpiece). Structure:

```markdown
### 5.10 Feature Ablation

To guard against p-hacking, we pre-registered the ablation protocol at `.planning/ablation_protocol.md` before any experiment ran (commit timestamp verifiable in repository history). We applied Leave-One-Group-Out (LOGO) ablation across five pre-specified feature groups on both LR and XGBoost, using a three-way temporal split: train\_proper (earliest 85% of training data, ~5,780 rows), ablation\_holdout (latest 15%, ~1,020 rows) for feature-set selection, and final\_test (1,673 rows, original test set) held out until after selection was frozen.

**Feature Groups.** The 51 model features were partitioned into five non-overlapping groups: (A) raw aligned OHLCV (15 features), (B) cross-platform basics including spread and divergence metrics (10 features), (C) rolling and momentum indicators (6 features), (D) classical microstructure estimators — Amihud illiquidity, Kyle lambda, Roll spread, Corwin-Schultz spread, high-low volatility (13 features), and (E) prediction-market-specific dynamics including longshot score and trade-size extremes (7 features).

**Results.** Table X presents all 12 LOGO configurations (6 per model). No configurations were omitted. [INSERT THE EXACT 12-ROW TABLE FROM report.md HERE — copy verbatim]

**Minimum Sufficient Set.** [Write 2-3 sentences interpreting actual results. Example template — fill in with real outcomes:]
Groups [list load-bearing groups] proved load-bearing: dropping [group name] reduced LR P&L by $[delta] (95% CI: [$ci_lower, $ci_upper]) and XGBoost P&L by $[delta]. Groups [list droppable groups] were droppable: neither 95% CI excluded zero, consistent with Finding [10/12] that [these features] are data-starved at N=6,802. The minimum sufficient set — Groups [letters] ([N] features, [pct]% of 51) — achieved final-test P&L of $[actual_number] on LR versus $[baseline_final_test] for the full 51-feature model, a [direction] of $[difference].

If any CI straddles zero with |mean| > $10, write: "Group [X] was inconclusive: the 95% CI for delta-P&L straddled zero ([$ci_lower, $ci_upper]), indicating insufficient power at this dataset scale to classify the group as definitively load-bearing or droppable."

**Future Work.** Classical microstructure estimators (Group D) are Nyquist-starved at the current 4-hour bar interval; ablation should be re-run at 250+ bars/pair where Amihud and Kyle lambda become estimable with less noise [cite Finding 10 and Finding 12].
```

Use the exact 12-row table from `experiments/results/ablation/report.md`. Paste it verbatim — do not reformat or omit rows.

**Step 3 — Write Finding 25 in FINDINGS.md:**

Insert after the last line of Finding 24. Format:

```markdown
## Finding 25: Minimum Sufficient Feature Set — [N] Features Carry Full Edge (Phase 12, April 2026)

**Experiment:** LOGO feature ablation, pre-registered at `.planning/ablation_protocol.md`.
**Protocol:** 12 configurations = {LR, XGBoost} × {baseline, drop\_A, drop\_B, drop\_C, drop\_D, drop\_E}. Three-way temporal split: train\_proper (~5,780 rows), ablation\_holdout (~1,020 rows), final\_test (1,673 rows).

**Load-bearing groups:**
[List each group that meets ALL three criteria: CI below zero, |delta| > $10, DA drops > 2pp for both models]
- Group [X] ([N] features): LR delta $[value] (95% CI [$lo, $hi]); XGBoost delta $[value] (95% CI [$lo, $hi])

**Droppable groups:**
[List groups whose 95% CI straddles zero or |mean| < $10]
- Group [X] ([N] features): CI straddles zero → inconclusive / droppable

**Minimum sufficient set:** Groups [letters] = [N] features ([pct]% of 51).
**Final-test P&L (minimum set):** LR $[value] vs. all-51 baseline $232.67 on final_test.

**Implication:** [Concise 1-2 sentences tying to the parsimony narrative and the research question about complexity vs performance. Example: "This confirms that [fraction] of engineered features are dead weight at N=6,802; the edge is carried by raw prices and cross-platform spread signals, not microstructure."]

**Caveat:** Ablation-holdout P&L (selection metric) is not the generalization metric. Final-test result is the only reportable number. CI estimates on ~1,020 holdout rows are wide; re-run at 250+ bars/pair for tighter inference.
```

Fill every placeholder with actual numbers from summary.json. Do not fabricate.

After both edits are complete, commit:
```
cd "/Users/iansabia/Desktop/DS340 Project" && git add PAPER_DRAFT.md FINDINGS.md && git commit -m "docs(12-02): add §5.10 Feature Ablation and Finding 25 with actual ablation results"
```
  </action>
  <verify>
    <automated>cd "/Users/iansabia/Desktop/DS340 Project" && grep -n "### 5.10 Feature Ablation" PAPER_DRAFT.md && grep -n "## Finding 25" FINDINGS.md && grep -c "^|" PAPER_DRAFT.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "### 5.10 Feature Ablation" PAPER_DRAFT.md` returns a match with line number
    - `grep -n "## Finding 25" FINDINGS.md` returns a match
    - §5.10 appears between the last line of §5.9 and the first line of `## 6. Discussion`
    - §5.10 contains a markdown table with `| Model | Dropped Group |` header row
    - `grep -c "^| LR\|^| XGBoost" PAPER_DRAFT.md` returns at least 12 (all 12 data rows present in paper)
    - §5.10 contains the string "ablation_protocol.md" (references pre-registration)
    - §5.10 contains the string "95% CI" (bootstrap CIs reported)
    - Finding 25 contains "minimum sufficient" and at least one concrete dollar amount from summary.json
    - Finding 25 contains "Caveat" section warning about ablation-holdout vs generalization
    - No number in §5.10 contradicts summary.json (reviewer spot-check: P&L for baseline configs matches json["configs"][0]["pnl"])
    - `git log --oneline -- PAPER_DRAFT.md | head -1` shows the commit
  </acceptance_criteria>
  <done>§5.10 is present in PAPER_DRAFT.md with the 12-row ablation table and actual numbers from summary.json. Finding 25 is in FINDINGS.md with minimum sufficient set determination and honest caveats. Both files are committed.</done>
</task>

</tasks>

<verification>
**§5.10 existence and placement:**
```bash
cd "/Users/iansabia/Desktop/DS340 Project"
grep -n "### 5.10 Feature Ablation\|## 6. Discussion\|### 5.9" PAPER_DRAFT.md
# §5.10 must appear between §5.9 and §6
```

**12-row table in paper:**
```bash
grep -c "^| LR\|^| XGBoost" PAPER_DRAFT.md
# Must be >= 12
```

**Pre-registration reference:**
```bash
grep "ablation_protocol" PAPER_DRAFT.md
# Must return at least one match in §5.10
```

**Finding 25 in FINDINGS.md:**
```bash
grep -n "## Finding 25" FINDINGS.md
# Must return a line number
```

**No fabrication (spot-check baseline P&L):**
```bash
LR_BASELINE=$(jq '.configs[] | select(.model=="LR" and .dropped_group=="none") | .pnl' experiments/results/ablation/summary.json)
grep "$LR_BASELINE\|$(printf '%.2f' $LR_BASELINE)" PAPER_DRAFT.md
# LR baseline P&L from json must appear in paper
```
</verification>

<success_criteria>
- `grep "### 5.10 Feature Ablation" PAPER_DRAFT.md` returns a match
- §5.10 contains the full 12-row ablation table (no cherry-picking)
- §5.10 references pre-registration at `.planning/ablation_protocol.md`
- §5.10 includes future-work hook about re-running at 250+ bars/pair
- Finding 25 is in FINDINGS.md with concrete numbers from actual results
- Finding 25 includes honest caveat about ablation-holdout vs generalization metric
- Both files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/12-feature-ablation/12-02-SUMMARY.md` following the standard summary template.
</output>
