---
phase: 07.2-live-paper-trading
plan: 03
subsystem: ml-pipeline
tags: [retrain, model-comparison, metric-delta, parquet, xgboost, gru, lstm, ppo]

# Dependency graph
requires:
  - phase: 07.2-live-paper-trading/07.2-01
    provides: "Live data collector with bars.parquet schema and --demo mode"
provides:
  - "RetrainPipeline class combining original + live data for model retraining"
  - "Before/after metric comparison across all 8 models"
  - "Retrain event history in data/live/retrain_log.json"
  - "CLI: python -m src.live.retrain with --skip-tier3 and --original-only flags"
affects: [paper-trading, weekly-retrain-cron, model-improvement-tracking]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Reuse run_baselines.py functions for consistent model training"]

key-files:
  created:
    - src/live/retrain.py
    - models/live/.gitkeep
  modified: []

key-decisions:
  - "Reuse run_baselines.py build_models/prepare_xy/prepare_xy_for_seq for identical training logic"
  - "Test set always fixed (test.parquet) for fair before/after comparison"
  - "Primary improvement criterion: RMSE delta < 0 (lower RMSE = better)"
  - "Naive/Volume baselines marked as '--' for improvement (deterministic, data-independent)"

patterns-established:
  - "Retrain log accumulates events as JSON array for historical tracking"
  - "Skip-tier3 flag for fast development iteration (PPO takes ~10 min each)"

requirements-completed: [LIVE-RETRAIN, LIVE-METRICS-DELTA]

# Metrics
duration: 3min
completed: 2026-04-06
---

# Phase 7.2 Plan 03: Auto-Retrain Pipeline Summary

**RetrainPipeline combining original train.parquet + live bars for all 8 models with before/after RMSE delta tracking to data/live/retrain_log.json**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-06T23:54:13Z
- **Completed:** 2026-04-06T23:58:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- RetrainPipeline class with load_combined_data, evaluate_baseline, train_and_evaluate_combined, compute_deltas, log_retrain_event, run methods
- CLI with --skip-tier3 (skip PPO for speed), --original-only (baseline validation), and configurable --threshold
- Validated dry-run: XGBoost RMSE 0.2857, GRU 0.2873, LSTM 0.2910, LR 0.3081 -- matches Phase 5/6 known values
- Graceful handling of missing bars.parquet (uses original data only with warning)
- retrain_log.json created with correct JSON structure tracking per-model before/after/delta/improved

## Task Commits

Each task was committed atomically:

1. **Task 1: Retrain pipeline with metric comparison** - `d29c2833` (feat)
2. **Task 2: Dry-run retrain with original data only** - validation run, no source changes needed

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `src/live/retrain.py` - RetrainPipeline class and CLI entry point (355 lines)
- `models/live/.gitkeep` - Directory for retrained model artifacts

## Decisions Made
- Reuse `run_baselines.py` functions (`_build_split`, `_feature_columns`, `prepare_xy`, `prepare_xy_for_seq`, `build_models`) for identical training logic across retrain and original evaluation
- Test set always fixed (data/processed/test.parquet) for fair before/after comparison
- Primary improvement criterion: RMSE delta < 0 (lower RMSE = better); small tolerance (1e-8) for float noise
- Naive/Volume baselines shown in table but marked "--" for improvement (deterministic, unaffected by training data)
- `--original-only` mode uses before==after for zero-delta validation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Retrain pipeline ready for integration with paper trading engine
- Can be cron'd for weekly retraining: `python -m src.live.retrain --skip-tier3`
- All 8 models validated on fixed test set with matching metrics from Phase 5/6
- Paper trading engine (07.2-02) can invoke retrain after collecting sufficient live bars

## Self-Check: PASSED

- FOUND: src/live/retrain.py
- FOUND: models/live/.gitkeep
- FOUND: .planning/phases/07.2-live-paper-trading/07.2-03-SUMMARY.md
- FOUND: commit d29c2833

---
*Phase: 07.2-live-paper-trading*
*Completed: 2026-04-06*
